# ══════════════════════════════════════════════════════════════════════════════
# 【本地偏差 · 本文件已被本仓替换，理由是"字节可复算"】
#   上游原始内容: 67 行 / 1425 B / sha256=2c621eaf0983ae89b1e4f4b1fc6acfe009da0cdaccd4323c02c08f5f337566a5（首 16 位 2c621eaf0983ae89）
#   原始内容保存在 `docs/UPSTREAM-PROVENANCE.md` 的附录里（逐字），并在该文档顶部登记为
#   **已知且刻意的偏差**。原始规则是 `* text=auto` 一族（Windows 仓库的便利设定）。
#   本仓要求 **磁盘字节 == 库内字节**（冻结基线/臂日志/语料指纹/M1–M4 全是字节摘要），
#   而 git 的属性**深层文件胜出** ⇒ 只改仓库根不够，必须把这一层也关掉。
#   ⇒ 因此这里改成 `* -text`（不做任何换行转换）。
#   复核命令（任一文本文件都应满足）：
#     sha256sum <file> == git cat-file blob HEAD:<file> | sha256sum
# ══════════════════════════════════════════════════════════════════════════════
* -text
