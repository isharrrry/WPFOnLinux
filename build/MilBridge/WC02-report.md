# WC02 报告 —— `D-G98` **「一格定罪」终局（判词 `K3`）** 落册 ＋ `TASK-0111` **要件改写** ＋ `TASK-0110` 追加 ＋ 一条仪器缺陷并入 `D-G107` ＋ 三条自伤照实记

> **车道 = WC02**（会话级前缀 `WC##`；目录 `~/wc02/`，**不用 `wNNNa` 命名**）。
> **任务** = 登记 `D-G98` 的「一格定罪」终局 ＋ 改 `TASK-0111`／`TASK-0110` 要件 ＋ 推一笔。
> **性质** = **纯文本编辑 ＋ 推送**（零 `dotnet`／零构建／零门禁／零应用／**不占槽**）。
> **判据先行** = `~/wc02/criteria.md`（**开工、动任何册/地图/声明表之前**写完并现算 sha16 = **`0fab7fd5ce05f96c`**，84 行）。
> **冻结基线（本件全程）= `#52` `27293fb5ab91b778`**；`docs/CURRENT-STATE.md:9` = `gen=#52`（**收尾复核：逐位未变**）。
> **报告自身口径**（照 `D-G104` 第三条"同一件两个值必须两行都给"）：**FULL** = `sha256sum build/MilBridge/WC02-report.md`；**去行口径** = `head -n -2 build/MilBridge/WC02-report.md | sha256sum | cut -c1-16`（本文件末两行是空行 ＋ 自证行）⇒ **两个值都写在 §⑨**。

---

## §① `D-G98` 追加段（**逐字**；追加 bullet、原文与上方各条追加**一字未动**）

落点 = `samples/WpfFeatureProbe/KNOWN-DEFECTS.md` 的 `D-G98` 段**末尾**（即在 `### D-G99` 之前；**行锚定**）。**本件 WC02 只登记、未改任何产品件**：

- 主 bullet：`- 🔴 **追加（车道 W124A，2026-09-23；本件 = 车道 **WC02** **只登记、未改任何产品件**；上面判词与各条追加**一字未动**）—— 「**一格定罪**」终局：判词 **`K3` = 两个候选机制在应用侧都被推翻**。…`
  - **来源注（逐字要件之一）**：`build/MilBridge/W124A-report.md`（**FULL `b38a054b71f64ecc…`／去行口径 `head -n -2 … | sha256sum | cut -c1-16` = `df292e7faf2e0239`／**339 行**）；判据 = `~/w124a/criteria.md`（**封存值（追加 §7.1 之前）= `57a624cae1d217af`**，同时刻读数记在其 `~/w124a/STATUS.md:11,18`；**追加后现算 FULL = `c6f2055675fb9aaf`**）。
  - 子 bullet **①** `WM_NORMAL_HINTS` **全程是常量**：27 条红腿逐字 `present=1 flags=0x10 PMinSize=1x1`、**`PMaxSize` 从未声明**；**更强读数** = **主臂 60 腿的 209 个 `SEG` 行去重后只有 1 个值** ⇒ **机制②否、机制①否**（`K1`/`K2` 结构上不可能成立）。
  - 子 bullet **★② 新事实**：**27/27 红腿几何序列 = `800x600 → 1280x1024 → 800x600 → 1280x1024`**（先**真的还原**、**0.1 s 内又被顶回最大化**）＋ `_NET_WM_STATE` **全程 `[FOCUSED]`**；旁证 = 应用自报布局里还原按钮停在最大化槽位（红 `scr=1183,1` vs 绿 `703,1`）。
  - 子 bullet **⚠️ 射程（必写）**：**只排除"提示"这一族**；**不证**那 0.1 s 内**是谁**发的申请（**属 `N3` 写者归因**）。
  - 子 bullet **③ 阳性对照**：注入"最大化后、还原前" ⇒ **`5/5` 让还原失败**（`PMinSize` 单独 `2/2` ＋ `PMinSize=PMaxSize` `3/3`；皆 `r_ok2=0`、停在 `1280x1024`）⇒ **装置有牙**；⚠️ **差异也写**：注入臂 `state` **仍是最大化**、产品红腿是 `[FOCUSED]` ⇒ **只证灵敏度、形态不同**。
  - 子 bullet **④ 趟数／分母**：主臂 **60 腿跑满**（`M2:R2` 33 ＋ `M1:R2` 27）、落地 `L=1`（= `m_ok`）**`60/60`**、**分母 `60`**、**红 `27`**；`SHIM=bd037229be8db4f6` 逐批印；槽等待合计 **> `3400 s`**；**作废趟 `1`**（`W124A-M2R2a-05`，宿主重启截断、无 `RESULT` 行 ⇒ **不入分母**）。
  - 子 bullet **⑤ 臂间红率差 10 倍、判词一字不变**：`3/33 = 9.1%` vs `24/27 = 88.9%` ⇒ **差在触发路径、不在提示值**；并落 **Fisher 两值都给**（见 §② 口径句）。
  - 子 bullet **🆕 取代声明**：§15m 族那句「**首选成因 = 应用那份过期的尺寸约束挡住还原**」**在应用侧已被本段推翻 ⇒ 该句作废、不许再当结论引用**；**上方各条追加原文一字未动**。**本号仍红、产品侧未修**。
  - 子 bullet **边界／`NOINFO`**：① "那 0.1 s 内是谁发的申请" **`NOINFO`**｜② 两臂红率差的**成因未定**｜③ 只登记，未改任何产品件／判据件／装置件，**未把任何红写成绿**。

### ①-b 本件 WC02 对 `W124A` 报告的**独立复算**（口径先写；`E3`）

**口径（写在 `~/wc02/criteria.md` §1 `E3`，跑之前）**：读 `~/w124a/logs/W124A-M1R2-*`／`-M2R2a-*`／`-M2R2b-*` 各腿的 `xhints.log`（`SEG` 行）与 `probe.txt`（`RESULT` 行）**逐趟现算**，**不经报告汇总表**。结果：

| 量 | 本件 WC02 现算 | 报告字段 | 判定 |
|---|---|---|---|
| 主臂腿数（有 `probe.txt`者） | **60**（`M1R2` 27 ＋ `M2R2a` 4 ＋ `M2R2b` 29） | `60`（33 ＋ 27） | **相同** |
| 缺 `probe.txt` 的腿 | **`W124A-M2R2a-05`（唯一一个，= 报告点名作废的那趟）** | 作废 1 趟 | **相同** |
| `SEG` 行总数（60 腿） | **209** | `209` | **相同** |
| `uniq(hints)` | **1**，逐字 `{present=1 flags=0x10 PMinSize=1x1}` | 1，同值 | **相同** |
| 落地证据 `m_ok=1` | **60/60** | `L=1` 60/60 | **相同** |
| 红（`RESULT` 含 `r_ok2=0`） | **27**（`M1R2` **24** ／ `M2R2b` **3** ／ `M2R2a` 0） | 27（`M1:R2` 24 ／ `M2:R2` 3） | **相同** |
| 红腿几何序列 | **27/27 全为 `800x600 1280x1024 800x600 1280x1024`**，其它形态 **0** | 27/27 | **相同** |
| 红腿 `state` | **每条红腿 `MAXIMIZED` 只出现 1 次、末态一律 `[FOCUSED]`（0 例外）** | "窗态确已还原" | **相同** |
| 非红腿序列 | 31 条三跳（`800 1280 800`）＋ 2 条 `800 1280 1280 800` | （报告未列） | **本件补记** |
| 旁证 `GEO_BTN_AFTER_R` | `scr=704.x` 组 **34** 行（`ButtonRestore scr=703,1`）／`scr=118x` 组 **27** 行（`1183,1`） | 红 `1183,1` vs 绿 `703,1` | **相同** |

⇒ **报告 §0／§3.2／§6 的承重读数逐个字段成立**（本件复算**不依赖**报告的汇总表）。

---

## §② `TASK-0111` 要件改写 ＋ 两条口径句（**逐字**）

落点 = `docs/ROUTES.md` 的 `TASK-0111` 行下（**行锚定**：插在该行既有 4 条 bullet 之后、`TASK-0703` 行之前）。**上面 §15i／§15k／§15m 的登记一字未动**：

- 主 bullet（**要件改写**）：`- 🔴 **要件改写（**主控裁定 · dated；车道 WC02，2026-09-23**…）**：**① `N1`（"值没变就不写提示"）撤销/暂缓** —— **理由（逐字）**：**提示族已被 `K3` 排除**（…`present=1 flags=0x10 PMinSize=1x1`、**`PMaxSize` 从未声明**、**209 个 `SEG` 行去重后只有 1 个值**）⇒ 先落 `N1` 只会拿到"**红率不变**"的**空结果**；且 §15k／§15m 那条旧理由（"撤回不了已发布的过期上限/下限"）**已随之失效** —— 那份"过期约束"**从来不存在**。**② 下一步 = `N3` 写者归因**：专抓"**还原后 0.1 s 内那一次几何申请**"（归因面从"几何写"扩到 `XChangeProperty`／`XSendEvent`／`XSetWMNormalHints`／`XSetInputFocus`／`XRaiseWindow`…，并**在红腿上对齐**），**且靶子用 `M1:R2` 臂**（红率 **`24/27 = 88.9%`**，功效最高；`M2:R2` 臂只有 **`3/33 = 9.1%`**）。`
- 口径句 bullet（**两条，逐字**）：`- 📌 **口径句（本波新发现，逐字两条；**必须同趟写进该族的预登记**）**：**（a）**「**两臂 `hints` 逐字相同，但红率相差 10 倍**（`M2:R2` **`3/33 = 9.1%`** vs `M1:R2` **`24/27 = 88.9%`**；Fisher 双尾 **`p ≈ 1.1e-10`** —— 报告值，⚠️ 本件 WC02 用超几何精确双尾复算同一张表 = **`1.96e-10`**，**两个值都给**，口径见 … `D-G98` 新追加段）⇒ **差在"触发路径"，不在"提示值"**」；**（b）**逐字：「**本族红率随触发路径（臂）而变 ⇒ 跨臂读数不许当同一件事合并**」 —— 即"**同一装置换个臂就换红率**"这件事**本身必须先写进预登记**，否则后人会把两臂的读数当同一件事。`
- 状态 bullet：`- ⚠️ **状态位仍 `🔴`**（`N3` 未落地、写者未归因 ⇒ **不许**写 ✅）；⚠️ §15i 那句"**先决条件逐字 = 待 W115A**"**已满足**（`W115A` 已收工），但**因为写者归因那一格未取，本行仍不转 ✅**。`

### ②-b ⚠️ Fisher 值：**两个值都给**（`D-G104` 第三条；不许只给一个）

| 来源 | 值 | 口径 |
|---|---|---|
| `W124A-report.md` §3 | **`p ≈ 1.1e-10`** | 报告自报（未写口径） |
| 本件 WC02 现算 | **`1.96e-10`** | 超几何精确双尾：`Σ P(表) ≤ P(观测表)`（含 scipy 同款 `1+1e-7` 松弛），表 = `[3,30;24,3]` |
| 本件 WC02 现算（备查） | `1.81e-10` ／ `3.67e-10` ／ `1.84e-10` | 分别 = `P(观测表)` ／ `2·min(单尾)` ／ 左尾 `P(X≤3)` |

⇒ **如实记：`1.1e-10` 本件未能用任何常见 Fisher 口径复现**（本件另扫了 40 组相邻表，多组都能给到 `≈1.1e-10` ⇒ **无法从报告字段反推出它用的是哪张表/哪种口径** ⇒ 那格归 **`NOINFO`**）。**结论不受影响**：两个值同为 `1e-10` 量级 ⇒ "**红率差 10 倍显著**"**不变**。

---

## §③ `TASK-0110` ／ 仪器缺陷 ／ 自伤 —— 追加逐字

### ③-1 `TASK-0110`（`docs/ROUTES.md`，行锚定；状态位行**原话未动**）

`  - 🔁 **`TASK-0110` 追加一行（车道 WC02，2026-09-23；上面各条登记**一字未动**）**：真因**部分排除** —— **提示族已被排除**（…判词 = **`K3`**…）… **应用侧那次几何申请已确证存在**（**27/27 红腿几何序列 = `800x600 → 1280x1024 → 800x600 → 1280x1024`**、`_NET_WM_STATE` 全程 `[FOCUSED]`…）**但未归因**（**属 `NOINFO`**）⇒ **状态位仍 🟡**（**不许**转 ✅）。`

### ③-2 仪器缺陷 → **并入 `D-G107`（不新号）**

**先写后判的理由（`~/wc02/criteria.md` §2.1，改册之前写的）**：① 册内**能容纳**这一格的号 = `D-G107`（要件② 逐字 = "**取值时射程必须按该对象的真实结构定义**"；本次坏的是**同一个要件的"遍历面"一侧**）；② `D-G104` **机械排除** —— 那条要件② 的形态是"**读数器自身坏了、不报警**"，而本次修前件是**大声失败**（`rc=5` 非零 ＋ 自带 `reason=no-window` ＋ 独立工具 `xdotool` 证明对象在场）；③ 同一结构陷阱**已在册**（`D-G64`，产品侧 `WindowFromPoint` 只返直接子窗 ⇒ 点击全被吞；修法逐字 = "**逐层下沉**"）⇒ **不满足"新号"条件** ⇒ **并入 `D-G107` ＋ 交叉引用 `D-G64`**。

追加内容（落点在 `D-G107` 段末尾、`### D-G108` 之前）：

- 主 bullet：`- 🆕 **同要件的第三个现场实例：本次坏在"遍历面"而非"解析面"（车道 W124A，2026-09-23；本件 = 车道 WC02 **只登记**；上面判词与各条追加**一字未动**）** —— …观测器 `~/w124a/bin/xhints` **第一版 `find_win` 只遍历 `root` 的直接子窗**；而 WM 把客户窗 **reparent** 进 frame ⇒ 目标是**孙代** ⇒ **找不到**。**成对读数**：**修前件** `bin/xhints-prefix`（`3e747a47869f00d1`）⇒ **`rc=5 reason=no-window`**（而 `xdotool` 独立确认窗口 **`10485764` 在场**）；**修后件** ⇒ **`WID=0xa00004`、`rc=0`**…`
- 子 bullet（**合并理由**，逐字）：`…本条要件② 已写死 —— "**取值时射程必须按该对象的真实结构定义**"；本次坏的正是**同一个要件的"遍历面"一侧**…⚠️ **`D-G104` 被机械排除**…`
- 子 bullet（**跨对象先例**）：`…**`D-G64`**（**产品缺陷**：`WindowFromPoint` 原先**只返回 `root` 的直接子窗口**…）⇒ **本次只是同陷阱在仪器侧复现** ⇒ 不满足本文预写的"新号"条件…`
- 子 bullet（**边界／`NOINFO`**）：真正跑出 `rc=5` 的第一版二进制**已被覆盖、未留副本** ⇒ 效力只来自"**同一份源 ＋ 只改那一处**＝单变量"；"全 `$HOME` 还有几处仪器只遍历一层"**未逐处枚举**。

### ③-3 自伤照实记（**不许缩小**）

**(a) `D-G103` 追加"实例清点"**（落点在 `D-G103` 段末尾）：

`- 🔁 **实例清点（车道 WC02，2026-09-23；上面各条**原文一字未动**，本条只清点 ＋ 划线）**：本条在册实例现为**四个** —— ① 教训表 `L14`（**第一次实例**）② **本条形态段**记的车道 **W124A** 诊断命令 `pkill -f 'HandyControlDemo.dll'` ③ 第二个现场实例（**W126A** 探活版）④ 第三个现场实例（**W127A** 自己 SIGTERM 自己）。⚠️ **必须划清的一条线（防并条）**：**W124A 那一例是 `~/w128a` 之外的另一个实例** —— 它命中的是 **W118A 的 `gdb --args dotnet HandyControlDemo.dll` 装置**、代价记在 **`~/w118a`**（41 趟里 **1 趟被判"外部打断"并剔出分母**，`G20301`…⇒ 分母 `41→40`），**与 `~/w128a` 批次的仪器缺陷（`D-G106`／`D-G107`，实例是 `W071`／`W077`）是两件不同的事** ⇒ **不许把 `~/w118a` 的样本损失记到 `~/w128a` 头上，也不许把两条并成一条**。⚠️ **无法自证的部分逐字保留**：W124A **无法证到它到底杀了几个进程**…`

**(b) `D-G101` 追加同族第三种机制**（`sed` 漏换 `W=$HOME/w105a` ⇒ 4 件写进禁改车道）＋ 口径句：

`- 🆕 **同族第三种机制（**"变量没换" ⇒ 生成脚本把文件落到别人车道**…）**：W124A 用 `sed` 从 `~/w105a/bin/xup-185.sh` **生成**自己的起 X 脚本时**漏换 `W=$HOME/w105a`** ⇒ `xvfb188.pid`／`xfwm188.pid`／`out/xvfb188.log`／`out/xfwm188.log` **四个文件写进 `~/w105a/`（禁改车道）**。**处置（现场已完成）**：**按 PID** 收掉那两个进程；四件**移出**到 `~/w124a/misfiled-w105a/`（**不删、留痕**）；复核 `~/w105a/` **已无我的残留**（`find … -newermt` ⇒ **0**）；起 X 脚本**改为手写**。…（与本条的"**共享 inode**"、`D-G108` 的"**索引 `-A`**"**三者同族 = "我以为我只动了我的"，机制各不同** ⇒ **并入 ＋ 交叉引用，不新号**）`
`  - **口径句（逐字）**：**凡"用脚本生成脚本/配置"的写法，命令行里任何 `$VAR` 或 `W=$HOME/<某车道>` 拼路径**，**必须先 `echo` 待执行命令全文再跑**；产物一律**写进本车道目录**，并对**目标父目录**做一次"**这是不是我的目录**"断言（本次是 `~/w105a` ⇒ **不是**）。`
（另附一行交叉引用：**第四种机制 = `git add -A`** 已由 `D-G108` 独立立号，本条**只交叉引用、不重复登记**。）

**(c) `D-G102` 追加同族另一形态**（编排器顺序错 ⇒ **腿根本没跑而输出看着正常**；输出目录被 `rm -rf` ⇒ 证据被删）：

`- 🆕 **同族另一形态（编排面：仪器的"等待对象"由被测者的下游创建 ⇒ 整腿没跑、而输出看着正常…）**：…**① 证据被被测者删掉**：把观测器输出**放在腿会 `rm -rf` 的路径名下** ⇒ 日志被删；**② 等待对象永远不出现**：**先起观测器等窗、后起腿** —— 而**应用是腿拉起来的** ⇒ 观测器在等**一个永远没人建的窗**（**34 s 超时**）⇒ **腿根本没跑**（而台账会显示"这一趟跑了、没抓到东西"）。**修法（已落地）**：腿**后台先起** → 观测器等窗（**看见即打 `WID=`**）→ 腿**前台等收**；**输出目录与腿的清目录路径分离**；**诊断凭据留档** `obs_logs/<tag>/FAILDIAG.txt`。…（**并入 ＋ 交叉引用，不新号**）`
`  - **交叉引用**：`D-G96`（**证据保全**）｜`D-G97`｜`D-G104`｜`D-G105`。`

---

## §④ `DEFREG` 两条机读行（现场跑两遍，`rc=0/0`，输出 `cmp` **IDENTICAL**）

```
DEFREG_DECLDRIFT=0 changed-route-files-since-DECL-GEN
DEFREG=PASS declared=145 route_ids=145（每个声明编号在其 req 的每个 route 文件里都在；无未声明编号）
DEFREG_DECL=n=145 route_ids=145 grammar=D-[A-Z][0-9]*[a-z]?(-[A-Za-z0-9]+)*
DEFREG_ROUTES=KD=edd5d6199fcdb733 CS=737c78e3a7e5a3a5 HO=e4dc264200b421d0 AB=27293fb5ab91b778
```

- **`declared` 不变 = `145`**（本批**不开新号** ⇒ 与 §15p／§15q 相同）；`--emit` 重生成后 **`ID` 行 diff = 0 行**（**零幻影声明**）。
- ⚠️ **`--emit` 现场不是 `--emit > 件`**（照 `D-G101` 教训"**落盘一律 `temp ＋ rename`**"）：本件用 `mktemp` 生成临时件 ⇒ `mv -f` 换入；件 `links=1`、**inode `4854448 → 4854450`**；件 sha16 `c8d59c5b8dc36ef1 → a8013a170c7dd367`（**内容差异只在 `DECL-GEN` 时间戳行与 `DECL-ANCHORS` 的 `KD` 锚** —— `KD` 由 `58d9056f497799ba` 变为本件编辑后的 `edd5d6199fcdb733`）。
- ⚠️ **`KRJ=f108775906eac9aa`** 是 `$R` 现场值（`#53` 在办件，**本件不许碰**）⇒ 在**远端**跑同一牙时该**非门禁诊断行**仍会读成 `DECLDRIFT=1`（唯一漂移项 `KRJ`），待 `#53` 推该件后自愈（与 §15q 同一条口径）。

---

## §⑤ 推送（前后 head ＋ `BYTECHECK` ＋ 件对账）

### 件对账（**改了几件 ↔ 暂存几件 ↔ 推了几件**）

| # | 件 | 改前 sha16 | 改后 sha16 | 行数变化 |
|---|---|---|---|---|
| 1 | `samples/WpfFeatureProbe/KNOWN-DEFECTS.md` | `58d9056f497799ba` | **`edd5d6199fcdb733`** | `2828 → 2852`（**+24**） |
| 2 | `docs/ROUTES.md` | `94bea50bd8bac16f` | **`cd51130c11c13032`** | `685 → 699`（**+14**） |
| 3 | `build/MilBridge/tools/defect-registry-declared.tsv` | `c8d59c5b8dc36ef1` | **`a8013a170c7dd367`** | `154`（不变） |
| 4 | `build/MilBridge/WC02-report.md` | （不存在） | **（新建；见 §⑨）** | 新建 |

**行数复核（防吞行）**：`wc -l` 改前/改后逐件现算，差 = **本件插入行数**（册 +24 / 地图 +14；两件**只增不减**）⇒ **零吞行**。

**推送机制**：`git fetch origin feat-Linux:refs/remotes/origin/feat-Linux`（refspec 陷阱）→ **逐径** `git add`（**无 `-A`／`--force`**）→ `git commit` → `git push origin feat-Linux` → **重新 fetch** ⇒ `git rev-parse HEAD` ＝ `git ls-remote origin HEAD` ＝ `refs/remotes/origin/feat-Linux`（**三者一致**）＋ `ls-remote --symref origin HEAD` 仍 `refs/heads/feat-Linux`。

- **开工 head** = `17fa2c6ae392e3dbeba2212c558283f525fd1b77`（＝ 车道 WC01 的 `RECORD-补` 笔；**开工时 `HEAD = origin/feat-Linux = ls-remote`**、`git status` 干净）。
- **推送后 head** / `BYTECHECK` / 笔数：**见本文件 §⑤-补（RECORD-补）** —— 该节在推送之后由**第二笔（仅报告自身）**补入并即时核对；**每笔推送前**用 `git status --porcelain` 与上表**逐件对账**。

---

## §⑥ `$R` ↔ 远端不同的件（单列）

**判据（先写）**：逐件 `cmp` `$R` 与克隆工作树；不同者单列，并标注是否属 `#53` 在办清单。
**扫描口径（现写）**：`$R`↔克隆**逐件 `cmp -s`**，覆盖面 = 克隆 `git ls-files` 的**全部在册件**（机械、非抽样；`cmp -s` 逐字节）。结果见 **§⑤-补**（与推送后的三方核对同批给出）。

**开工时已知的"本地领先（`#53` 在办）"候选**（主控点名，本件**一件都不许 add／改／当声明值**）：`verify-all.sh`（`mtime 12:10`）｜`docs/WAVE53-PREREGISTRATION.md`（`12:09`）｜`build/MilBridge/known-red.json`｜`src/WpfGfx.Linux.Native/src/{win32_core.c,win32_x11.c,win32_internal.h}`（`11:49:23`，W131A 在办值）。

---

## §⑦ `NOINFO` ／未做（如实，不缩小）

1. **`~/w124a/criteria.md` 的"封存值"本件未能独立复现 ⇒ 该格 `NOINFO`**。
   - **判据先写**的截断口径（`~/wc02/criteria.md` §1 `E2b` 逐字）用 `sed -n '1,/^## §7\.1/p'`（**两个 `#`**），而现场该节标题实为 **`### §7.1`（三个 `#`）** ⇒ **第一枪是我的口径写错**（**自记一条**：写判据时把分隔符数错了一位 ⇒ 该枪**空转**，属"**我没看到不一致 ≠ 我比过**"同族）。
   - 改用正确分隔符（`^### §7\.1`）＋ 逐个行数试（`head -n 130…141`）后**均不等于** `57a624cae1d217af`（最接近的 `head -n 139` = `bdf628ed90377a23`）⇒ **无法复原"追加前的字节"**（未知原件的末行结构）。
   - **处置**：册/地图里 `57a624cae1d217af` 一律按"**报告 §1 ＋ `~/w124a/STATUS.md:11,18` 两处独立留档**"**引用**，**不声称已复算**（**引用 ≠ 断言**）。
2. **Fisher `p` 的 `1.1e-10`** 未能识别其口径 ⇒ 那两个值本件**都给**（§②-b）；**不影响结论**。
3. **"那 0.1 s 内是谁发的几何申请"仍 `NOINFO`**（本件只登记上游的排除结论，**不做写者归因**）。
4. **未跑任何牙的 `--selftest`／任何门禁／任何构建／任何应用**（本件纪律：纯文本 ＋ 一次推送；`DEFREG` 牙是**只读对账**、非门禁，按任务书要求跑的）。
5. **"全 `$HOME` 还有几处仪器只遍历一层"／"几处 `pkill -f` 形态"／"几处 `--emit > 件` 原地写"**均**未逐处枚举** ⇒ `NOINFO`（本件只登记现场已出现的实例）。
6. **自伤自记（本件）**：① 上述 `E2b` 分隔符写错（空转一枪）；② `--emit` 第一次我按 `>` 直写思路跑，**发现 `--emit` 其实是"打到 stdout"**（`emit_decl()` 逐行 `printf`）⇒ 当场改为 **`mktemp` ＋ `mv`** 落盘（**未**发生原地截断：现场 `links=1`、inode 变更留档于 §④）。
7. **本件未改**：`samples/WpfTextDemo/ACCEPTANCE-BASELINE.md`（`27293fb5ab91b778` 未变）｜`docs/CURRENT-STATE.md:9`（`gen=#52` 未变）｜`handoff.md`（`e4dc264200b421d0` 未变）｜`verify-all.sh`／`docs/WAVE53-PREREGISTRATION.md`／`known-red.json`／三件 native 源（**`#53` 在办，mtime 全早于本件开工**）｜任何既有牙／任何产品件。
   - **机械证**：`find $R -newermt '2026-09-23 13:24' -type f`（排除 `obj/`／`bin/`）⇒ **共 4 件** = 本件的 `KNOWN-DEFECTS.md`／`docs/ROUTES.md`／`defect-registry-declared.tsv` ＋ **别的车道**的 `build/MilBridge/WC01-report.md`（**不是本件写的**）。

---

## §⑧ 大白话小结（≤5 行）

1. W124A 在**真的红的那 27 趟**上当场读了 X 侧尺寸提示：**全程只有 `minimum size: 1 by 1`、根本没有 `maximum size`**（60 趟 209 个采样行**去重后只有一个值**）⇒ "下限钉住"和"上限夹住"这两条候选，**在应用侧都被推翻**（判词 `K3`）。
2. 那些红趟是"**先真的回到 800x600、约 0.1 秒后又跳回 1280x1024**"，窗态**一直不是最大化** ⇒ 是**应用自己又申请了一次最大化几何**，不是被提示卡住；**但那是谁发的，本件没证**（只排除提示族）。
3. 所以我把 `TASK-0111` 的要件改了：**`N1`（少写提示）撤销/暂缓**（提示族已被排除 ⇒ 落它只会拿到"红率不变"的空结果），**下一步做 `N3` 写者归因、专抓那 0.1 秒里的那次申请、靶子用 `M1:R2` 臂**（88.9% 红，功效最高）；`TASK-0110` 仍 🟡。
4. 新教训按"**能并就并**"落：观测器第一版**只遍历一层** ⇒ 找不到被 reparent 的客户窗（**并入 `D-G107`**，同一陷阱产品侧先例 `D-G64`）；W124A 的 `pkill -f` 自杀式误杀**划清**为"`~/w128a` **之外**的另一个实例"（并入 `D-G103`）；`sed` 漏换变量把 4 件写进禁改车道（并入 `D-G101`）；编排器顺序错**腿根本没跑**（并入 `D-G102`）—— **本批零新号**。
5. 我自己也踩了两处：判据里截断分隔符**写错一位**（空转一枪 ⇒ 封存值那格如实记 `NOINFO`）、`--emit` 其实是打 stdout（**当场改成临时件 ＋ rename**）；`DEFREG=PASS declared=145 route_ids=145`／`DECLDRIFT=0`／`rc=0/0`，推送读数见 §⑤-补。

---

## §⑨ 报告自身 sha16（**两个口径都给**；写法 = "自指不自欺"）

本报告的 sha16 **由 `§⑤-补` 给出**，并且**分两次给**（照 §15p／§15q 的先例）：
- **`① 已推送那一版`**（本文件被 `git add` 时的内容）：用 **`git show <commit>:build/MilBridge/WC02-report.md | sha256sum`** 现算 ⇒ **从 git 侧取，不由我手抄**；
- **`② RECORD-补之后（含 §⑤-补 的最终版）`**：`sha256sum` ＋ `head -n -2 … | sha256sum` **两口径都写**在 `§⑤-补` 里。

⚠️ **为什么不在这里写死一个数**：本文件**自指** ⇒ 任何写进正文的 sha 都会**立刻过期**（写进去那一刻它就不是那个值了）。仓内已有的正确姿势 = **值落在"后一笔"里**（先例：`W130A`／`WC01` 的 `RECORD-补`），本件同法。

