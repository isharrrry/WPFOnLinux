# W100A 报告 —— `TASK-0203`／`TASK-9907` 收口登记 ＋ 地图 ＋ **第三笔文档推送**

车道 **W100A**｜任务 = 把最后两条 `[Next]` 的收口写进**册**与**地图**，并推第三笔文档
｜仓库根 `R=/home/links-dev/netTest/wpf-linux-20260906/wpf-linux`｜fork 克隆 `~/netTest/GitProj/WPFOnLinux`
｜**零 `dotnet`／零构建／零应用／不占重活槽**（全程未跑 `verify-all.sh`／`close-wave.sh`／`integration-wave.sh`／任何 `dotnet`）
｜时间：开工 `2026-09-22 19:0x`（+0800）
｜⚠️ 波 `#50` **已冻结并推送**（`#50 = 1f4189c1257737a9`，推送前远端 head = `e671f74a086d94ef`）

**读了哪些原始材料（先读、后写）**：`build/MilBridge/W98A-report.md`（整份 `74df2f1a289bcc45`，43,718 B）＋ `~/w98a/runs.tsv`（**129 行 × 33 列** = 表头 ＋ 128 趟）＋ `~/w98a/criteria.md`（`c2aeeceffae9757e`）＋ `build/MilBridge/W97A-report.md`（`a6fcd1c188725168`）＋ `build/MilBridge/tools/nul-bytes-check.sh`（`409d83d945f7d563`）＋ 册内既有 `D-G71`…`D-G92` 体例。

**我自己的独立复算（不是引用）**：用 33 列机读 `~/w98a/runs.tsv` 逐行重算 ⇒ `:95` 臂 A **60 趟全 `rc=124`**／臂 B **59 趟 `rc=134` ＋ 1 趟 `rc=139`**／`:96` 两臂各 2 趟 `rc=124`／`PCA` 2 趟 `124`／`PCB` 2 趟 `134`／`detect_power` 分布 `{yes:126, no:2}`／`clicks_landed` 分布 `{7:1, 8:125}`／`134` 族 61 趟里日志 `≥1M = 47`、`<1M = 14`（**14/61 = 23.0%**）／`void` 列全 `-`／唯一 `cand139` = `L1B024`。**与主控给的读数逐条一致**。

---

## ① 改了哪几件（before → after，**sha16 全部现场现算**）

| 件 | 改前 sha16 | 改后 sha16 | 变化 |
|---|---|---|---|
| `samples/WpfFeatureProbe/KNOWN-DEFECTS.md` | `5dc11e2e87073a39` | **`be6203dce80e9b81`** | 2458 → **2483** 行（新增 `D-G93`／`D-G94` 两条 ＋ `D-G87` 追加 2 条 dated bullet ＋ 节标题补一笔） |
| `docs/ROUTES.md` | `32df06d0f36cd7c9` | **`a69f0d3cbc4cfcb3`** | 367 → **390** 行（§13 树 2 处、§14 两行、新增 §15b） |
| `build/MilBridge/tools/defect-registry-declared.tsv` | `0f5fb2d6b8ab0234` | **`afab9819b82bd669`** | `--emit` 重生成（`declared 128 → 130`，内容 `diff` = **＋2 行／0 删／0 改**） |
| `build/MilBridge/W100A-report.md` | （无） | **本文件** | 新建 |
| `samples/WpfTextDemo/ACCEPTANCE-BASELINE.md`（冻结基线） | `1f4189c1257737a9` | **`1f4189c1257737a9`** | **未动（逐位相同）** |
| `docs/CURRENT-STATE.md` | `1ff381a9be8c3c7a` | **`1ff381a9be8c3c7a`** | **未动**（`:9` 机器行自然未动） |
| `handoff.md` | `e4dc264200b421d0` | **`e4dc264200b421d0`** | **未动** |

**本笔顺带补推的三件（此前未推，不是本车道产出，逐件点名）**：`build/MilBridge/W97A-report.md`（`a6fcd1c188725168`）、`build/MilBridge/W98A-report.md`（`74df2f1a289bcc45`）、`build/MilBridge/tools/nul-bytes-check.sh`（`409d83d945f7d563`）—— 三件都是**原样逐字节搬入克隆**（见 ⑤ 的 `BYTECHECK`）。

⚠️ **没推、但逐件点名的**（**派生件、非本波报告/登记件、产出车道未入库**）：`build/MilBridge/gen/tline-ledger-lines-20260921-1224.txt`／`-1231.txt`／`-1540.txt`／`-1623.txt`／`-1629.txt` —— 现场机械核：这 5 件**在 `$R` 有、在克隆没有、且 `git check-ignore` 说不被忽略**（`rc=1`）；`build/MilBridge/gen/` 本身有 **112 件已入库**。我**判断不归本件**（不属"本波报告与登记件"，产出车道未 add），故**未推、只点名**，供主控裁定。

---

## ② 两件收口的**逐字判词**（已写进 `docs/ROUTES.md` §14 与 §13 树、§15b）

### ②.1 `TASK-0203` → **🟡（写清"到哪一格、缺哪一格"；不是 ✅ 一刀切）**

> - `TASK-0203` [Next] 🟡 **静默 `139` 长跑 —— 已办，但只到 🟡（车道 W98A，报告 `build/MilBridge/W98A-report.md` `74df2f1a289bcc45`；主控已独立复核：台账 `~/w98a/runs.tsv` **128 行 × 33 列**逐行复算与我一致、`criteria.md` `c2aeeceffae9757e` 先于第一条样本）** —— **到哪一格**：
>   - ✅ **精度目标达成**：臂 A（现场权威件 `33352e5797031999`）无 WM 腿 **0/60** ⇒ 单臂 **95% 上界 `4.87% ≤ 5%`**（公式 `1-0.05^(1/60)`）；两臂合并 **1/126 ⇒ `2.35%`**（主判据腿 120 趟口径 = `2.47%` —— ⚠️ 报告 §6.1 用 120、§5.1b 用 126，**分母口径两处不同**，引用时指名）；`void=0`、`oom=0`、`MAXHold_KILL=0`。
>   - ✅ **`134` 族两极化干净成立**：腿1 臂 B（修前件 `abf6879c027c5e73`）**59/60 崩**（`rc=134`＋`Stack overflow.`）vs 臂 A **0/60**；崩溃点逐趟一致（第 8 击 `tab3`，`DEAD_AT_S`≈30–32 s）；阳性对照 `PCB 2/2 崩`（6,478,046 B／**19,393 B**）＋ `PCA 0/2` ⇒ 整套读数成立。
>   - ❌ **缺哪一格：`139` 族仍 `NOINFO`** —— 唯一 1 次命中 `L1B024` 落在**修前件臂**（真 `139`＋核心转储、**应用自己 0 字节**、7 击全落地后死在 `nav2`、`DEAD_AT_S=28`）⇒ **方向与 `D-G66` 那条改动集一致，但 1 次撑不起归因**（下界仅 ≈0.08%）；两臂各 `0/60` 之外只剩这 1 次。
>   - ⚠️ **判据级两个新洞（已登记 `D-G94`／并入 `D-G87`）**：① `clicks_total` 分母把 `CLICK … SKIP dead`（**没点**）算成"点了没落地" ⇒ **那次唯一真 `139` 会被自己的口径判成"无检测力"并剔除**（W98A 第一版就判错了；现行 `tried = ct − skipped` ⇒ `7/7 = 100%`、`detect=yes`）；② `.NET`／`timeout: 被监视的命令已核心转储`（43 B）**不是应用输出** ⇒ 判"应用 0 字节"必须先剔掉它；③ `D-G87` **第三次复核**：61 趟 `134` 里 **14 趟（23%）只写 18–19 KB 折叠形** ⇒ 日志体积不能当判别量。
>   - ⚠️ **验收装置第四次反证**：**无一条腿能在两臂上同时有检测力** —— 有 WM 腿（`:96`）**对修前件点击被吞**（`C2B1/C2B2` 逐击 `AE = 348243, 0, 0, 0, 0, 0, 0, 0` ⇒ 落地 `1/8`、`detect=no`）⇒ 那格 `0/2` **不算数**；与 `D-G64`／`D-G84`／`D-G85`／`docs/WAVE49-PREREGISTRATION.md` §13.4-⑦ 同读：**两条腿都要，且每格各自标明"有没有检测力"**。
>   - 🆕 **同趟新登记 `D-G93`**（装置缺陷 · 按关键词认对象族）：闸门 `pgrep -af '[v]erify-all\.sh…'` 命中**别家自己的 `bash -c` 轮询命令行** ⇒ **假死锁 300 s**（而槽当时是空的；旧口径 48 min 只走 5 趟／单趟平均等 840 s）⇒ 改用读 `/proc/*/cmdline` 的探测器 ＋ **成对自检 6/6**。
>   - ⚠️ 判决力上限与代价：两 shim 差 **25 个导出**（W85A 时 14，`#50` 落地 `A1`/`A2` 后变 25）⇒ **不许归因单一函数**；本机显示与用户现场（`:10` xrdp＋xfwm4）**不同构**；`rc=124` 只说"窗内没死"，**不说"没有该缺陷"**；**再压上界** `≤2% ⇒ N≥149/臂`、`≤1% ⇒ N≥299/臂`（≈3.1 h／6.2 h 每臂）。
>   - ⚠️ **下一条车道的建议（W98A §6.5，照抄）**：**别急着平铺 300 趟；先把"`139` 与 `134` 同源"坐实** —— 在修前件臂上把点击密度/时长推到必崩点，用 `gdb`／核心转储抓**原生栈**。
>   - `NOINFO`：`139` 归因（1 次）、两 `.so` 25 个导出里**具体哪一处**（未做二分）、有 WM 那格对修前件的**检测力标定**（未做）、用户现场同构复现（未做）。

**§13 树那一行**（同一条，只把旧判词换掉；**§14 与本处不再分叉**）：`│   ├─ TASK-0203 [Next] 🟡 **精度目标达成、`139` 族仍 `NOINFO`** …`，并把"代价与边界"两条挂成子行。

**⚠️ 我多做的一处（超出任务书字面，理由与边界写明）**：`TASK-0201`（`[MVP] 🟡`）那行的树行**判词与状态一字未改**，只加了**一条子行**引用 `TASK-0203` 收口后的上界 —— 因为 `§14b` 的 `TASK-0201` 仍写"15 趟零命中（上界≈20%）"，而 W98A 已在同签名上把上界压到 **4.87%**；**两处留着会构成"同一事实两套数"**（本仓明令不许）。**没有**把 🟡 改成别的。

### ②.2 `TASK-9907` → **✅（但写明"未接线"）**

> - `TASK-9907` [Next] ✅ **已办（但「未接线」；车道 W97A，报告 `build/MilBridge/W97A-report.md` `a6fcd1c188725168`，工具 `build/MilBridge/tools/nul-bytes-check.sh` `409d83d945f7d563`，683 行）** —— `D-G82` 的普查**已成牙**：现场 `NULBYTES=PASS files=1172 hits=0 bytes=249995686 skipdir_dirs=141 binext=280 noext=11 diag_noext_nonelf=0 canary=ok`、`rc=0`、**0.3 s**（主控现场重算为 `files=1171 bytes=249951968`，差 **1 件 = W97A 报告自己**，判据只与 `hits=` 有关）；`--selftest` **31/31 PASS**、含**四条"必红"＋三条"必 NOINFO"**的反极性；**历史重放能抓 `D-G82`**（W83A 修前备份 `f0d3d1501aebcd8c` ⇒ `NULBYTES=FAIL hits=1 path=…/wic_proxy.c offset=15873 line=289 n=3`，与 W83A 独立读数逐个吻合；换修后件 `8dc634b9254295f4` ⇒ `PASS`）。
>   - `TASK-9907` ⚠️ **边界口径（主控已裁定，接线时逐字进判据）**：本牙的绿**只**等于「**声明覆盖面里** 0 件含 NUL」**≠「全仓 0 件」** —— 判据外仍有三类**未判**：`upstream/**`（**6417 件，未测 ⇒ `NOINFO`**）、**11 件无扩展名 ELF**（首 NUL 恒在偏移 7 ⇒ **真二进制，只 `DIAG` 不判红**；判它就是**造假红**）、**280 件按定义是二进制**的件（扫它们对 `D-G82` 零信息）。
>   - `TASK-9907` ⚠️ **接线归波 `#51`**：现在接进 `verify-all` 第 `[27]` 步会把冻结的 `26 gen=#50` 步数声明**当场打成过期** ⇒ **本波不接**。⚠️ 接线时若按仓内惯例把它纳入 `fp_inputs()` 的 `printf` 名单，**必然移动 `inputs_fp`**（`$HOME` 副本模拟：`ee543f44… → 80c6fa0fa91f4fbe…`）⇒ 那一改**必须安排在 `IN_FP_0` 采样之前**。

**我自己现场的只读读数（本车道独立跑的，不是转述）**：
```
NULBYTES=PASS files=1172 hits=0 bytes=249995686 skipdir_dirs=141 binext=280 otherext=0 noext=11 diag_noext_nonelf=0 diag_otherext_nul=0 canary=ok
NULBYTES_SELF path=…/build/MilBridge/tools/nul-bytes-check.sh sha16=409d83d945f7d563        rc=0
```
⚠️ **件数差 1 件可逐条归因**（不是"读数漂移"）：W97A 那趟是 `files=1170`（17:5x）→ 主控读到 `1171` = **＋W97A 报告自己**（38,611 B）→ 我读到 `1172` = **再＋W98A 报告**（43,718 B；`249995686 − 249951968 = 43718` 逐字节对得上）。⇒ **这三趟的差异全部来自"新写的 .md 自己进了覆盖面"**，与 `D-G82` 的判据（只与 `hits=` 有关）无关。
⚠️ 我**只跑了只读扫描**，**没跑 `--selftest`**（避免被读成"跑门禁"）⇒ `--selftest 31/31` 与"历史重放抓 `D-G82`"两条读数**引自 W97A 报告 ＋ 主控现场重算**（复核来源已逐条注明）。

---

## ③ 新登记走的是"并入既有"还是"新号"—— **两条新号 ＋ 一条并入**，理由逐条

**先搜册（现场 `grep`，命中逐条列）**：

| 搜索词 | 命中 | 判断 |
|---|---|---|
| `pgrep` | `KNOWN-DEFECTS.md:546`（教训表 **`L14`**：`pgrep -f <样例名>` 命中别的主控轮询命令行 ⇒ 差点误杀别的车道）／`:964`／`:2073`（W46K `pgrep -f` **自匹配**留档）／`:2137`（另一族） | **是同一根因的第一次实例**，但**不是 `D-*` 编号**（是教训表行号），且**行本身在表格里**（往里塞长 bullet 会把表打坏）⇒ **不并，改交叉引用** |
| `假匹配`／`按关键词`／`图案匹配` | 只命中 `:1163` 一处，**语义不同** | 无合适既有条 |
| `0 字节`／`应用 0 字节` | 唯一相关 = `D-G87` 正文那句"`W63A` 那条"`139`＋0 字节 = 原生层"**只成立'原生不留日志'这一半**" | **同族（日志字节数当判别量）⇒ 并入 `D-G87`** |
| `检测力`／`分母` | `检测力` **0 命中**；`分母` 命中 `:756`／`:809`／`:826`／`:843`／`:1010`，**全是排版/像素族的分母**，与点击计数无关 | **无合适既有条 ⇒ 新号** |
| `D-G87`／`D-G84` 族 | `D-G87` = 日志体积；`D-G84` = 判据射程错 | `D-G87` 见上；`D-G84` 只作交叉引用 |

**最终选择**：

1. **`D-G93`（装置缺陷 · "按关键词认对象"族）** ← **新号**（现场末号 `D-G92`，连续取）。
   理由：① `L14` 是**教训表行**、不是缺陷编号，且**同一表格结构装不下**；② 本条有 `L14` 没有的三样东西 —— **假死锁 300 s 的成对读数**、**整批代价**（48 min 只走 5 趟 vs 换后 128 趟共让路 1,020 s）、**带成对自检的修法**（`WAVEPROBE-SELFTEST-ALL=PASS 6/6`）；③ 与 `D-G89`（判据恒真）／`D-G79`（判据认错对象）同族，本仓对**装置/判据缺陷**一贯给 `D-G` 号。
   **`L14` 原文一字未动**，只在 `D-G93` 里交叉引用（并点明它是"第一次实例"）。
2. **`D-G94`（判据缺陷 · 分母口径）** ← **新号**。
   理由：`检测力`／`分母` 在册**零合适条**（见上表）；本条后果是**把命中抹掉**（不是"漏报噪声"），且它第一次给出**分母的机器定义**（`tried = ct − skipped`）。
3. **`D-G87` ← 并入（加 dated bullet，原文一字未动）**：
   - 第三次复核（`134` 族 **14/61 = 23%** 折叠形）；
   - 新补 `W98A-F7`（`timeout` 的 43 B 不是应用输出 ⇒ 判"应用 0 字节"必须先剔掉）。
   理由：两条都在"**日志字节数当判别量**"这一格上，而 `D-G87` 的结论句已经点名了同一件事的另一半 ⇒ **不许再开第二条编号**。

**⚠️ 一处我如实记的"判断留给主控"**：若主控认为 `W98A-F1` **应当只并进 `L14`、撤掉 `D-G93`** —— 那是一次**单点回退**（删 `D-G93` 整条 ＋ 重跑 `--emit` ＋ 把 `ROUTES.md`／§15b 里那两处引用改成 `L14`），**其余改动不受影响**（`D-G94`／`D-G87` bullet 与它无关）。我**没有**在 `L14`／`L17` 等教训表里动一个字。

**登记体例自查**：两条都照既有 `D-G88`…`D-G92` 的形态（现象（读数）→ 判定点（`文件:行`）→ 成对读数 → 修法/处置 → 边界/`NOINFO`），并各带**交叉引用**与**不许读宽的边界句**；`D-G94` 另带一条"**判据文本与实现分叉**"的自我记账（见下）。

### ③.1 `D-G94` 里那条"判据文本 ≠ 实现"（**本趟新抓的一格，写全**）

- `criteria.md` §5（判据原文、先于第一条样本）写的是 `clicks_total ≥ 8 ∧ clicks_landed ≥ ceil(0.8×clicks_total)`；
- 现行实现（`$HOME/w98a/bin/record.sh:59-63`）是 `tried = ct − skipped`、`tried ≥ 7 ∧ cl×10 ≥ tried×8`；
- **报告 §1.3 的节选文本是第一版口径**（与 `criteria.md` 同）⇒ **三处文本 + 一处实现，两个口径**。
- **本批判决不受影响**（126 趟主判据腿全是 `tried=8`；把门槛换成 `≥8` 只让 `L1B024` 那一格变 `no`，而它本就单列），**但"文本与实现分叉"本身不许静默** ⇒ 已逐字写进 `D-G94`，并在地图 §14 里点名"引用必须指名出处"。

---

## ④ `DEFREG` 两条机读行（**连跑两遍、逐字相同、`rc=0`/`rc=0`**）

```
DEFREG_DECLDRIFT=0 changed-route-files-since-DECL-GEN
DEFREG=PASS declared=130 route_ids=130（每个声明编号在其 req 的每个 route 文件里都在；无未声明编号）   rc=0
```
配套读数（同两趟）：
```
DEFREG_DECL=n=130 route_ids=130 grammar=D-[A-Z][0-9]*[a-z]?(-[A-Za-z0-9]+)*
DEFREG_ROUTES=KD=be6203dce80e9b81 CS=1ff381a9be8c3c7a HO=e4dc264200b421d0 AB=1f4189c1257737a9
DEFREG_EXTRA=KRJ=8a0c0f221e35f42b KRF=ab09235afd949bc2 KRP=3c9e3a309b990d31
```
- **改前基线** `declared=126 route_ids=126` ⇒ **＋4**（`D-G93`／`D-G94` 两条编号 ＋ 两条 `req=KD`）；`--emit` 去时间戳/锚点后 `diff` = **＋2 行 / 0 删 / 0 改**（**只做加法有机证**）。
- ⚠️ **两个新号的 `req=KD` 只**（`D-G62+` 一贯如此）；`AB` 锚 `1f4189c1257737a9` **逐位未变** ⇒ 冻结基线**没被碰**（这也是我"没改冻结块"的机器证）。

---

## ⑤ 推送前后 head ＋ `BYTECHECK`

| 时点 | head |
|---|---|
| 推送前（远端 `origin/feat-Linux` ＝ 本地 `HEAD`） | `e671f74a086d94ef7f834d2aee5f6599c7002203` |
| **推送后** | **`462ef02c35c2d1373624b30868a5db30b873bcb4`** |

- 逐径 `git add --`（**未用 `-A`、未用 `--force`**）；`git fetch origin feat-Linux:refs/remotes/origin/feat-Linux`（**显式 refspec**，避开陷阱）→ `git push origin feat-Linux` ⇒ `e671f74…→462ef02…`。
- 核对：`git rev-parse HEAD` ＝ `git ls-remote origin HEAD` ＝ **`462ef02c35c2d1373624b30868a5db30b873bcb4`**；`git ls-remote --symref origin HEAD` 仍 **`ref: refs/heads/feat-Linux`**。
- commit 信息：`docs(#50): 0203 长跑／9907 NUL 牙收口 ＋ 新登记 D-G93/D-G94`（一笔，6 件）。
- `git diff --numstat`（本笔 = `e671f74…→462ef02…`）：`1859 insertions(+), 8 deletions(-)`；**8 条删除逐条核对过，全部是我有意重写的那 8 行**（`ROUTES.md` 5 行 = 3 条 §13 树行 ＋ §14 的 `TASK-0203`／`TASK-9907`；`KNOWN-DEFECTS.md` 1 行 = 节标题；`defect-registry-declared.tsv` 2 行 = `DECL-GEN`／`DECL-ANCHORS`）⇒ **没有误删任何既有判词**。

**逐件字节核对**（`git cat-file blob <rev>:<path>` 与磁盘 `cmp`；计数器在主 shell 累加，未用 `tee`）：
```
OK        samples/WpfFeatureProbe/KNOWN-DEFECTS.md
OK        docs/ROUTES.md
OK        build/MilBridge/tools/defect-registry-declared.tsv
OK        build/MilBridge/W97A-report.md
OK        build/MilBridge/W98A-report.md
OK        build/MilBridge/tools/nul-bytes-check.sh
OK        samples/WpfTextDemo/ACCEPTANCE-BASELINE.md
OK        docs/CURRENT-STATE.md
OK        handoff.md
BYTECHECK ok=9 mismatch=0 nobody=0  rev=462ef02c35c2d1373624b30868a5db30b873bcb4
```
（本报告自身的第二笔推送见文末 §9；报告无法自述"包含这一行的那一笔"的 sha —— **自指**，照 W95A 的先例如实不写一个会立刻作废的数。）

---

## ⑥ `fp_inputs` 影响判断：**零影响（机械证，不是推理）**

- 真调用覆盖面函数（`eval "$(sed -n '/^fp_inputs()/,/^}/p' build/close-wave.sh)"` 后调 `fp_inputs`，**不 source 整脚本**）：
  - **开工（改任何件之前）** = `ee543f44b1090c7498dcc2562ef69e1309764aaa2b92b8fa65e29d06a1e18f48`
  - **收工（改完 3 件之后）** = `ee543f44b1090c7498dcc2562ef69e1309764aaa2b92b8fa65e29d06a1e18f48`（**逐位相同**）
  - 该值 **== `#50` 冻结值**（`docs/ROUTES.md` §15 与 W99A 报告记的就是这个值）⇒ **无需重钉、无需重冻**。
- 覆盖面清单**当场点算 = 147 件**；本件编辑的 4 件（`KNOWN-DEFECTS.md`／`docs/ROUTES.md`／`defect-registry-declared.tsv`／`W100A-report.md`）在清单里 **命中全 0**（`grep -c` = 0）。⇒ **"零影响"是点数出来的，不是判出来的**。
- 另：本笔补推的 3 件（两份报告 ＋ `nul-bytes-check.sh`）同样**不在**覆盖面里（`nul-bytes-check.sh` **尚未**被纳入 `fp_inputs()` 的 `printf` 名单 —— 这是 W97A 报告 §7 点明的"接线时才会动"的那一格，**接线归 `#51`**）。

---

## ⑦ `NOINFO` / 未做清单（逐条如实）

1. **`TASK-0203` 的 `139` 族本车道没有新增读数** —— 我只做了**登记与地图**（零应用、零长跑）；W98A 的 `NOINFO`（1 次命中撑不起归因、25 个导出里具体哪一处、有 WM 那格的检测力标定）**原样保留**，我**没有**把它写成绿，也**没有**把它写成红。
2. **`D-G93` 的修法在仓外**（`$HOME/w98a/bin/`）⇒ 仓内**没有可复核的牙**；全仓"按关键词认进程/认对象"的同类写法**未逐处枚举**（已知四处：`L14`／W46K `:2073`／W77A `:412`／本条）。
3. **`D-G94`**：① `detect=no` 只有 2 趟 ⇒ "被吞到什么密度才算无检测力"**没有标定读数**；② 现行门槛 `≥7`（而非原文 `≥8`）**没有逐字论证**（只有机制注释）；③ 仓内 `grep` 核过：`clicks_total`／`clicks_landed` **在现役判据件里 0 命中** ⇒ 本条现场**只影响那套私有装置**。
4. **`--selftest` 未跑**（我这一趟）；`--selftest 31/31` 与"历史重放"两条读数**引自主控现场重算 ＋ W97A 报告**。
5. **5 件 `build/MilBridge/gen/tline-ledger-lines-20260921-*.txt` 未推**（派生件、产出车道未入库；机械判据见 ①）⇒ **逐件点名、留给主控裁定**。
6. **未跑任何构建／门禁／应用**（纪律遵守，不是未做到）；**未碰** `samples/WpfTextDemo/ACCEPTANCE-BASELINE.md`、`docs/CURRENT-STATE.md:9`、`verify-all.sh`、`build/close-wave.sh`、`integration-wave.sh`、`r-gate-step.sh`、`nul-bytes-check.sh`（**只读引用**）、`known-red.json`、两本 `known-red-*-copies.md`、任何产品件、任何 applier；**未用 `pkill -f`**（全程无进程处置动作）。
7. **没有锁、没有握手**：本车道与其它车道（若有）之间**没有互斥机制**——我这次**没有观察到**并发写入（收工前 `find -mmin -60` 只列出我自己的 3 件 ＋ W98A 的报告），但"没撞车"这条**只是时间序观察，不是机证**。

---

## ⑧ 大白话小结（≤6 行）

1. **最后两条 `[Next]` 收口了**：`TASK-0203` 写成 **🟡**（精度目标达成：单臂上界 **4.87% ≤ 5%**、`134` 族 **59/60 vs 0/60** 两极化干净；但**`139` 那一次只抓到 1 次、落在修前件臂，撑不起归因** ⇒ 仍 `NOINFO`，**不许当绿**）；`TASK-9907` 写成 **✅ 但"未接线"**。
2. **登记用了"两条新号 ＋ 一条并入"**：新号 **`D-G93`**（闸门 `pgrep -f` 命中别家 `bash -c` ⇒ **假死锁 300 s**，而槽当时是空的）、**`D-G94`**（把"**没点**"算进分母 ⇒ **唯一那次真 `139` 被自己的口径剔除**）；`D-G87` 里**并入**第三次复核（61 趟 `134` 里 **14 趟=23% 只写 18–19 KB**）＋ `timeout` 那 43 B 不是应用输出。
3. **为什么不全并进旧条**：`pgrep` 那个只有教训表行 `L14`（不是编号，且表格装不下），`检测力/分母` 在册**零命中** ⇒ 两条都够格开新号；`L14` 等旧文本**一个字没动**。
4. **门禁全绿、冻结物没碰**：`DEFREG=PASS declared=130 route_ids=130`、`DEFREG_DECLDRIFT=0`、`rc=0`（两遍）；`AB` 仍是 `1f4189c1257737a9`；`fp_inputs` 开工=收工=**`ee543f44…`**（== `#50` 冻结值）⇒ **零影响**。
5. **第三笔文档已推**：`e671f74… → **`462ef02c35c2d1373624b30868a5db30b873bcb4`**，`BYTECHECK ok=9 mismatch=0 nobody=0`，`symref` 仍 `feat-Linux`；顺手把**还没推**的 W97A／W98A 报告与 `nul-bytes-check.sh` 补齐。
6. **我没做到的**：没跑 `--selftest`、没做仓内同族全域扫、`139` 归因仍空、5 件 `tline-ledger-*.txt` 未推（已逐件点名）。

---

## §9 本报告自身的推送（可复算，自指那一段如实留白）

本报告是**第二笔**提交（只动 `build/MilBridge/W100A-report.md` 一个文件），紧随 `462ef02…`；推送后 head 与 `BYTECHECK` 追加行记在 `~/w100a/STATUS.md` 末条 —— **报告正文无法自述"包含这一行的那一笔"的 sha**（自指），照 W95A 报告 §7.3 的先例**不写一个会立刻作废的数**。
