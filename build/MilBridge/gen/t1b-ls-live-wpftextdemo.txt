== T1b · LS 绊线（真值来源：ld.so 的 LD_DEBUG=symbols 日志）==
日志 = /tmp/t1b-td/ld-all.txt（聚合自 1743 个进程日志，共 1778048 行）

-- LS 家族符号被查找的次数（Lo*/Ls*/Nl*/Fs*）--
     38 LoadWeight
     27 LoCreateContext
     27 LoadCursorA
      3 LsDisableSpecialCharacterLigature
      3 LoGetEscString
      3 LoadCursor

-- 关键符号：是否被查找过 / 查找传播范围 --
   判据（不猜命中位置，只报事实）：查找行数 = 1 ⇒ 第一个库里就有；行数 ≥ 6 ⇒ 整条依赖链都翻遍（MISS）
  LoAcquireBreakRecord: **没有**被查找过（0 行）
  LoCreateLine: **没有**被查找过（0 行）
  LoCreateContext: 被查找 27 行 ⇒ MISS（翻遍 9 个库没找到 ⇒ 真被调到就会抛 EntryPointNotFoundException）
  LoGetEscString: 被查找 3 行 ⇒ FOUND（只翻了 1 个库就命中：/tmp/wptd-run-761765/libwpfwin32.so）
  LsDisableSpecialCharacterLigature: 被查找 3 行 ⇒ FOUND（只翻了 1 个库就命中：/tmp/wptd-run-761765/libwpfwin32.so）
