== LS 绊线装置 · 自证 ==
（用一个**已知存在**与一个**已知缺失**的符号做对照；判据 = ld.so 日志 + nm -D 结论一致）

命令退出码 = 1
== T1b · LS 绊线（真值来源：ld.so 的 LD_DEBUG=symbols 日志）==
日志 = /tmp/tmp.vSaYNceLrJ/ld.724485（2557 行）

-- LS 家族符号被查找的次数（Lo*/Ls*/Nl*/Fs*）--
      9 LoCreateLine
      9 LoAcquireBreakRecord
      7 LoGetEscString
      4 LoadLibraryW
      4 LoadLibraryExA
      4 LoadLibraryA
      4 LoadImageA
      4 LoadCursorA
      1 LsDisableSpecialCharacterLigature

-- 关键符号：是否被查找过 / 查找传播范围 --
   判据（不猜命中位置，只报事实）：查找行数 = 1 ⇒ 第一个库里就有；行数 ≥ 6 ⇒ 整条依赖链都翻遍（MISS）
  LoAcquireBreakRecord: 被查找 9 行 ⇒ MISS（翻遍 9 个库没找到 ⇒ 真被调到就会抛 EntryPointNotFoundException）
  LoCreateLine: 被查找 9 行 ⇒ MISS（翻遍 9 个库没找到 ⇒ 真被调到就会抛 EntryPointNotFoundException）
  LoCreateContext: **没有**被查找过（0 行）
  LoGetEscString: 被查找 7 行 ⇒ FOUND（只翻了 4 个库就命中：/lib/x86_64-linux-gnu/libc.so.6）
  LsDisableSpecialCharacterLigature: 被查找 1 行 ⇒ FOUND（只翻了 1 个库就命中：/home/links-dev/netTest/wpf-linux-20260906/wpf-linux/src/WpfGfx.Linux.Native/bin/libwpfwin32.so）

  ✓ nm -D：LsDisableSpecialCharacterLigature **已定义**（与日志 FOUND 一致）
  ✓ nm -D：LoAcquireBreakRecord **未定义**（与日志 MISS 一致）

⇒ 装置自证 通过（捕获到的查找与 nm -D 事实一致）
  原始日志保留在 /tmp/tmp.vSaYNceLrJ/（未删，供复核）
