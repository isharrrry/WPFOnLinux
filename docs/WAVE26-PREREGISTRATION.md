# 波 `#26` 预登记（**落地前先登记；红了只许加严，不许放松**）

> 生成：主控，2026-09-17 13:4x，基于**当前冻结基线 `#25`**（权威 = `samples/WpfTextDemo/ACCEPTANCE-BASELINE.md`；⚠️ 按纪律 53 **本文件不重述整份 sha 的值** —— 唯一声明处是 `docs/CURRENT-STATE.md` 的 `BASELINE-FROZEN` 机器行，由 `verify-all` 第 `[7]` 步现场核对）。
> 承接：`docs/WAVE25-PREREGISTRATION.md` §10.4 的候选表（① `D-G10`｜② `D-G14` 列级闸｜③ `D-G9`｜④ `D-G11`｜⑤ `D-G12`｜⑥ `D-T5-R` 变有牙｜⑦ `D-T7` 真机重录｜⑧ `hasOverflowed` 更大判别面）。
> **本波的资源方针（用户要求：控内存 + 最大并行）**：见 §6。`#25` 实测信封 = `MemAvailable` 最低 1961 / 均值 2773 MB、`SwapFree` 最低 537 MB、`load1` 峰值 9.75（7 条车道 + 1 个构建者）⇒ **本波按同一形态铺开**。

## §1 内容（六条车道；**写域互不重叠**是硬约束）

| 车道 | 件 | `dotnet` | **独占写域**（越界即停） |
|---|---|---|---|
| **W26A** | **`D-G14` 列级覆盖闸**（把"整例跳过"改成"逐列判定"，只放行**与字体无关**的列）+ **`D-G12`**（`perChar[].width` 接进比较）——**同一份文件、一笔构建** | **是（本波唯一构建者）** | `build/MilBridge/tests/CoverageProbe/Program.cs` ＋ 三支 `tab-*` 臂日志的重取产物 |
| **W26B** | **`D-G10`**：`verify-all.sh` 绿分支回显**结论行** + `frame-step` 逐腿累加器（三颗牙齿**今天就在自报**，缺的只是 `run_step`） | 零 | **`verify-all.sh` 只有它能写** + `build/MilBridge/tools/frame-step.sh` |
| **W26C** | **`D-G13`（仪器级）**：`--selftest` 的 `SELFTEST_M2` 竞态 ⇒ **不一致时报 `NOINFO`、不再报 `FAIL`** | 零 | **`build/DirectWrite.Linux/wic-shim/check-applocal-sync.sh` 只有它能写** |
| **W26D** | **`D-G9`**：给 `known-red.json` 的 `generation` 加**结构化** `arm_logs` + 写只读核对器 `build/MilBridge/tools/arm-log-sha-check.sh` | 零 | **`build/MilBridge/known-red.json` 只有它能写** + 新建的核对器 |
| **W26E** | **`D-T5-R` 变"有牙"的方案草稿**（新写一步，用现成的 `D5CbrProbe`）——**只出草稿，不接线** | 零 | 仅 `$HOME/w26e/` |
| **W26F** | **对抗性审计**：独立复核 W25K 的"**① 有牙 24**"分类 —— 抽 8–10 条**逐条读码**确认"它真的会比较、且比较的字段真的进判据"，产出"**假有牙**"清单 | 零 | 仅 `$HOME/w26f/` |

**接线由主控在波尾做**：`D-G9` 的核对器接成 `verify-all` 第 `[8]` 步（**步数 13 → 14**）由**主控**在波尾执行（避免与 W26B 抢 `verify-all.sh`；且 `arm_logs` 的值必须在 W26A 重取臂**之后**重算）。

## §2 位移预测（**表外位移 ⇒ 停**）

| 位/量 | 预期 | 依据 |
|---|---|---|
| 九位（`bridge`/`pc`/`pf`/`wb`/`provider`/`win32shim`/`wic_shim`/`hbtextline`/`dwf`） | **全部不动** | 本波**不动产品件**：`CoverageProbe/Program.cs` 与三个 shell/JSON 都不产出九位件 |
| `inputs_fp` | **不动** | 覆盖面 = `patch-*.py` / `port-lib.py` / `integration-wave.sh` / `close-wave.sh` / `build/shims/**/*.cs` / `src/WpfGfx.Linux/**/*.cs` —— 本波**一个都不碰** |
| `known-red.json` 的 `generation` 三项（`GEN_KEYS`） | **不动**（`generation` 仍 `#23`） | 本波不动 `run.sh`/`Parity.cs`/shim |
| **三支 `tab-*` 臂日志** | **必变**（W26A 重取） | `CoverageProbe` 的字节变了 ⇒ **弱配对会失鲜**（纪律 34 的**真空档**：探针不在 `GEN_KEYS` ⇒ **没有机器会强制你重取**）⇒ **人工重取 + 记 `changelog`** 是唯一保障 |
| `tline`/`textlineproto` 两支臂 | **不重取** | 它们不经 `CoverageProbe` |
| **`verify-all` 步数** | **13 → 14**（波尾由主控接线 `[8] ARM-LOG-SHA`） | §1 |
| `ap_次`（应用门禁） | **必须重取**（`pc` 虽未变，但冻树牙齿的读数被 W26A 改动影响） | 纪律 46 的收官顺序 |

⚠️ **一条硬时序**：W26A 会**改写臂日志**，而 W26D 的 `arm_logs` 值必须取自**重取之后**。⇒ W26D 先按**当时**的值写入并在报告里**明说"若 W26A 之后重取臂，本值会变、核对器会正确报 FAIL"**；**波尾由主控重算并重注入**（这正好是一次"这颗牙真的会红"的现场演示）。

## §3 W26A 的判据（`D-G14` + `D-G12`）

**已备好的施工件**（`#25` 只读车道 W25I，**主控已实测可 `patch`**：`patch -p1` `rc=0`、产物与其参考件**逐字节相同**、仓内原件未被碰）：`~/w25i/column-gate.diff`（`9340d85dfcf91fc0`）+ `~/w25i/report.md`（`1ea7e434ded9386c`）。

- **目标收益（验收数）**：`tab-anchor` 的 `Start` 列 **判定行 421 → [569, 615]、`NOINFO` 194 → [46, 0]**；**RTL 非零 `Start` 由"行使 0 行"→"行使 33 行"**（`D-paraindent` hebrew RTL：PI24 15 + PI48 18；⚠️ **33 是真值侧可达数，我方实判 24~33** —— 下标 ≥1 的行需要我方行数 ≥2/3）。
- **正极性**：改后**既有 288 例/421 行判定一行逻辑未改**；`合计`/`跳过=`/`最大差=` 逐字节不变；`红=0`。
- **反极性（预测已给，必须实测）**：`Start => 0` ⇒ **171 红**（138 既有 + 新释放 24~33，下界 162）｜`=> PI+24` ⇒ **615**｜`=> Indent` ⇒ **278**；**行数不等必须走 `NOINFO`、不许误红**（判据：`红+绿+NOINFO == 615`）。
- **`D-G12`**：把 `perChar[].width` 接进比较（**今天读了不用**，356 条真值）；反极性 = 改一位 ⇒ 必红。
- ⚠️ **红线**：**不许动产品件**、**不许动 `build/shims/PresentationCore.HbTextLine.cs`**（动它 = 付世代成本，与本波预测冲突 ⇒ 停）。
- ⚠️ **接线边界**：本波只到"探针改好 + 重取臂 + 取到读数"，**不接进 `verify-all`**（那是下一波；理由：接线会改门禁的臂集合，须与 `tline-gate.sh:160-179` 的认臂逻辑同趟做，那是另一件）。

## §4 W26B/W26C/W26D 的判据

- **W26B**：`verify-all.sh` 绿分支回显一条 `^[A-Z][A-Z0-9_]*=(PASS|FAIL|NOINFO)` + 红分支"零命中兜底 `tail -12`" + 红分支印出被 `cp` 的日志路径；`frame-step` 加 1 行逐腿累加器（**终局行不能直接读 `$FR`**，否则印出"只剩最后一腿"的假数）。**只加不删**（机器证：改前独有行 = 0）。⚠️ **不许**改 `run_step` 的判据语义、**不许**把 `NOINFO` 与 `FAIL` 合并成第四态。
- **W26C**：`SELFTEST_M2` 的失败形态改成 `NOINFO`（**"两个时刻的仓库不同"是无信息，不是判据失败**）；`--selftest` **必须仍 18/18**，且**反极性要能证出：真坏 ⇒ `FAIL`；只是仓库被改 ⇒ `NOINFO`**（两条都要有读数）。**只许加严**。
- **W26D**：字段 = `generation.arm_logs`（键 = 文件名主干、**值 = 64 位全值**）；核对器三态、**缺声明 = `NOINFO`**、`rc=0` 只在全 PASS、`--selftest` 含"**前缀相等而全值不等 ⇒ 必红**"。⚠️ **加未知键的安全性已有机器证**（门禁只按 `GEN_KEYS` 取键、无 schema 校验器、两棵只差 `arm_logs` 的临时树跑门禁 `cmp` 逐字节相同）—— **但 W26D 必须自己再证一遍**（不许引述）。

## §5 停条件（**触发即停、如实上报**）

1. 九位里**任何一位变了** ⇒ 停（本波不该动产品件）。
2. `inputs_fp` 变了 ⇒ 停。
3. `build/shims/PresentationCore.HbTextLine.cs` 被碰 ⇒ 停。
4. `known-red.json` 的 `generation` 三项（`run.sh`/`Parity.cs`/`shim` 的 sha）变了 ⇒ 停。
5. 任何车道**改了不属于自己写域的文件** ⇒ 停、回退、登记。
6. 五臂门禁从 `PASS` 变 `NOINFO`/`FAIL` ⇒ 停（W26A 重取臂后**必须仍 `PASS`**；若因新红条目导致 `UNREGISTERED` ⇒ **停在"取到读数"这一步**，把新红如实上报给主控裁定，**不许自行登记或压绿**）。

## §6 资源与内存纪律（**用户要求：控内存 + 最大并行**）

- **同时最多 2 个 `dotnet` 进程**；**构建者只有 W26A 一个**，它的构建阶段**独占**（其余车道此刻只做 `grep`/读文件/纯文本编辑）。
- ⚠️ **"零 `dotnet`"必须连"会自行构建的步骤脚本"一起禁止**（纪律 51 细化）：`frame-step.sh:94` 自建 harness、`verify-all.sh` 第 [1] 步直接 `dotnet build`、`tline-gate.sh`/`pc-line-step.sh` 会跑探针 ⇒ **零 dotnet 车道不许跑这些脚本**。
- 构建旗标：`-m:1` + `DOTNET_gcServer=0`；**起步前 `MemAvailable` 必须 > 1200 MB**。
- 采样器 + 看门狗：**阈值已按 `#25` 的教训修正** —— `#25` 的看门狗用 `dotnet>2` 报了 **60 条全是误报**（`verify-all` 的 `dotnet test` 正常起 3–6 个 testhost）⇒ 本波改为 **`dotnet>3`**，并在波尾**明说这一档在跑 `verify-all` 时不适用**（**一个在正常操作上会响的判据是坏判据**）。
- 不许 `pkill`；不许用 `pgrep -f`/`ps|grep` 单独下结论（**自匹配**已踩三次）；**定人不能用 `PPID`**（所有车道的 bash 都由同一个 dsh 宿主派生）⇒ 用 `list_agents` 的存活态 + 时间窗。

## §7 收官清单（顺序不可颠倒 —— 纪律 46；收官时再加勾）

- [x] **W26A**：`D-G14` 列级闸 + `D-G12` 落地 ⇒ `CoverageProbe/Program.cs dea2a02cf8bab55a → 2477901979795979`；`Start` 列 **判定行 421 → 615、NOINFO 194 → 0、红 0**；**RTL 实判 33/33**；三级反极性 **171 / 615 / 484**；**既有 288 例/421 行零位移（机器证）**；**重取三支 `tab-*` 臂**（`ln -f`）
- [x] **W26B**：`D-G10` —— `verify-all.sh` 绿分支回显结论行 + 红分支零命中兜底 + 印日志路径；`frame-step.sh` 逐腿累加器 ⇒ **"改前独有行 = 0 / 0"**；改前/改后 11 个用例的通过失败**完全相同**（6/5）
- [x] **W26C**：`D-G13` —— `SELFTEST_M2` 的假红改 `NOINFO`（`rc=3`）；**该红的仍红**（三档真坏全 `FAIL`）；**判定面位移 = 0**（静树整份输出逐字节相同）；并**加严**到"数字必须逐位相等"（旧自测里抓出真缺陷）
- [x] **W26D**：`D-G9` —— `generation.arm_logs`（外科式插入）＋ `arm-log-sha-check.sh`（`--selftest` **12/12**，7 例断言"必须红"；缺声明 ⇒ `NOINFO`）
- [x] **W26E/W26F/W26G/W26H**：四份只读交付（`D-T5-R` 有牙草稿、对 W25K 的对抗性审计、`D-G2`/`D-G3` + 回归牙方案、列级闸接门禁草稿）
- [x] **主控**：接线 `[8] ARM-LOG-SHA`（**13 → 14 步**）＋ **重算并重注入 `arm_logs`**（W26A 重取臂之后 ⇒ 核对器先 `FAIL pass=2 fail=3`、注入后 `PASS 5/5`，**一次"先咬后合"的现场实证**）
- [x] `close-wave.sh`（**第一个跑**，rc=0）⇒ 九位/`inputs_fp` 复核 ⇒ 五臂实测 ⇒ `verify-all`（**14 步**）⇒ 应用门禁 ×2 ⇒ **重冻 `#26`** ⇒ 文档
- [x] 纪律 52b 第④条：**波尾第一步核冻件复原**（`hbtextline` `cmp` 与原件 IDENTICAL）
- [x] 纪律 54：**重冻后再跑一趟 `verify-all`**，使第 `[7]` 步的 PASS 落在 `#26` 冻结版上

## §11 收官（主控：`#26` 波收口）

### §11.1 顺序（纪律 46）逐条兑现
`close-wave.sh --skip-verify-all`（15:09:26→15:11:41，rc=0）→ **核冻件复原**（纪律 52b：`hbtextline` `cmp` IDENTICAL）→ 九位/`inputs_fp` 复核 → 接线 `[8]` + 重注入 `arm_logs` → 五臂（`PASS`）→ `verify-all` **14 步** → 应用门禁 ×2 → **重冻 `#26`** → 文档 → **再跑一趟 `verify-all`**（纪律 54）。逐条读数见勾选清单、`handoff.md` 的 `#26` 记录与 `ACCEPTANCE-BASELINE.md` 的 `#26` 块。

### §11.2 预测 vs 实测（本文件 §2 位移表 / §5 停条件）
| 预测 | 实测 | 判定 |
|---|---|---|
| 九位**全部不动** | **只 `pf` 变**（环成员、波尾 `integration-wave.sh` 重编引起） | ✅（`pf` 是环成员，`#17` 起每波必变 —— §2 表该补这一格） |
| `inputs_fp` **不动** | 与 `#25` 冻结值**逐位相同** | ✅ |
| 世代三项不动（`generation` 仍 `#23`） | `tree_gen=same`、`GEN_KEYS` 三项未变 | ✅ |
| 三支 `tab-*` 臂**必变** | `tab-anchor 56abc845dbd29e93 → 574d012a41a3db06`、`tab-zero → 78204619fa5b4eee`、`tab-rtl → cbaa579c547b4ac3` | ✅ |
| `tline`/`textlineproto` **不重取** | 逐字节未动 | ✅ |
| `verify-all` 步数 **13 → 14** | 14 步（第 `[8]` ✅） | ✅ |
| **停条件 1**（九位任何一位变） | `pf` 变 —— **但 `pf` 是环成员、由波尾重编引起**；`pc`/`bridge`/`hbtextline` 等**逐位未动** ⇒ **不算触发**（本表已在 `#24` 起把 `pf` 列为"预期变"） | ✅ 未触发 |
| **停条件 2**（`inputs_fp` 变） | 未变 | ✅ 未触发 |
| **停条件 3**（shim 被碰） | 中途**曾被 W26A 的反极性①临时改成 `Start => 0`**（它声明过、有备份、**已 `cmp` 复原**）⇒ **主控按纪律 52b 先核复原**、并**作废**了自己在污染窗口里取到的那次门禁读数 | ✅ 已复原 |
| **停条件 4**（`known-red.json` 的世代三项变） | 未变（`entries` 四条亦**逐字节未变**） | ✅ |
| **停条件 5**（车道越写域） | 无（`find -newermt` 逐条归因到各车道写域与主控） | ✅ |
| **停条件 6**（门禁从 `PASS` 变 `NOINFO`/`FAIL`；新红 `UNREGISTERED`） | 重取臂前后**逐项相同** `PASS`；**零新红** | ✅ 未触发 |

### §11.3 预登记**没写**、收官时新做或新发现的事（如实登记）
1. **一处"零产品位移但仪器大改"的波**：九位只动环成员、`inputs_fp` 未变，但**六件仪器 + 三支臂日志**都变了（`CoverageProbe/Program.cs`、`verify-all.sh`、`frame-step.sh`、`check-applocal-sync.sh`、`known-red.json`、新建 `arm-log-sha-check.sh`）⇒ 预登记的"位移表"只覆盖了九位与 `inputs_fp`，**没覆盖"仪器"这一类** ⇒ `#27` 起位移表要**加一列"仪器"**。
2. **`arm_logs` 的"先咬后合"**：这是**新牙第一次在真实不一致上生效**的实证（臂重取后 `FAIL pass=2 fail=3` 精确点名三支；重注入后 `PASS 5/5`）。
3. **`D-G17`（新登记）**：`verify-all` 对 `total_skipped` **零断言** ＋ X11 在**发现期** `Skip` ⇒ **`--no-x`/无 X 机器上静默关牙而全绿**（属"静默变绿"，`#27` 应治）。
4. **`D-G15`（新登记）**：缺陷登记**地点分散**（`D-G2`/`D-G3` 只在 `CURRENT-STATE`，缺陷册里没有）。
5. **`D-G16`（新登记）**：`RED_BY_FIELD` 6 键而 `entries` 只用 3 键 ⇒ "未被使用的能力"会骗过审计者（W25K 就被骗过）；**但主控裁决：那不是"红条件失效"**（判红走 `fail_n>0` 独立路径）。
6. **`D-G13` 的两条更正**：① `#25` 的"结构上必然"**只对一半**（还有与树动不动无关的一条）；② "改前基线 18/18"**作为单趟读数对、作为稳定性质不对**；③ 它报的 `printf|grep -q` ⇒ SIGPIPE 假红（自称 ≈6.6%/次）**主控复现失败（2500 次、0 假判）⇒ 标"自称、待复现"**。
7. **W26F 对 W25K 的对抗性审计**：把"① 有牙 24"校正为 **真牙 14／半牙 8／假牙 2**（另 3 处重复计数 ⇒ 独立牙齿 24→21；**5 条 `①w` 不算牙**）⇒ 新立**纪律 58**（**审计报告本身也要连 artifact + sha**）。
8. **零产品位移是"仪器大改 + 既有读数零位移"两件事都要证**：既有 288 例/421 行零位移由 W26A 给**机器证**（436 条 `CASE` 行 diff 0 行等）；应用门禁的 `drawn`/`colors`/`frames`/`cross_ae` 与 `#25` 逐位相同由**两趟门禁**证。
9. **一次环境事故（本波首次遇到）**：门禁第一次跑遇上 **`ENOSPC`（磁盘写满）** ⇒ `WPTD_GATE=`/`RUN1_RC=` **全缺** ⇒ **该趟作废并重跑**（根因 = 门禁产物 + harness spill 瞬时顶满；`wfp-runs` 当时 65 GB）。⇒ 用户指示清理：**`wfp-runs` 65 GB → 5.7 GB**（删 `*.xwd` 35.4 GB 与 app-local/OSX 运行时副本 24.3 GB，**保留全部 6834 个文本读数、10265 张 PNG、反极性留档 `.so`**）。**教训**：**"结论的载体"和"过程的原始转储"要分开保管** —— 前者只有几百 MB、必须长留；后者每波几十 GB、验证完即删。

### §11.4 下一波（`#27`）候选（**按价值**）
① **列级闸接进五臂门禁**（草稿 `$HOME/w26h/gate-wiring.diff` 已备、`+106/−5`、**无读取点被删**；**接线＋钉下限＋重取臂＋重算 `arm_logs` 必须同趟**；要动门禁 judge 版本 `/2 → /3`、`RED_BY_FIELD`、登记表 `judgment_version`/`pending`）—— 它治的是 W26H 矩阵③那行**假绿**（"放行了但一行没判 ⇒ 仍 `PASS`"）｜② **`D-G17`**（`total_skipped` 零断言 ⇒ `--no-x` 静默关牙）｜③ **`D-T5-R` 变"有牙"**（草稿 `$HOME/w26e/hidden-only-step.sh`：32 格 + 10 条断言 + 机制门；**是构建者**）｜④ **`D-R8` 的回归牙**（W26G 首推；判据零 `dotnet`/零世代/不动 `inputs_fp`；⚠️ 报数要写清"文件数 vs 行数"—— 现场 **40 个 csproj / 40 条 `<Import` 元素**〔⏪ **`#27` 更正**：本行原写"**42 条 `Import`**"，那是**错口径** —— 42 = "`.csproj` 内含 `Import` 子串的行"数（含 2 行注释散文），**不是元素数**；详见 `KNOWN-DEFECTS.md`。〕）｜⑤ **`D-G11`**（产物↔生成物等号读者；改 `patch-*.py` ⇒ **必须波前落地**）｜⑥ **`D-G15`** 两份登记建机器对账｜⑦ **`D-G12` 的末字符退化**（3 条）｜⑧ **`D-R3` 的补法要绕坑**（`ResolverGuardProbe` `return 0` ×6、`return 1` ×0 ⇒ 直接接线会是**恒绿假牙**）。
