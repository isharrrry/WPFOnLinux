# 波 `#27` 预登记（**落地前先登记；红了只许加严，不许放松**）

> 生成：主控，2026-09-17 17:2x，基于**当前冻结基线 `#26`**（权威 = `samples/WpfTextDemo/ACCEPTANCE-BASELINE.md`；⚠️ 按纪律 53 **本文件不重述整份 sha 的值** —— 唯一声明处是 `docs/CURRENT-STATE.md` 的 `BASELINE-FROZEN` 机器行）。
> 承接：`docs/WAVE26-PREREGISTRATION.md` §11.4 的候选表（① 列级闸接门禁｜② `D-G17`｜③ `D-T5-R` 变有牙｜④ `D-R8` 回归牙｜⑤ `D-G11`｜⑥ `D-G15` 对账｜⑦ `D-G12` 末字符｜⑧ `D-R3` 的坑）。

## §1 内容（七条车道；**写域互不重叠**是硬约束）

| 车道 | 件 | 需 `dotnet`？ | **独占写域**（越界即停） |
|---|---|---|---|
| **W27A** | **列级闸接进五臂门禁**（`gate-wiring.diff` + `generation.column_gate` 声明 + **钉下限**）+ 推 4 条 `entries[*].caliber.judgment_version`（`/2 → /3`）+ 改写 `pending.open[0]` | **否**（门禁是**纯读者**：`dotnet` 命中 0、不跑 harness） | **`build/MilBridge/tools/tline-gate.sh`** + **`build/MilBridge/known-red.json`**（这两件只有它能写） |
| **W27B** | **`D-G17`**：`verify-all` 对 `total_skipped` **零断言** ⇒ 让"跳过"**可见并进结论**（X11 在发现期 `Skip` ⇒ `--no-x` 静默关牙） | 否 | **`verify-all.sh`**（只有它能写） |
| **W27C** | **`D-R8` 回归牙**：新写 `build/MilBridge/tools/build-hygiene-import-check.sh`（三态 + `--selftest` + 反极性）——**只交付，不接线**（接线由主控波尾做，避开与 W27B 抢 `verify-all.sh`） | 否 | 新建的那一个 `.sh` |
| **W27D** | **`D-G15` 机器对账**：新写只读核对器，比对 `KNOWN-DEFECTS.md` / `CURRENT-STATE.md` / 各登记表里的 `D-` 编号集合与出处 ——**只交付，不接线** | 否 | 新建的那一个 `.sh` |
| **W27E** | **`D-G12` 末字符退化**（3 条"我方宽=0"都在**末字符**）⇒ 只读根因侦察 + 最小复现方案 | 否 | 仅 `$HOME/w27e/` |
| **W27F** | **`D-T5-R` 有牙草稿复核**：W26E 的 `hidden-only-step.sh` 是在**13 步**版上写的接线 diff ⇒ 按现件重锚（⚠️ **`#27` 收官时现件 = 16 步**，机器证 `grep -c '^run_step "' verify-all.sh`；W27F 开工时是 14 步，**又漂了 2 步** ⇒ 重锚交付物已再次过期，见 §8）+ 复核 14 档替身结论是否仍成立 | 否 | 仅 `$HOME/w27f/` |
| **W27G** | **`D-G11` 波前落地时序 + 补丁可用性**：验 W25J 的 `tfformatter-sha-inject.patch` 能 `patch`、产出落地配方、**定清 `inputs_fp` 时序约束**（它与 `pc` 重建的关系） | 否 | 仅 `$HOME/w27g/` |

**主控在波尾做**：① 接线 W27C/W27D 的核对器（单写者、时点明确）；② `close-wave.sh` → 五臂 → `verify-all`（**步数已定 = 16 步**：接了 W27C 的 `[9] BUILD-HYGIENE` **与** W27D 的 `[10] DEFECT-REGISTRY`）→ 应用门禁 ×2 → 重冻 `#27`。

## §2 位移预测（**表外位移 ⇒ 停**）

| 位/量 | 预期 | 依据 |
|---|---|---|
| 九位（9 位） | **全部不动**（含 `pf`——**本波没有 `integration-wave.sh` 级重编**，但**波尾 `close-wave.sh` 仍会重编环成员 ⇒ `pf` 预计变**，这一格按 `#24` 起的惯例列为"预期变"） | 本波不动产品件 |
| `inputs_fp` | **不动**（覆盖面 = `patch-*.py`/`port-lib.py`/两个波脚本/`build/shims/**`/`src/WpfGfx.Linux/**` —— 本波一个都不碰） | 同上 |
| `known-red.json` 的 `GEN_KEYS` 三项 | **不动** | 不动 `run.sh`/`Parity.cs`/shim |
| `known-red.json` 的 `judgment_version` | **`/2 → /3`（4 条 `entries`）** | W27A 的接线**扩大了判据面** ⇒ 按本工程惯例必须推版本（否则新旧口径混用） |
| `tline-gate.sh` | **必变**（接线） | W27A |
| `verify-all.sh` | **必变 ×3**（① `D-G17`；② 主控接第 `[9]` 步 `BUILD-HYGIENE`；③ 主控接第 `[10]` 步 `DEFECT-REGISTRY`） | W27B + W27C/W27D 交付 + 主控 |
| 三支 `tab-*` 臂日志 | **不动**（`#26` 刚重取过，且门禁只增读取点、不要求新日志） | —— |

⚠️ **一条硬时序**：W27A **必须在"臂日志已重取"之后**接线（`#26` 已完成重取 ✓），否则门禁会 `NOINFO`；且**不许**再动 `arm_logs`（`#26` 已重注入且核对器 `PASS`）。

## §3 W27A 的判据（**本波唯一的"判据面扩张"**）

**背景（`#26` 车道 W26H 的 12 行矩阵）**：门禁对列级读数**零读取点**（`grep TAB_LINES START/OVERFLOWED tline-gate.sh` = **0**）⇒ 其中一行是**假绿**：
- **"放行了但一行没判"（`字形释放行=0`）+ 今天的门禁 ⇒ 仍 `PASS/rc=0`** ⇒ **必须有下限把它抓住**。
- **"释放列真红" + 原门禁 ⇒ `FAIL/unregistered=1`** ⇒ **这一半牙齿今天就有**。
- **把那条红登记进 `entries`（`field=列START·红`）⇒ `PASS registered=5 unregistered=0`** ⇒ **新列红可登记**。

**验收读数（缺一不算完成）**：
1. **正极性**：接线 + 钉下限（下限值 = `#26` W26A 的**实测**值：`判定行=615`、`字形释放行=194`）+ 现臂日志 ⇒ **`TLINE_GATE=PASS`**，且**新机读行 `GATE_COLUMN=PASS … state=READINGS-OK 判定行=615 字形释放行=194`**（逐字）。
2. **反极性（≥4 档，全部实测）**：① 退化日志（`字形释放行=0`）⇒ **`FAIL column-gate-regressed`、`rc≠0`**（**这正是治假绿的那一档**）；② `judged_min+1` ⇒ `FAIL`；③ `released_min+1` ⇒ `FAIL`；④ **删声明 ⇒ `NOINFO`**（**不许当绿**）。
3. **零位移**：既有 13 个读取点**一个都不许删或放宽**（机器证：改前每一行含 `TAB_LINES` 的读取点在新件里**逐字节仍在**）；`RED_BY_FIELD` 原六键 **6/6 KEPT**；`TLINE_GATE=` 那一行的既有字段（`arms/red/green/noinfo_arm/registered/unlocated/drift/gone/unregistered/caliber/generation/tree_gen/saved_shim`）**逐项与 `#26` 相同**（除新增项）。
4. **世代三件不动**：`GEN_KEYS` 三项未变、`entries` 的 `reason`/`red_authority`/`carrier` 等**逐字节未变**（只推 `caliber.judgment_version`）。
5. **`--selftest`？**：门禁没有 `--selftest`；替代 = **造两棵只差"有无 `column_gate`"的沙箱树**跑门禁，归一化后 `cmp` 一致（照 W26D 的做法，**沙箱树必须硬链接**——纪律 56）。
6. ⚠️ **不许**改 `verify-all.sh`（W27B 的写域）、**不许**动臂日志、**不许**动 `arm_logs`。

## §4 W27B/W27C/W27D 的判据

- **W27B（`D-G17`）**：目标 = **`total_skipped` 不再无声**。要求：① 把**跳过清单**打到屏上（今天只有一个数字）；② 给跳过数**上限断言**（"依赖 X 的用例跳过数 > 0 时必须显式列出并计入结论"）；⚠️ **不许**做成"跳过即失败"（会误伤）；③ **只加不删**（机器证：改前独有行 = 0）；④ `bash -n` 通过；⑤ **反极性**：造"某用例被跳过"的情形 ⇒ 必须**可见**；绿的时候**不许误报**（11 个既有用例的逐例结果不许变）。
- **W27C（`D-R8`）**：判据 = "**每个应有的 csproj 恰有 1 行 `Import`，总行数与名单一致**"。**口径必须写清**：`#26` 现场实测是 **40 个 csproj / 40 条 `<Import` 元素**〔⏪ **`#27` 主控更正**：本行原写"**42 条 `Import` 行**"，**那是错口径** —— 42 = "`.csproj` 内**含 `Import` 子串的行**数"（其中 2 行是注释散文）；而 "38" = `#21` W21B 的**动作计数**（今陈旧）。**真值 = 40 文件 / 40 元素**，现场重算见 `KNOWN-DEFECTS.md`。⚠️ 这条错口径**曾被本预登记当判据传给 W27C**（它自己查出来并顶回来了）⇒ 又一次"**预登记里的数也必须现场算**"。〕⇒ 你的判据要**分别报"文件数"与"行数"**，并给出**名单长度**的出处。三态 + `--selftest`（含"删一行 ⇒ 必 FAIL"、"重复插一行 ⇒ 必 FAIL"、"删 golden ⇒ NOINFO"）。**零 `dotnet`、零世代、不动 `inputs_fp`**。
- **W27D（`D-G15`）**：只读核对器，报"两边 `D-` 编号集合差集 + 各自出处（文件:行）"；三态（缺一侧 ⇒ `NOINFO`）；`--selftest` 含反极性。**只交付，不接线。**

## §5 停条件（**触发即停、如实上报**）

1. 九位里**任何一位变了**（`pf` 除外——若波尾重编）⇒ 停。
2. `inputs_fp` 变了 ⇒ 停。
3. `build/shims/PresentationCore.HbTextLine.cs` 被碰 ⇒ 停。
4. `known-red.json` 的 `GEN_KEYS` 三项变了 ⇒ 停。
5. **接线后门禁变 `NOINFO`/`FAIL` 且不是"反极性那一档"** ⇒ 停、复核（可能下限钉错）。
6. 任何车道**改了不属于自己写域的文件** ⇒ 停、回退、登记。
7. **W27A 若发现"必须改臂日志或 `arm_logs` 才能接线"** ⇒ **停**（那说明前提不成立，回报主控）。

## §6 资源与内存纪律（**用户要求：控内存 + 最大并行**）

- **并行车道上限 = 7**（纪律 57：`#26` 实测第 8 条让 harness RSS 涨到 4.04 GB、**swap 一度只剩 7 MB**）。
- 本波**预计零构建者**（门禁与各核对器都是纯 bash）⇒ 内存压力应显著低于 `#26`；**若某车道需要 `dotnet`，必须先报备**（构建者独占、起步前 `MemAvailable > 1200 MB`）。
- ⚠️ **"零 `dotnet`"必须连"会自行构建的步骤脚本"一起禁止**（纪律 51 细化）：`frame-step.sh:94` 自建 harness、`verify-all.sh` 第 [1] 步直接 `dotnet build`、`pc-line-step.sh` 会跑探针 ⇒ **都不许跑**；**`tline-gate.sh` 例外（纯读者，可以跑）**。
- 采样器阈值：`avail<1000` / `swapfree<250` / **`dotnet>3`**（并在跑 `verify-all` 时**不适用**这一档）。
- 不许 `pkill`；不许用 `pgrep -f`/`ps|grep` 单独下结论（**自匹配**）；**定人不能用 `PPID`**（所有车道的 bash 都由同一 dsh 宿主派生 —— `#26` 主控据此撤回过一次误判）。
- **重取/归档纪律（59）**：本波**不该**有臂重取；若某车道认为需要，**先停、报备**（换 `OUT` + 先归档）。

## §7 收官清单（顺序不可颠倒 —— 纪律 46；收官时再加勾）

- [x] W27A：列级闸接线 + 钉下限 + 推 `judgment_version` `/2→/3` + 反极性 **4 档** + 零位移机器证（**主控现场复核**）
- [x] W27B：`total_skipped` 可见性与上限断言（只加不删；两件"改前独有行" = 0/0）
- [x] W27C：`build-hygiene-import-check.sh`（交付 `2607d0ba856f169a`，**主控已波尾接线为第 `[9]` 步**）
- [x] W27D：`D-` 编号对账核对器（交付 `defect-registry-check.sh 35838bf64f658447` + 声明件 `defect-registry-declared.tsv`，**主控已波尾接线为第 `[10]` 步**）
- [x] W27E/W27G：两份只读交付（末字符根因 `debd498f0e72148b` / `D-G11` 时序 `e2c803a6b17e8f6d`）
- [~] W27F：`D-T5-R` 重锚交付 `f6374df504d369d0`，**但它的锚点已再次过期**（它按 14 步重锚，收官时已是 16 步）⇒ 需 `#28` 再重锚
- [x] **主控**：接线 W27C 的 `[9] BUILD-HYGIENE` ＋ W27D 的 `[10] DEFECT-REGISTRY`（单写者）；下条记"冻件复原"核对结果
- [x] `close-wave.sh`（**第一个跑**，rc=0）⇒ 九位/`inputs_fp` 复核 ⇒ 五臂实测 ⇒ `verify-all`（**16 步**，两趟）⇒ 应用门禁 ×2 ⇒ **重冻 `#27`** ⇒ 文档

---

## §8 收官（`#27`，主控，2026-09-17 18:3x）—— **预登记 vs 实际，逐条对照**

> 按 `#26` §11 的体例：先对 §2 的位移预测逐格判"中/未中"，再列停条件核对、新条目与下波候选。

### 8.1 位移预测 vs 实际（§2 逐格）

| 位/量 | §2 预测 | **实际** | 判定 |
|---|---|---|---|
| 九位（9 位） | 全部不动，**但 `pf` 预计变**（波尾 `close-wave` 重编环成员） | 变化的位 = **`['pf']`**；`ebbe3bab855cdd55 → 3d44e756475b5dd4` | ✅ **逐字中**（其余八位**逐位断言未动**，含 `hbtextline`） |
| `inputs_fp` | 不动 | `0b8b655965fbc5678c0cb0bb3a7739935cf98997f16289e16cfdf5f663eba2d8`（与 `#26` 逐位相同） | ✅ 中 |
| `known-red.json` 的 `GEN_KEYS` 三项 | 不动 | 逐字节未变（`generation` 仍 `#23`） | ✅ 中 |
| `known-red.json` 的 `judgment_version` | `/2 → /3`（4 条） | `/2 → /3`（4 条 `entries`） | ✅ 中 |
| `tline-gate.sh` | 必变 | `b37a5c9f55ae71a4 → 59ce84346325eb21`（`+116/−5`，680→791 行） | ✅ 中 |
| `verify-all.sh` | 必变（`D-G17`；再接线则再变） | 必变 **×3**：`f1dc01793a160c19 → ad705fa5b0cdb331 → ff0d3a0b636ad302 → d9812c6657f7eda3 → a4db9f8149af7a07 → bb1efc78b88cf3b4` | ⚠️ **方向中、格数未中**（原表只留了 2 个变因，实际 3 个 ＋ 4 处口径更正；已补进 §7 与记录） |
| 三支 `tab-*` 臂日志 | 不动 | **未动**（`tline`/`textlineproto` 同样未动） | ✅ 中 |
| **（新增一行）仪器：新建件** | 原表**没有这一行** | 新建 `build-hygiene-import-check.sh` ＋ `defect-registry-check.sh` ＋ `defect-registry-declared.tsv` | 🔴 **表缺口**（与 `#26` 同一个形态：**位移表必须给"仪器/新建件"留行** —— `#26` 已记过这条，`#27` 又犯一次） |

### 8.2 停条件核对（§5 逐条）

| # | 停条件 | 实测 | 判定 |
|---|---|---|---|
| ① | 九位里任何一位变了（`pf` 除外） | 只有 `pf` 变 | **未触发** |
| ② | `inputs_fp` 变了 | 一字未变 | **未触发** |
| ③ | shim 被碰 | `e89fed55fd8e32bc` 未变 | **未触发** |
| ④ | `GEN_KEYS` 三项变了 | 逐字节未变 | **未触发** |
| ⑤ | 接线后门禁变 `NOINFO`/`FAIL` 且不是反极性那一档 | 现场 `TLINE_GATE=PASS` ＋ `GATE_COLUMN=PASS`；四档反极性**全部落在预期档** | **未触发** |
| ⑥ | 任何车道改了不属于自己写域的文件 | 未发现（三条波尾车道的收工 sha 逐条核过；`verify-all.sh` 的 5 次改动**全是主控本人**） | **未触发** |
| ⑦ | W27A 若发现"必须改臂日志或 `arm_logs` 才能接线" | **没发生** —— 下限值已在冻结日志里，`arm_logs` 现场 5/5 SAME | **未触发** |

⚠️ **一处需要如实记的事故（不是停条件）**：W27A 的自检夹具顺着**硬链接**打穿了仓内 `arm-logs/tab-anchor.log`（`open(p,"w")` 原地截断），**4 分钟内原地复原**、内容与 `generation.arm_logs` 声明**逐位相同**（6 份独立 `cp` 副本互证、核对器 `PASS`）。**唯一残留位移 = mtime `14:31 → 17:52:10`**。**主控裁定：接受**（理由见记录），并**加强纪律 56/60**（硬链接沙箱是**双向**的）。

### 8.3 新条目（预登记没写、收官时补的）

1. **波尾追加三条零-dotnet 只读车道**（W28A 独立咬合取证／W28B 文档口径改单／W28C `D-G19` 方案＋假绿实证）。**理由**：`#27` 的两颗新牙都是"作者交付 ＋ 作者自测"形态 ⇒ **必须有人独立咬一次**；且 `verify-all.sh` 里我自己写的注释需要一次外部口径审计。**收益**：抓出我 **3 处写宽的口径** ＋ **4 处文档失真** ＋ **一条我自己的复核器错误**。
2. **登记 `D-G19`–`D-G25`**（7 条）；**更正旧记录 4 处**（含 `D-G15` 的"证据自我 disqualifying"）。
3. **`w27-freeze.py` 的两项改进**：① 世代**参数化**（`#26` 版把 `#26` 写死在 6 处）；② 新增"**头注释 vs 现实**"的牙齿（断言 `^run_step "` 计数 == 步数，**且头注释逐字声明了同一数字**）—— 这正是 `#24`/`#27` 两次"顶层结论没有牙齿"的通用化修法。
4. **一条判例（新）**：**冻结块正文不许为了更正一个数而改写** —— 改它会让那一代记录的**整份 sha 变成不可复算**。⇒ 更正一律"**加注不覆盖 ＋ 写进新一代块**"。这条把 `#23` 那次事故从"教训"升级成"规则"。

### 8.4 `#28` 候选（按价值排序，含判据与成本）

见 `handoff.md` 波 `#27` 记录的 ⑫ 与 `samples/WpfFeatureProbe/KNOWN-DEFECTS.md`；头号 = **`D-G19`**（补丁与 6 档反极性已备，**零世代成本、不需重取臂、0 次 `dotnet`**）。
