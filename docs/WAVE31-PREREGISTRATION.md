# 波 `#31` 预登记（**落地前先登记；红了只许加严，不许放松**）

> 生成：主控，2026-09-18 00:1x，基于**当前冻结基线 `#30`**（权威 = `samples/WpfTextDemo/ACCEPTANCE-BASELINE.md`；⚠️ 按纪律 53 **本文件不重述整份 sha**）。
> 承接：`docs/WAVE30-PREREGISTRATION.md` §9 与 `#30` 块 §⑧ 的候选表。**本波是"产品侧补课"波**（纪律 67）。

## §1 内容（三条车道 ＋ 主控波尾）

| 车道 | 件 | 需 `dotnet`？ | **独占写域** |
|---|---|---|---|
| **W31A** | ⭐ **产品侧：加一支「走产品入口」的臂**（纪律 67 的落地）。照 `layout-b34/src/LayoutOracle/TextModel.cs:59-68` 的客户端形态，用 **`TextFormatter.Create()` ＋ `FormatLine`**（**不走** `FormatParagraph` 仪器路径）跑 **`layout-b34-compact.json` 里那 5 个 M 例**（真值**直接复用**、**免 Windows 重录**）。**判据 = 逐行等于真值**：`winf/w320/w200` 各 1 行 `w=156.9167 ext=18.0 lbNull=true`；`w120` 2 行 `115.6900/37.0667`（行0 `lbNull=false`）；`w80` `74.5733/78.1833`。**今天预期红**：`Extent=18.0800`（含隐形段字形）或 `winf Width≈43.59`（零宽到段末）。⇒ **它的价值 = 把"产品路径"第一次纳入回归** | **是**（1 新工程 ＋ 1 次 build） | 新建 `build/MilBridge/tests/ProductEntryArm/**` ＋ `$HOME/w31a/` |
| **W31B** | **`D-G30` 第三格**（`#30` W30C 的方案）：① `tab-zero`/`tab-rtl` 的 `OVERFLOWED` 对账行**只互校不下限**（`COL2_ARM` 补两臂 ＋ 声明册显式 `judged_min:null` ＋ **"该列须整列 NOINFO"的自证**）；② **`判定行 == 红 + 绿` 等式牙**（探针 `Program.cs:1573/1761/1809` 表明两列都是**定义等式** ⇒ 只抓"单独改判定行"）；③ 三层协同谎报**刻意不抓**（写成明示边界） | 否（门禁是纯读者） | **`build/MilBridge/tools/tline-gate.sh`** ＋ **`build/MilBridge/known-red.json`** |
| **W31C** | **三条基础设施小项**：① **反引号的机器检查**（同一陷阱已 4 次现场：`run-wpfprobe.sh:659`、W28F、主控、W30C ⇒ 新写一个零 `dotnet` 的检查器，扫全部 `.sh`/`.py` 里"双引号内的裸反引号／`$( )` 里可疑内容"，**含反极性**）；② **`arms23` 的"归档"是硬链接** ⇒ 改成 `cp -p` **真副本**（纪律 59 的归档层今天是空的）；③ **`fp_inputs()` 纳入新读者**（`column-floor-check.sh`/`hidden-only-step.sh`/`verify-all-step-check.sh`/`fp-inputs-hygiene-check.sh` —— 它们改了自己没人看着，与 `D-G22`/`D-G26` 同族） | 否 | 新建那一个检查器 `.sh` ＋ **`build/close-wave.sh`** ＋ `$HOME/w31c/` |

**主控在波尾做**：① 接线 `[13] HIDDEN-ONLY` 与 `[14] COLUMN-FLOOR`（**B 式声明**：保留 `#30` = 18 作史实）＋ **那 4 类声明行与重冻同趟**；② `close-wave.sh` ⇒ 五臂 ⇒【**零构建窗口**】⇒ `verify-all` 两趟 ⇒ 应用门禁 ×2 ⇒ **重冻 `#31`** ⇒ 文档四件同趟。

## §2 位移预测（**表外位移 ⇒ 停**）

| 位/量 | 预期 |
|---|---|
| 九位 | **全部不动**（`pf` 预计波尾变 —— 惯例）。⚠️ **W31A 只加探针工程、不动 `pc`/shim** ⇒ 世代三件不动、**不重取臂** |
| `GEN_KEYS` 三项 | **不动** |
| `inputs_fp` | **必变**（W31C 改 `close-wave.sh` 自己的覆盖面 ⇒ 设计性变更，须单列并说明） |
| `known-red.json` | **必变**（W31B） |
| `tline-gate.sh` | **必变**（W31B） |
| `verify-all.sh` | **必变**（主控接线 18 → 20 步） |
| 三支 `tab-*` 臂日志 | **不动**（W31B 只加读取点，不需新日志） |

## §3 W31B 的判据（**本波唯一的"判据面扩张"**）

1. **正极性**：`TLINE_GATE=PASS`、`GATE_COLUMN=PASS`、`GATE_COLUMN_EXTRA=PASS`、**`GATE_PROBE=PASS`** 全绿、`FAIL(col)` **0 条**；`tab-zero`/`tab-rtl` 的对账行**被读到**且打 `judged_min=none`（**键在但 null**）——⚠️ 必须与"**键缺** ⇒ `NOINFO`"**分开**（保既有极性）。
2. **反极性 ≥5 必红 ＋ ≥2 必 `NOINFO`**：① `tab-anchor` 的对账行谎报 ⇒ `FAIL`（既有）；② **`tab-zero`/`tab-rtl` 的对账行谎报 ⇒ `FAIL`**（**新增**，这是本项的核心收益）；③ 该两臂对账行删除 ⇒ `NOINFO`；④ **单独改 `判定行`**（421→615）⇒ `FAIL`（等式牙）；⑤ 协同改"判定行＋红"使等式成立 ⇒ **抓不到**（**固化为明示边界**）；⑥ 删声明 ⇒ `NOINFO`。
3. **零位移**：既有 `TAB_LINES` 读取点逐字节仍在；`RED_BY_FIELD` 不改键集；既有四行机读行**逐字不变**（除自证字段）。
4. **`judge=` 版本推 `/6 → /7`**（判据面扩张；`known-red.json` 的 4 条 `caliber.judgment_version` **同趟推进**）。

## §4 停条件（**触发即停、如实上报**）

1. 九位里任何一位变了（`pf` 除外）⇒ 停。
2. `inputs_fp` 变了**且不是 W31C 引起的** ⇒ 停。
3. `build/shims/PresentationCore.HbTextLine.cs` 被碰 ⇒ 停。
4. `GEN_KEYS` 三项变了 ⇒ 停。
5. **W31A 若为了跑通而需要动 `pc`/shim** ⇒ **停、报我**（那就变成世代级改动，必须另案）。
6. 任何车道改了不属于自己写域的文件 ⇒ 停、回退、登记。
7. **任何写操作落在硬链接沙箱路径上** ⇒ **立即停并报**（纪律 60）。
8. **`--selftest` 只在旧件沙箱里绿** ⇒ 视为**未达成**（纪律 63／`D-G29`）；**落地前必须把 pre-landing 备份放到 `$HOME/<lane>-run/backup/` 并在报告里给 before sha16**（纪律 65）。
9. ⚠️ **主控与车道自己的命令行文本不许含 `WpfTextDemo`/`WpfFeatureProbe`/`run-wpf*`**（`D-G34`）⇒ 用**薄包装脚本**调用收官链。

## §5 资源与内存纪律

- **并行车道上限 = 7**；本波**只有 W31A 一个构建者** ⇒ 它独占。
- 采样器阈值**必须同时看 `swapfree`**（`#29` 实测 `swapfree=0MB` 而 `avail` 看着健康）。零 `dotnet` 车道**不许跑会自行构建的步骤脚本**；**`tline-gate.sh` 例外（纯读者）**。
- 不许 `pkill`；不许用 `pgrep -f`/`ps|grep` **单独**下结论（自匹配）；**定人不能用 `PPID`**。

## §6 收官清单

- [ ] W31A：产品入口臂（真值复用 5 个 M 例）＋ 预期红的成对读数 ＋ 成本
- [ ] W31B：`D-G30` 第三格（互校读者 ＋ 等式牙 ＋ 明示边界）＋ `judge=/7`
- [ ] W31C：反引号检查器 ＋ `arms23` 改真副本 ＋ `fp_inputs()` 纳入新读者
- [ ] **主控**：接线 `[13]`/`[14]` ＋ 4 类声明行同趟 ＋ `close-wave` ⇒ 五臂 ⇒【零构建窗口】⇒ `verify-all` ×2 ⇒ 门禁 ×2 ⇒ **重冻 `#31`** ⇒ 文档四件

## §7 主控裁定（W31B 的请裁项，逐条）

**① C3 判 `FAIL` 而不是 `NOINFO` —— 采纳实测，承认派单书自相矛盾。** 我在 §3 的 D③ 写"该两臂对账行删除 ⇒ `NOINFO`"，却在同一段 (a) 里写"该两臂对账行删除 ⇒ `FAIL`"。**W31B 实测为 `FAIL`（`…extra-recon-gone`），并指出 W29A `:773-783` 本来就是判 `FAIL`** ⇒ **以实测与既有先例为准，判 `FAIL`**（更强，且与"结构上不可能缺失"的论证一致：汇总行与对账行同在一个 `if` 块、中间无分支）。**我的派单书错在两处口径不一致，不是它错。**

**② (b) 等式牙判 `FAIL` —— 采纳（不改成 `NOINFO`）。** 理由：`判定行 == 红 + 绿` 是探针源码里的**定义等式**（`Program.cs:1573`/`:1761`/`:1809`），**被破坏 = 仪器自相矛盾**，属"错项"而非"缺项" ⇒ `FAIL` 正确。**"缺项判 `NOINFO`、错项判 `FAIL`"这条线在本波保持一致。**

**③ C5（三层协同上向改 ⇒ 抓不到）—— 采纳为**明示边界**，并要求写进注释与记录。** 门禁内部任何自洽校验**按定义**抓不到协同谎报；责任在**下限声明**（`known-red.json`）＋ **`column-floor-check.sh` 的语料复算那一档**（只有它回答"这个数该是多少"）。⇒ 这条边界是 `#30` 的 `D-G27` 存在的理由的**反向印证**。

**④ C8（`null` 臂被协同造出判定面 ⇒ 唯一出声者是 `floor-null-but-judged`）—— 采纳，且它印证了 `#30` 的裁定 (b)**："`judged_min:null` **必须**同趟加『该列须整列 NOINFO』的自证"——**实测证明：九层自洽全过、只有那条自证出声。**

**⑤ `--help` 的固定行窗位移 —— 登记，本波不改（接受它的处置）。** `tline-gate.sh:122` 的 `sed -n '2,90p'` 因表头 +12 行而顶掉旧尾部 12 行。它按 W29B 对"死子句"的**"只登记不改"**先例处理 —— **正确**（一处显示面位移不值得为它动一次判据件）。**一句话修法已记**：`'2,/^set -uo/p'`。⇒ 留给 `#32` 与那条死子句**一起**改。

**⑥ 它纠正的口径**：`RED_BY_FIELD` 现场是 **7 键**（我派单书写 6 是 W27A 之前的旧口径 —— 我在 `#29` 已被 W28D 顶回过一次，这是**第二次**同一处，说明我的派单书模板里还留着旧数）。**⇒ 纪律：派单书里引用任何键数/行数/步数前必须现场算（纪律 49 对"我自己的模板"同样适用）。**

**⑦ 一条文档陈旧（它未改、留我裁）**：`known-red.json` 里 `tab-anchor` 那段的 `note` 写"`tab-anchor.log` 第 1342 行的 `OVERFLOWED` 汇总行"，而现盘在 **`:1343`**。⇒ **按"加注不覆盖"处理**（纪律 61 同族），随 `#31` 收官文档同趟改。

## §8 主控裁定（`#31` 车道请裁项 ＋ 主控自纠，逐条）

**① W31C ① 的 12 条反引号真陷阱 —— 修，不登记 `known-red`。** 理由是"**它们是语言陷阱，不是缺陷**"：`bash -n` 对三处**全部静默通过**（实测），而"抽出来真跑"能逐条证出**丢字 ＋ stderr `未找到命令`**（W31C 的判据表）。⇒ 修掉它（我落，3 文件 4 行）比登记它更省、更硬；登记成"在册红"会把**逐条点名**变成**逐条豁免**，而这正是本波要治的那一族。落地 sha16：`run-df1-criteria.sh 6d19d148b2ab9607 → 5d45016767624269`｜`seg-instrument.sh fbef833932d90586 → 5c9b63ba4cdef6b6`｜`run-wpfprobe.sh 3848a43dcc589a95 → ace2b3116a6da4ce`；修后新牙 `SHELL_QUOTE_TRAP=PASS reason=ok traps=0`。

**② W31C ② 的 `retake-arms-w23.sh` 守卫 —— 落（W31E 执行）。** 理由：W31C 的清别名只清了**当前实例**，**机制还在**（脚本第 5 步的 `ln -f` 会把 `OUT` 与 `arm-logs` 重新绑成同一 inode ⇒ 下次按默认 `OUT` 重取，`arms23` 又变回硬链接、归档层再次变空）。这是"**把一次静默的归档破坏变成一次响亮的拒跑**"，方向完全正确；三档成对读数（真 `arm-logs` ⇒ `exit 4` ＋点名 inode／真副本目录 ⇒ `rc=0`／空目录 ⇒ `rc=0`）＋**未打补丁原件的阴性对照**在 W31E 报告里。

**③ W31C ③ 的"第 4 个读者纳入 `fp_inputs()`" —— 采纳并已落。** 覆盖面 **114 → 115**（`build/MilBridge/tools/shell-quote-trap-check.sh` 进第 145 行那串 `printf`）；理由与另四件同族：**它判什么算陷阱 ⇒ 改它必须看得见**。附 `FP_INPUTS_HYGIENE=PASS coverage_n=115 artifact_n=0`。

**④ W31D ① 的"上屏行窗 `head -8` 顶到天花板" —— 采纳：`8 → 12`。** 现场实测：新第 `[14]` 步 `COLUMN_FLOOR` 匹配 **7 行**、**总判行正好第 7 个**；而 W31B 已把两支 `judged_min:null` 臂写进登记表，一旦冻结构也声明它们 ⇒ 逐档行 3→5 ⇒ 上屏 **9 行** ⇒ **总判被截掉**。⇒ 放宽**只动显示窗**（`grep … | head -N`），**不碰任何 `KEY=VALUE` 语义、不参与 `rc`**。⚠️ 这不是"为了好看"：**总判看不见**属于仪器形态缺陷（读日志的人会以为这一步只有内档）。

**⑤ W31D ② 的"行名前缀不再唯一" —— 本波不改，交 `#32` 与行名口径同趟改。** 三支 `OVERFLOWED` 臂的行名前缀**逐字相同**（`COLUMN_FLOOR_OVERFLOWED_JUDGED_MIN=`），与文件头抄 `arm-log-sha-check.sh` 时那句"前缀唯一 ⇒ 抽取无歧义"冲突。**今天不构成歧义**（只有 `anchor` 产整数行），改它会**移动真树上屏行**（动读数面）⇒ 与"行名 = `COLUMN_FLOOR_<臂>_<列>_<键>`"一起作为 `#32` 一项。

**⑥ W31D ③ 的"要不要给两支 `judged_min:null` 臂插 `# COLUMN-FLOOR … judged_min=none`" —— 本波故意不插。** 三条理由：(a) `D-G19` 的裁定就是"**该列无下限 ⇒ 刻意不声明**"，插一行 `judged_min=none` 在语义上比"不声明"**更弱**（后者是"没这个下限"，前者是"有一下限，值为 none"）；(b) 插了立刻撞 ④（上屏 9 行）与 ⑤（前缀重名）两处；(c) **今天的边界不是假绿**：把某支 `null` 臂在登记表里改成小整数 ⇒ 冻结构无对应行 ⇒ 判 `NOINFO`（**挡住全绿**），不是 `PASS`。⚠️ W31D 已把三态语义**实现并测好**（`decl=none & frozen=none ⇒ PASS` 等 8 例），**要激活只差两行**——这一点必须写清，别让后人以为"读不了"。

**⑦ W31A 的产品入口臂 —— 本波不接线（留给 `#32`）。** 它**按设计是红的**（18 条逐行红，`rc=1`）；把它接成 `verify-all` 一步会让整趟**不可能绿**，除非先给它一套"**在册红**"的声明机制（像 `[4]` 五臂那样：`known-red.json` 里按"**产品入口臂**"这一类登记**期望红的坐标与条数**）。那是 `#32` 的头号候选，且**必须与产品侧修法同趟**（方案 A：`CollectLenient` 补 `out modifierScopeEnd` ＋ 调用点传参 ⇒ 动 `pc`；方案 B：shim 侧墨迹并集 ⇒ 动 `instr_shim`）—— 两者都会动九位，不能与本波同趟。

**⑧ 主控自纠（本波，逐条留档）**：
- **(a) 我在 `#30` 块与 `CURRENT-STATE` 里写的"那 5 条 `M_modifier` Extent 余差与 modifier 跨度同根"被 W31A 的端到端实测推翻**：5 个 M 例 `ext` 我们 == 真值 == `18.000000`（5/5 `OK`），`18.08` 是**阴性对照的真值**。⇒ 已按纪律 61「加注不覆盖」在**纪律 67 下追加 ⏪ 注**（原文一字未动），并明确"引用本条时不许再引用那个 `0.08`"。
- **(b) 第 `[9]` 步 echo 里的"40 份 csproj"是陈旧口径**（roster `wired` 已 41、核对器 `EXPECT_N=41`）⇒ 本波更正为 **41**（同"4 个测试套件"那次一样，属**标签陈旧**、不参与判定）。
- **(c) `docs/CURRENT-STATE.md` 顶部"唯一权威…… 最新冻结（现在 `#24`）"** 早已陈旧（本波冻 `#31`）⇒ 更正。
- **(d) 我派单书里写死"真树读数应为 `NOINFO rc=2`"，而我自己在 09:58 已把声明行插上** ⇒ W31D 报告 §1 当场点出"派单书过期"。⇒ **纪律：派单书里凡是"现场读数应为 X"的句子必须带取样时刻**（否则它是一条**会自己过期**的判据）。
- **(e) 我先前把 `ps -eo pid,args | grep -c dotnet` 当进程数判据** ⇒ 现场复现 `pgrep -c -x dotnet = 0` 而它报 **4**（多出的全是**承载这次测量的那个 `bash -c` 自己**）⇒ 立**纪律 69**。
- **(f) 本条 §8 立了两条新纪律**：69（数进程不许数到自己命令行）、70（散文式否定宣言与现场会悄悄分叉，且**可以就发生在同一段文字里** —— `#30` 块那句"本波刻意不写"的**下面两行**就是活声明）。

**⑨ 【预登记·写在任何收官读数之前】`w27-freeze.py` 的"只有 `pf` 变"那两条断言，`#31` 起改为按代开关（`pf_required`）。**
动机：`#26`–`#30` 每代都写死 `assert changed == ['pf']` 与 `assert now['pf'] != G['prev_pf']` —— 那是把**上一代的惯例**当成**这一代的判据**（`D-G29` 同族：当前世代不该由过去定义）。本波**产物侧零改源**（只改 `.sh`／`.json`／`.md`；`BRIDGE_SRC_FP` 的覆盖面只收 `*.cs|*.csproj|*.props|*.targets|*.resx`）⇒ 真正的判据只有一条：**表外位移才停**（`set(changed) ⊆ {'pf'}`）。
⇒ `#31` 的处置（**已写进脚本，早于 `close-wave` 的任何读数**）：`GENS['#31']['pf_required']=False`，并且
- `pf` 逐位**变了** ⇒ 打印"与 `#26`–`#30` 惯例一致"，如实写进块里；
- `pf` **没变** ⇒ 打印"**不是**表外位移、**不是**事故"，**如实写"没变"**，且**明令不许**为了凑 `changed==['pf']` 去动任何产品件；
- 出现 `{'pf'}` 之外的位移 ⇒ **仍然** `AssertionError`（`#31` 预登记 §2 的"表外位移 ⇒ 停"**未放宽**）。
⚠️ **这条改动本身也要能被反驳**：它与 `docs/WAVE31-PREREGISTRATION.md` §2 预测表"九位全部不动（`pf` 预计波尾变 —— 惯例）"**逐字相容**，且**没有**降低任何门槛（放宽的是"预期"，不是"判据"）。

## §9 收官（`#31` 完成，2026-09-18 11:5x）

**`#31` 已重冻：整份 `sha16=dd3b2da7422181c4`（376,617 B）**；机器行核对 `rc=0`、`BASELINEGEN=PASS decl_gen=#31 file_newest_gen=#31`、`BASELINEDUP=PASS`。
**两趟 `verify-all` 各 `rc=0` / 21 步 / 871 通过 / 2 跳过 / `SKIP_GUARD=PASS`，结论区逐字相同**（唯二差异 = `[0]` 步"启动 Xvfb"↔"复用已有"、门禁 `outdir` 的时间戳）；应用门禁**两趟** `RUN1_RC=0`/`RUN2_RC=0`、**各 6/6 `result=PASS`**（`WPTD_GATE=PASS acceptance=2/2 line_advance=PASS`）。五臂 `TLINE_GATE=PASS arms=5 … judge=t1b3-tline-gate/7` ＋ `GATE_PROBE=PASS` ＋ `GATE_COLUMN=PASS` ＋ **`GATE_COLUMN_EXTRA=PASS`（三支臂，两支 `judged_min=none`）**；`ARMLOG_SHA=PASS 5/5`。
**九位只有环成员 `pf` 变**（`51e987d58da11654 → f418131a53fa2951`；`changed == ['pf']` 逐字命中 §2 预测，也命中本文件 §8 ⑨ 预登记的 `pf_required` 规则），其余八位**逐位未动**、`hbtextline` 一字节未动 ⇒ **不重取五臂**；`BRIDGE_SRC_FP` 两侧一致（`fdcb41bdc373eee3`，**未重发**）。`inputs_fp` **设计性变更**：`98f600e5… → 46d2a2b0f510f770…`（成员 **110 → 115**）。
**本波三条车道的收官更正（留档）**：① W31E 指出我插话里说"那 7 处加注还没落"**是错的** —— 我复核到的 `known-red.json b39a9b0c2f953ba0` **就是加注后**的件（它 10:04:07 写完后对该件零写入）；② W31F 指出 `close-wave.sh:136` 那句"两次采样都在波尾"**与代码不符**（`IN_FP_0` 在 `[1/6]` 之前），我已按纪律 61 就地更正（**结论不变、推理换掉**）；③ W31E 实测到 `defect-registry-check.sh` 的**并发假红**，我用静树 60/60 与两条机器证把它**收窄**成 `D-G39`（**机制未取到**）。
**未接线项（刻意，逐条给理由）**：产品入口臂（按设计是红的；接线要先给"在册红"机制）｜两支 `judged_min:null` 臂的 `# COLUMN-FLOOR … judged_min=none` 行（`D-G19` 的裁定就是"无下限 ⇒ 刻意不声明"；且会撞上屏行窗与前缀重名）｜行名口径 `COLUMN_FLOOR_<臂>_<列>_<键>`｜`D-G30` 残り（`--help` 窗 ＋ 死 `-maxdepth 2` 子句）｜`D-G11`｜`verify-all` 的实际执行轨迹。
**一条本波自己造出来又自己修掉的坑（留档）**：我把 `CURRENT-STATE.md` 里两处"最新冻结"改成 `#30` 时**没有同趟改成 `#31`**（当时 `#31` 块还不存在）⇒ 记住：**"中间态文档"也要在同趟收口**。
