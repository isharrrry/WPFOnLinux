# 波 `#29` 预登记（**落地前先登记；红了只许加严，不许放松**）

> 生成：主控，2026-09-17 19:5x，基于**当前冻结基线 `#28`**（权威 = `samples/WpfTextDemo/ACCEPTANCE-BASELINE.md`；⚠️ 按纪律 53 **本文件不重述整份 sha 的值** —— 唯一声明处是 `docs/CURRENT-STATE.md` 的 `BASELINE-FROZEN` 机器行）。
> 承接：`docs/WAVE28-PREREGISTRATION.md` §8.4 的候选表（9 条，本波挑前 6 条）。

## §1 内容（六条车道；**写域互不重叠**是硬约束）

| 车道 | 件 | 需 `dotnet`？ | **独占写域**（越界即停） |
|---|---|---|---|
| **W29A** | **`D-G30` 落地**：把探针为 **`OVERFLOWED`** 打的「逐例对账行」也纳入判据（`REC_RE` 泛化成**带列名捕获组**，对 `col_info`（START）与 `col2_info`（OVERFLOWED）**各自的声明臂**分别核对）。**必须自己重跑两列的全部反极性**（含 W28J 的 X1/X2/X3 三档） | 否（门禁是纯读者） | **`build/MilBridge/tools/tline-gate.sh`**（**只此一件**；⚠️ **不动 `known-red.json`** —— 本项不引入新声明） |
| **W29B** | **`D-G31` 的同族另两处 + 一条自检**：① `find build/shims -type f -name '*.cs'` 与 `find … -maxdepth 2 -name 'patch-*.py'` 也加 `obj|bin|.artifacts` 排除；② 新写一个零 `dotnet` 的自检，断言「**`fp_inputs()` 的覆盖面里不许出现 `obj/`/`bin/`/`.artifacts/`**」并能在注入一份产物 `.cs` 时**必红** | 否 | **`build/close-wave.sh`** ＋ **新建的那一个核对器 `.sh`** |
| **W29C** | **`D-T5-R` 接线复核**：W28H 已交付 `build/MilBridge/tools/hidden-only-step.sh d006c5bed70e0295`（**未接线**）＋**12 格对照读数已补齐**。本车道：① **独立复核那 12 格读数**（不许照抄 W28H 的自报）；② 按现件 **17 步**重锚（现件 `verify-all.sh` = `d3d08691aeff17bd`）；③ 在**副本**上做"接线后"的端到端模拟并给**成本实测**；④ 给锚文本。**只交付，接线由主控** | **是**（`dotnet`；**本波唯一构建者**） | 仅 `$HOME/w29c/`（**不许改仓内任何文件**） |
| **W29D** | **`D-G27` 判据设计**：给「**下限值本身**」一个**外挂**读者（⚠️ 那个"另一处"**不许**是同一份 `known-red.json` —— 自指等于没牙）。要求给 2–3 种形状 ＋ 各自的反极性设计 ＋ 假红风险分析 ＋ 最小 diff 草稿 | 否 | 仅 `$HOME/w29d/` |
| **W29E** | **`D-G26` 判据设计**：让探针在臂日志里**自报自己的 sha**，并给门禁一个读者（"日志自报的探针 sha == 现场 `CoverageProbe/Program.cs` 的 sha"）。⚠️ 落地要**重取三支 tab 臂**（走纪律 59）⇒ 本件只出**方案 ＋ 成本 ＋ 时序约束**，并回答"**有没有不重取臂的替代读法**" | 否 | 仅 `$HOME/w29e/` |
| **W29F** | **`D-G21` 落地**：把 `$HOME/w28f/` 的三节声明册（`wired`/`notneeded`/`suspended` ＋ 逐条**见证谓词**）落成 `build-hygiene-roster.tsv` ＋ 新的 `build-hygiene-import-check.sh`。**必须自己重跑 16 例自测 ＋ 真树正极性 ＋ 新增反极性**（含"新增未表态 csproj ⇒ 红"、"见证失效 ⇒ 红"） | 否 | **`build/MilBridge/tools/build-hygiene-import-check.sh`** ＋ **新建 `build-hygiene-roster.tsv`** |

**主控在波尾做**：① 判定并接线 W29A/C/F 的交付；② `close-wave.sh` → 五臂 → `verify-all`（**步数视接线而定**）→ 应用门禁 ×2 → 重冻 `#29`。

## §2 位移预测（**表外位移 ⇒ 停**）

| 位/量 | 预期 | 依据 |
|---|---|---|
| 九位（9 位） | **全部不动**（`pf` 预计波尾 `close-wave` 时因环成员重编而变 —— 照惯例列"预期变"） | 本波不动产品件 |
| `inputs_fp` | **不动** | ⚠️ **但 W29B 会改 `fp_inputs()` 本身**（`close-wave.sh` 在覆盖集里）⇒ 改后**必须当趟重算**并把新值钉进 `#29` 块 |
| `KNOWN-DEFECTS.md` | **必变** | 主控 |
| `GEN_KEYS` 三项 | **不动** | 不动 `run.sh`/`Parity.cs`/shim |
| `known-red.json` | **不动**（`D-G30` 不引入新声明；`D-G27`/`D-G26` 只出方案） | W29A/D/E 的裁定 |
| `tline-gate.sh` | **必变**（`D-G30`） | W29A |
| `verify-all.sh` | **视接线而定**（`D-T5-R` 若落地则 `17 → 18` 步，且声明块与口径句必须同趟改） | 主控 |
| `build-hygiene-import-check.sh` ＋ 新 roster | **必变/新建** | W29F |
| 三支 `tab-*` 臂日志 | **不动**（本波不重取；`D-G26` 只出方案） | —— |

## §3 W29A 的判据（**本波唯一的"判据面扩张"**）

**背景（`D-G30`）**：`#28` 合入的 `REC_RE` **只锚 `START`**，而探针**同时**为 `OVERFLOWED` 打对账行（`CoverageProbe/Program.cs:1832`；真日志 `tab-anchor.log:1343`）⇒ 该列的对账行**没有读者**。W28J 三档实测：**X1** 该列对账行**谎报**（绿 421→420）⇒ `rc=0`/`PASS`；**X3** 整条**删除** ⇒ `rc=0`/`PASS`；**X2** 该列**汇总行**的 `绿` 谎报 ⇒ 仍 `PASS`（`D-G19` **只读 `判定行`**）。

**验收读数（缺一不算完成）**：
1. **正极性**：现臂日志 ⇒ `TLINE_GATE=PASS`、`GATE_COLUMN=PASS`、`GATE_COLUMN_EXTRA=PASS`，且**读数行里出现 `OVERFLOWED` 的对账结论键**（逐字）；`FAIL(col)` **0 条**。
2. **反极性 ≥6 档、全部实测**（每档 `cp -p` **真副本**，**禁止硬链接** —— 纪律 60）：① **X1** `OVERFLOWED` 对账行谎报 ⇒ **`FAIL`**；② **X3** 该行整条删除 ⇒ `FAIL`（或按你的论证 `NOINFO` —— **必须论证**，并解释为何与 `START` 那一档不同或相同）；③ **X2** 该列汇总行 `绿` 谎报 ⇒ **必须先回答"该不该抓"**（`D-G19` 只钉 `judged_min` 是**刻意的**：`OVERFLOWED` 的行宽判定依赖字形 ⇒ 194 行本就 NOINFO）—— 若判"该抓"，给判据；若判"不该抓"，**给论证**并登记为边界；④ `START` 的既有八档**全部不变**（回到 `#28` 的结论）；⑤ 两列**同时**失真 ⇒ 两条点名俱在；⑥ **只 `START` 失真** ⇒ `OVERFLOWED` 侧不受牵连（反之亦然）。
3. **零位移机器证**：改前每一行含 `TAB_LINES` 的读取点在新件里**逐字节仍在**；`RED_BY_FIELD` **7/7 KEPT**；`TLINE_GATE=`/`GATE_COLUMN=` 两行的既有字段**逐项相同**（除自证字段）；`gate-readings.json` **删除 0**。
4. **世代三件不动**（`GEN_KEYS` 三项、`tree_gen=same`）；**`judge=` 版本**：本项是判据面扩张 ⇒ **推 `/4 → /5`**（`known-red.json` 的 4 条 `caliber.judgment_version` **主控同趟改**，你不改 `known-red.json`，只**报出**你要的版本号）。
5. ⚠️ **不许**改 `verify-all.sh`/`known-red.json`/`KNOWN-DEFECTS.md`/`docs/*`；**不许**动臂日志或 `generation.arm_logs`。

## §4 W29B/C/D/E/F 的判据

- **W29B**：① 三条 `find` 统一加排除后，`inputs_fp` **当趟重算**并给旧/新值；② 新自检**三态** ＋ `--selftest` 含"注入一份 `obj/*.cs` ⇒ 必红"；③ **证明修前后"现场覆盖面里 0 件产物"**（今天已成立，属"恰好没坏"）。
- **W29C**：① **独立复核 12 格**（12/12 `rc=0` ∧ `verdict=GREEN` ∧ `A1/A2/A3` PASS？）；② 按 **17 步**重锚并给锚文本；③ **成本实测**（`WALL`/峰值 `RSS`）与"接线后 `verify-all` 涨多少"；④ **独立复核 W28H 的两条推翻**（"旧产物上恰 4 格红"实测 `不符=28`？"缺 shim ⇒ 32 格 `abort(134)`"？）；⑤ 它自称的"**假红与真红只看 `rc` 不可分**"是否成立。
- **W29D**：给 2–3 种形状（必须含"外挂且非自指"这一硬约束）＋ 反极性 ≥4 档 ＋ 假红风险 ＋ 最小 diff；**必须回答**："门禁已经把 `judged_min=`/`released_min=` 打进机读行了（`#28` 起），那么**这算不算已经有一个读者**？"（若不酸 ⇒ 说清"自报"与"被核对"的差别）。
- **W29E**：给方案 ＋ 成本（重取三支臂的代价）＋ 时序约束（纪律 59：先换 `OUT` 并归档）＋ **替代读法**（能否用臂日志的 mtime/内容指纹间接定版？—— `#28` 已实测"mtime 先后不能定版"，所以要想别的）。
- **W29F**：① 16 例自测 ＋ 真树正极性 ＋ 新增反极性（新增未表态 csproj ⇒ `UNDECLARED`；见证失效 ⇒ `WITNESS-EXPIRED`；候选集截断 ⇒ `NOINFO`）；② **`CAND_MIN=82` 不许做成可覆盖的环境变量**（可覆盖 ⇒ 假绿）；③ 把 `src/WpfGfx.Linux/WpfGfx.Linux.csproj` 按 `#28` 的裁定写成 **`suspended` ＋ 见证 `no-in-repo-obj`**（⚠️ **它的见证今天不成立** —— 仓内 `obj` 里**有** `*.cs` ⇒ 按你的规则它会红；**这正是要的**：它应当**红着**直到主控接线它）；④ 报出"接线后 `[9]` 会红还是绿"，**别自己改判据让它绿**。

## §5 停条件（**触发即停、如实上报**）

1. 九位里**任何一位变了**（`pf` 除外）⇒ 停。
2. `inputs_fp` 变了**且不是 W29B 改 `fp_inputs()` 引起的** ⇒ 停。
3. `build/shims/PresentationCore.HbTextLine.cs` 被碰 ⇒ 停。
4. `known-red.json` 的 `GEN_KEYS` 三项变了 ⇒ 停。
5. **接线后门禁变 `NOINFO`/`FAIL` 且不是"反极性那一档"** ⇒ 停。
6. 任何车道**改了不属于自己写域的文件** ⇒ 停、回退、登记。
7. 若发现"必须改臂日志或 `generation.arm_logs` 才能接线" ⇒ **停**。
8. **任何写操作落在硬链接沙箱路径上** ⇒ **立即停并报**（纪律 60）。
9. **`--selftest` 只在"旧件沙箱"里绿** ⇒ 视为**未达成**（纪律 63 ／ `D-G29`）：必须在**落地位置、对现件**跑绿。

## §6 资源与内存纪律

- **并行车道上限 = 7**（纪律 57）。本波**只有一个构建者**（W29C）⇒ 它独占；其余纯 bash。
- ⚠️ **零 `dotnet` 必须连"会自行构建的步骤脚本"一起禁止**（纪律 51 细化）：`frame-step.sh`、`verify-all.sh` 第 `[1]` 步、`pc-line-step.sh` 都不许跑；**`tline-gate.sh` 例外（纯读者）**。
- 采样器 = `$HOME/w28-mem.sh`（v2，逐字段具名取 —— 纪律 62）；阈值 `avail<800` / `swapfree<100`。
- **不许 `pkill`**；不许用 `pgrep -f`/`ps|grep` 单独下结论（自匹配）；**定人不能用 `PPID`**。
- **重取/归档纪律 59**：本波**不该**有臂重取（`D-G26` 只出方案）；若某车道认为需要 ⇒ **先停、报备**。

## §7 收官清单（顺序不可颠倒 —— 纪律 46；收官时再加勾）

- [ ] W29A：`D-G30` 落地（`tline-gate.sh` 单件）＋ ≥6 档反极性 ＋ 零位移机器证
- [ ] W29B：`D-G31` 同族两处 ＋ 覆盖面自检（`close-wave.sh` ＋ 新核对器）
- [ ] W29C：`D-T5-R` 12 格独立复核 ＋ 17 步重锚 ＋ 成本实测（**只交付**）
- [ ] W29D：`D-G27` 判据草稿（只交付）｜- [ ] W29E：`D-G26` 方案 ＋ 成本（只交付）｜- [ ] W29F：`D-G21` 落地（声明册 ＋ 新核对器）
- [ ] **主控**：判定并接线 W29A/C/F 的交付（单写者）；推 `judge=` `/4 → /5`
- [ ] `close-wave.sh`（**必须第一个跑**）⇒ 九位/`inputs_fp` 复核 ⇒ **零构建窗口** ⇒ 五臂实测 ⇒ `verify-all`（两趟）⇒ 应用门禁 ×2 ⇒ **重冻 `#29`** ⇒ 文档

## §8 **本波新立的两条时序纪律（源自 `#28` 的两次返工，见其 §8.3）**

1. **「门禁 → 重冻」之间必须是一段零构建窗口**：`pf` 是环成员，**任何 `close-wave`/`integration-wave`/`verify-all` 都会动它** ⇒ 在门禁两趟**之后**再跑其中任何一个，门禁行的 `pf` 立刻成为旧值（`#28` 实测撞到，只能重跑门禁两趟）。
2. **不许在任何脚本里"复制"另一个脚本的函数体**：`#28` 的重冻脚本复制了一份 `fp_inputs()` ⇒ 与真源分叉 ⇒ 写进基线的 `fingerprint` 是**老定义**的值。⇒ **一律 `source` 真函数**（唯一真源）；若不得不返工，**必须用一个已知 sha 当靶做"可证还原"**（`#28` 用 `c8d086acb625a714` 证明还原精确）。

---

## §9 收官裁定（主控，2026-09-17 22:0x）—— `D-T5-R` **本波不接线**

**W29C 已完成它的全部交付**（报告 `$HOME/w29c-report.md 083fdf2e7837534f`）：12 格读数**独立复核成立**（12/12 `rc=0` ∧ `GREEN` ∧ `A1/A2/A3` PASS，两趟归一化 `cmp` 相同，墙钟 42.0/39.4 s）；17→18 步的重锚**已换成现场锚**（现件 `150361be49a7d96b`、**18 步** ⇒ 新步是 `[13]`、`18 → 19`）；端到端模拟证明**接线后 `[11]` 会自动读 19 并 PASS**（`vfile_sha16=67cb901df2083c73`）。

**裁定：本波不接线。** 理由四条：
1. **它把 `verify-all` 变成"构建者"**：单趟 **35 次 `dotnet`**（1 build ＋ 2 自检 ＋ 32 格）⇒ **顶层门禁从此拖着一个构建者**，与"构建者独占"（纪律 51 细化）和并行纪律冲突 —— 这与 `#27` 当初延后它的理由**同一条**。
2. **成本**：实测 **WALL 101–103 s、峰值 RSS ≈790 MB** ⇒ **每趟 +1.70 min**（两趟 +3.4 min）；相对涨幅 `+15.8%~16.4%`，但分母（`#26` 的 621/643/630 s）**引自 W28H、未独立取到** ⇒ 真实百分比 **≤16.4%**。
3. **它自曝两处缺陷要先修**：① 步骤脚本 `:499` 的 `*LoCreateContext*` 归因分支是**死代码** —— `:480` 的 `grep -aoE '[A-Za-z]+Exception'` **只取异常类型名**，而 `LoCreateContext` 只在**消息**里 ⇒ **每一格 `rc=134` 都被印成"不是 D-T5-R 的签名 ⇒ 先怀疑环境"**，**包括真是它的时候**（判据零影响，但归因被毁）；② `:316` 的屏上描述仍写"恰 4 格红"而真判据（`:343`）是"**≥4 且目标 4 格在内**" ⇒ 字面缺陷，**只改字面**。
4. `#29` 的已落地三项**自洽且已验**（`[9]`/`[11]`/`[12]`/DRC/基线全绿）⇒ 没有理由为它把收官链拉长。

**W29C 的独立更正（采纳）**：① **W28H 那条"推翻"要把两层分开读** —— **步判词层** `不符=28` ✅ 与 **原始探针层恰 4 格红** ✅ **两层都对**；`#26` 那句按**原始层**读没错（W28H 自己 §3.1 手测给的就是原始层 4）；② **"缺 shim ⇒ 只看 `rc` 不可分"作为结论错了**：它实测出**两个可分信号** —— **marker 文件是否被创建**（缺 shim ⇒ 崩溃早于 `Program.cs:195` 的 `Mark`）与**异常签名**（`DllNotFoundException` vs `EntryPointNotFoundException`），且**两者本来就在该脚本自己的证据里**。⇒ 该脚本的 `异常=` 字段应升级成**判据**（`#30` 候选）。
**边界（W29C 独立证实并引用准确）**：`A1/A2/A3` 对"占位是否真零宽"**零判别力** —— `D5CbrProbe/Program.cs:567` **自己就这么写**；全文件对产出行的属性读取**只有** `:259 line.Length` 与 `:272 line.NewlineLength`（无 `Width`/`GetTextBounds`/任何几何）⇒ **本步的绿 ≠ 隐形语义已正确**。

## §10 收官链现状（**未完**，如实记）
`#29` 已落地三项（`D-G30`／`judge=/5`／`D-G31`＋第 `[12]` 步），树自洽、四件仪器全绿、九位与 `#28` 逐位相同、`BRIDGE_SRC_FP` 未变。**仍缺**：`close-wave.sh --skip-verify-all` → `verify-all` ×2 → 应用门禁 ×2（**必须带 `WPTD_BASELINE_OUT`**）→【**零构建窗口**】→ 重冻 `#29`（`w27-freeze.py` 的 `GENS` 加 `'#29': nstep=18`）→ 文档四件同趟 → 第 2 趟 `verify-all`。
⚠️ **两条本波新立、已被各撞到一次的时序纪律**见 §8；另新增**纪律 65**（**任何车道落地前，主控必须先留 pre-landing 备份** —— `#29` 的退回动作一度因"备份晚了"而没有旧字节，靠车道自己的备份才救回来）。

## §11 收官（`#29` 完成，2026-09-17 22:3x）

**`#29` 已重冻：整份 `sha16=19a1f9e508593ab1`（328,298 B）**，机器行核对 `rc=0`、`BASELINEGEN=PASS decl_gen=#29`、`BASELINEDUP=PASS`。

| 读数 | 实际 |
|---|---|
| 九位 | **只有环成员 `pf` 变**（`9178561e0fb1451c → a91e564da4d4f2cb`）；`bridge`/`pc`/`hbtextline` 等八位**逐位未动** |
| `BRIDGE_SRC_FP` | `b6acdba4f01599d8 → fdcb41bdc373eee3`（`D-G32` 改 csproj）⇒ `[3/6]` **桥真的重发**，**但重发后 `.so` 逐位不变** ⇒ **零产品位移** |
| `inputs_fp` | `6f8e8ef7… → 4ce9f65af5f102a6…`（**设计性变更**：三条 `find` 统一加排除；`close-wave.sh` 在覆盖面里） |
| 门禁机读行 | **归一化 `pf`/`rundir` 后与 `#28` 逐字相同**；两趟 `RUN1_RC=0`/`RUN2_RC=0`、**6/6 `result=PASS`** |
| `verify-all` | **rc=0 / 18 步 / 871 通过 2 跳过**（`[4]`–`[12]` 全 ✅） |

**⚠️ 本波一次真实资源事故（如实记）**：采样器 **21:50:08 抓到 `swapfree=0MB`**，而**同一笔 `avail=4050MB` 看起来完全健康** ⇒ 10 条告警从 0 爬到 74 MB 才缓过来（≈2.5 分钟）。⇒ **预警必须看 `swapfree`，`avail` 会漏**；判据两个都要看。

**主控自纠（本波 5 处）**：`$?` 在管道后取到的是管道 rc｜**备份晚了**（⇒ 纪律 65）｜roster 编辑**漏第 5 列**（判据当场 `roster-malformed`）｜我的 `bash -c` 命令行文本**触发 `close-wave` 的 `pgrep` 自匹配** ⇒ 假停（⇒ `D-G34`）｜`grep -E '^FAIL\(col\)'` **恒 0 命中**（行首两空格，W29A 顶回）。**另：`w29-fill.py` 里 `fp_inputs()` 是从 `#27` 继承的硬编码拷贝** ⇒ 它打出的 `inputs_fp` 是**老定义**的值（**记录里用的是重冻脚本填的 `{INFP}`，而那个已改成 `source` 真函数** ⇒ 未污染）—— **"不许复制函数体"这条我只修了重冻脚本、没修 fill 脚本**，已记。

**`#30` 候选（按价值）**：`D-G30` 同族 ｜ **`D-G27`＋`D-G9` 合案**（三个自指值）｜ **`D-G26`**（探针自报 sha，要重取三支臂）｜ `D-G33`（换不依赖构建产物的见证）｜ `D-T5-R` 接线（+101.9 s/趟、35 次 `dotnet`/趟）｜ `D-G34`（`pgrep` 判据要排除调用者命令行）｜ `D-G11`。
