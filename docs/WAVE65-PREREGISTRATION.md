# 波 `#65` 预登记（**判据装置批 E**：`D-G125` 静默丢行修法 ＋ `DECL_UPPER` 承重 token ＋ `AMENDMENT-2/3`；零产品改动）

```
DECL: 37 gen=#65
```

> ⚠️ **本件的时序如实声明（逐字，不许读成"先写"）**：
> **本件是在波 `#65` 的落地读数之后补写的。** 落地动作完成于 `2026-09-24 16:24:55`（`~/w153a/w65rate2/landed/LANDED.tsv` 的
> `mtime`），而本件的创建时间晚于它。**判据本身**（下 §1 的审计链）**确实先于全部落地读数**，但**"预登记件"这个外壳是补的** ——
> 它是因为 `verify-all-step-check.sh`（第 `[11]` 步 `VERIFYALL-SELF`）要求**本代号出现在某件 `docs/WAVE*-PREREGISTRATION.md` 的标题行**
> （`grep -qE "^#+ .*${gen_decl}"`）才被建出来的（主控 `#65` 裁定②，逐字："缺件必致冻前 `37 ✅/0 ❌` 达不成"）。
> ⇒ **补救 = 下面 §1 的 sha16 ＋ mtime 链**（判据件全部早于落地与冻前读数）；**本件不冒充"先写"**。

## §1 「判据先写」的审计链（**sha16 ＋ mtime**，逐件现算）

| 件（本波判据链） | sha16 | **最后修改 mtime** |
|---|---|---|
| `~/w153a/w65rates/criteria.md`（**链的起点**：在册速率/口径穷举的判据，先写后跑） | `3a1ccf6e40888114` | `2026-09-24 14:58:02` |
| `~/w153a/w65rates/rates.tsv`（93 条事实表；风险位**程序计算**） | `cf8e031e72759c09` | `2026-09-24 14:58:59` |
| `~/w153a/w65rates/advisory.md`（建议文本） | `d880a247a0a644f0` | `2026-09-24 15:00:41` |
| `~/w153a/w65rate2/criteria.md`（**本波判据主体**：§3 ③ 判定表／§11 `AMENDMENT-2/3`／白名单／四不变量） | `a08293721b805036` | `2026-09-24 16:14:52` |
| `~/w153a/w65rate2/CONSTRUCTION-ORDER.md`（施工单：施工必记 7 条 ＋ 期望读数） | `0fbba59efae0c578` | `2026-09-24 16:14:47` |
| `~/w153a/w65rate2/cases-grammar.tsv`（7 值语法夹具：**先于**任何落地读数建） | `fcde8e09b014175a` | `2026-09-24 16:15:21` |
| `~/w153a/w65rate2/cases.tsv`（10 行用例；期望先写） | `c36a1a5d2d906f22` | `2026-09-24 16:08:06` |
| `~/w153a/w65rate2/template-amendment.md`（模板 OLD/NEW ＋ 锚断言先写） | `3dd6db267aebf055` | `2026-09-24 15:10:32` |
| `~/w153a/w65rate2/invariant_polarity.py`（四不变量正反双控；**先于**落地） | `b9734955c0b62dd1` | `2026-09-24 16:09:56` |
| `~/w153a/w65rate2/fp_predict_after.py`（`inputs_fp` **after 预测先算**） | `44add431fbcae294`→见 `SHA16.txt` | `2026-09-24 16:2x` |
| **落地时刻（对照点）** | — | **`2026-09-24 16:24:55`** |

**逐字声明**：**§1 全部判据件的最后修改 mtime 均早于落地时刻 `16:24:55`** ⇒ "判据先于读数"成立；
**但**本件（`docs/WAVE65-PREREGISTRATION.md`）**晚于**它 ⇒ 见开头的时序声明。

## §2 本波改了什么（**零产品改动**）

| # | 件 | before → after | 说明 |
|---|---|---|---|
| 1 | `build/MilBridge/tools/shell-quote-trap-check.sh`（`TASK-0718`，W152A 交） | `d4317aa7605a31e1` → **`39a2e2cdf1948675`** | 按 sha **逐字节应用**；自测 `41/41`；`ST_ATTEST=PASS` |
| 2 | `build/MilBridge/tools/fp-manifest-teeth-check.sh`（同上） | **新建** → **`be19edddf7f02797`** | 自测 `13/13` |
| 3 | `build/close-wave.sh`（`fp_inputs()` 白名单） | `168a33c3d4743559` → `9dfc1e43b4d67fbf` | **+1 行**：纳入第 2 件 ⇒ 覆盖面 `161 → 162` |
| 4 | `build/MilBridge/tools/baseline-rate-gate.sh`（`TASK-0719`） | `31cf77abc6a882e3` → `1bad58c07a8264e6` | `D-G125` 修法 ＋ `DECL_UPPER` ＋ 四不变量 ＋ 3‑9 改判 |
| 5 | `build/MilBridge/tools/baseline-rate-cases.tsv` | `8d71171d475a63dc` → `1a2df056f5677344` | **原 11 行逐字节不动** ＋ 新块（`legacy_rows=11`）10 行 ⇒ 21 行 |
| 6 | `docs/PREREG-TEMPLATE.md §3.2` | `17927a59d050fc83` → `300ec21903d54756` | 三栏 `p0`／`p0 时间窗`／`p0 来源锚`（12,397 → 18,874 B；`wc -l` 212 → 223） |
| 7 | `verify-all.sh`（四处声明） | `6a8ae4e80fefd97b` → 见冻前 | **不加步**（仍 `37`）；四处声明的世代位改 `#65` |

**产品面**：`src/**`／`build/shims/**`／native 源 **一字节未动** ⇒ 九位**不应有产品位移**；`pf` 是**环成员**（整波重建必变、**同尺寸 6,123,520 B**）⇒ 机械位移，**不许当漂移/回归判据**。

## §3 两极化 ＋ 判否条件（**先写后跑**）

| 判据 | 正例（必须绿） | 反例（必须红／必须点名） | 判否条件 |
|---|---|---|---|
| `D-G125` 静默丢行 | `--cases` 收下行数 == 声明行数 ⇒ `21/21` | 行宽既非 8 也非 10 ⇒ **响亮 `NOINFO-DECL-COLUMN-WIDTH`**（不许 `continue` 静默跳过） | 出现"静默跳过"即判否 |
| `DECL_UPPER` token | ③ 未命中 ⇒ `EVALUATED` | ③ 命中 ⇒ `NOT-EVALUATED`（**两值全函数**，永不为 `-`） | 出现第三值或 `-` 即判否 |
| ① `decl-upper-pairing-violated` | 7 值 ∪ `{na,-,column-width}` 全在白名单内 | 删掉 `F["DECL_UPPER"]="EVALUATED"` ⇒ 注入变体 `rc=1` ＋ 点名 | 不点名即判否 |
| ② `decl-upper-legacy-judged` | 前缀行 `checked ∈ {na,-}` | 把 3‑7 的 `na` 改成 `unmatched` ⇒ `rc=1` ＋ 点名（**8 处**） | 同上 |
| ③ `decl-upper-newrow-unjudged` | 前缀外新行 `checked ≠ na` | 让首行新行拿 `None` ⇒ `rc=1` ＋ 点名 | 同上 |
| ④ `decl-upper-notreached-unpaired` | `-` 只配三条前置 `NOINFO` 码 | 让 `missing` 分支不打 `checked` ⇒ `rc=1` ＋ 点名 | 同上 |
| 3‑9 改判 `FAIL/1` | 10 列新行 ⇒ `PASS`／`EVALUATED` | 新行退回 8 列 ⇒ **`FAIL/1` ＋ `column-width`** | 判成 `NOINFO` 即判否（**加严不许退回**） |
| 模板三栏 | 替换后旧锚归零／新三栏锚各 1／`§3.3` 仍在 1 | 任一锚命中数 ≠ 预期 ⇒ **停手** | 锚不唯一即判否 |

**读数（落地后现跑）**：`21/21 failed=0 rc=0`（真件）｜`9/9`（语法夹具）｜`BASELINERATE_SELFTEST=PASS`｜四不变量注入 **4/4 全中**。

## §4 `inputs_fp` 成对记账（**承重**）

```
before = f148203453b092206b6a9f9529821fa58570ba7035fb5253ca4f4e8da6dd77ae   （161 件）
after  = 4c1056ddca8afc700df3f0777a2c134932a5fc2ddcfc7d1192c38940e3c15fd5   （162 件）
after == after_predicted = YES
```

**`161 → 162` 的 +1 行归因（逐字）**：`close-wave.sh` 的 `fp_inputs()` 白名单里**新增一行** `build/MilBridge/tools/fp-manifest-teeth-check.sh`（与落点同名）⇒ 清单多一行。
**交叉表**（32 行／16 值；按"哪些件处于已落地状态"命名）：**全部退回 ⇒ 逐位 == `before` ✓**；与仪表 `infp.sh attrib` 交叉核一致 ✓。
**成对同值的机械原因**：`fp-manifest` 那一行**只在 `close-wave` 处于已落地状态时才存在于清单里**（不是"没算进去"，是"这一行此刻不存在"）。

## §5 `D-G120`／`D-G121` 的判据与反极性（本波载体）

- **`D-G120`（续行注释吃掉参数表）**：载体 = `shell-quote-trap-check.sh`（本波按 sha 应用）。**反极性**：真件参数表中段插注释 ⇒ 新件 `FAIL reason=continuation-comment`／旧件 `PASS traps=0`（由 W152A-W65 落地后**只读复跑**，取数时刻写进其报告）。**必需的机器证**：`after==after_predicted` 对同源污染**结构性失明**（两次都走同一条被污染管道）＋ 污染后指纹是 `sha256("")` 形状完美 ⇒ **只有与计算路径无关的两条牙能看见**（清单逐行形态 ＋ 件数对账）。
- **`D-G121`（公式/口径误用）**：载体 = `baseline-rate-gate.sh` ③。**反极性**：`(1,126)` 报 `0.0235`（k=0 式）⇒ `FAIL` 点名 `zero-hit-formula-on-nonzero-sample`；报正确的 `0.0371` ⇒ `PASS`（**防"见 `1/126` 就红"的假红**）；`(0,60)` 报 `0.0487` ⇒ `PASS`（k=0 上合法）。

## §6 射程边界（**这颗牙咬得到／咬不到什么**）

**咬得到**：① 新行退回旧形态（列宽）；② 申报上界与申报公式/样本 `k` 不自洽；③ token 与细分原因自相矛盾；④ 旧行被 ③ 判 / 新行未被 ③ 判；⑤ `-` 出现在非前置码行上。
**咬不到（如实划界，逐条 `NOINFO`）**：
1. **`k` 是否如实**（例如把 `1/126` 的分子记成 0）⇒ 属**输入侧另一条**，本波**不开号**；
2. **"上界该用哪一界当门"**（单侧 vs 双侧）⇒ 属 ⑤′ 口径之争的域；
3. **`fp-manifest-teeth-check.sh` 的调用**：本波**只入覆盖面、不接线**（见 §7）；
4. **三插法等价的证明只限本 21 行**（`NOINFO` 边界）；插入点已写死为 **位置 a**；
5. `required_n`／功效算程**未独立实现** ⇒ 不写死。

## §7 `UNWIRED` 声明 ＋ 接线决策表（主控裁定①逐字要求）

**`UNWIRED`**：`build/MilBridge/tools/fp-manifest-teeth-check.sh` **在 `fp_inputs()` 覆盖面内（第 263 行）但 `verify-all.sh` 里 `grep -c` = 0（未被调用）**，
其**正常运行读数 = `FP_MANIFEST_TEETH=NOINFO reason=no-manifest`（`rc=2`）** ⇒ **既不算绿也不算红** ⇒
**任何汇总不许把它算绿**。接线由主控新开的 `[Next] TASK-0724` 管（并在 `HANDOFF-NEXT.md` 落一行）。

**一行决策表（两种来源 × 是否动 `close-wave.sh` × 是否动 `inputs_fp` × 漏改时红在哪步）**：

| 清单来源 | 是否动 `close-wave.sh` | 是否动 `inputs_fp` | 漏改时红在哪步 |
|---|---|---|---|
| **`--paths FILE`**（在 `fp_inputs()` 尾 `tee` 一份清单） | **是**（`close-wave.sh` **自含于覆盖面** ⇒ 必再挪一次） | **是**（已验的 `after_predicted` 作废，须重算） | 第 `[11]` 步 `VERIFYALL-SELF`（声明/口径）＋ `FP-INPUTS-HYGIENE`（覆盖面）＋ 指纹对账 |
| **`--expect N`**（牙头推荐的**显式常数**） | **否**（`verify-all.sh` **不在**覆盖面 ⇒ 不动指纹） | **否** | 新增一步（`37 → 38`）⇒ 若漏改四处声明：第 `[11]` 步 **`NOINFO`**（缺声明 ≠ 通过），**不是绿** |

⇒ **本波取"只入覆盖面、不接线"**；接线波直接照上表选来源（推荐 `--expect N`，代价最小且不动指纹）。

## §8 「不做回归判定」声明（`TASK-0709` 的适用性；本件**必须**有这一节，否则第 `[•]` 步 `PREREG-FOUR-REQ` 判红）

PREREG-NO-REGRESSION-DECISION:

**本波不做任何回归判定**（`N/A`）：本波只落**判据装置**（一颗牙改严 ＋ 它的台账 ＋ 模板三栏 ＋ 两件交来的牙）与其接线，
**无被试件、无对照臂、无两臂比较** ⇒ 回归判定四要件对本波**不适用**（`N/A`）。本件**不含**任何判定工件机读判词行、
**不含**判定台账的文件名（⚠️ 本件**刻意不逐字复现**那两个"证据串"：该牙的"有证据"检测是**全文子串匹配** ⇒
逐字提到会被判成"有证据"而走 `FAIL` ＝ **假红方向**。这是**已登记的射程边界**，`#60`／`#63`／`#64` 预登记同样踩过）。

**本条的实际作用（如实记）**：`prereg-four-requirements-check.sh` 拿本件的 H1（含"判据"）当**判据节**（`extract_section` 取到全文）
⇒ 节内**逐字**有「本波不做任何回归判定」∧ 全文**无**证据串 ⇒ 判 `PREREG4=NA`（`rc=0`，**与 `PASS` 分开计数**）。
**✋ 我第一次没写这一节 ⇒ 落地后首趟 `verify-all` 在这一步判红（`步骤通过 36 ❌ 失败 1`）—— 这是本波现场咬到的第一处红，
成因 = 新预登记件缺 N/A 声明，不是产品/判据问题**；补本节后该步应回到 `PREREG4=NA`。

## §9 读数更正（主控 2026-09-24 指正；**逐字改口径**）

- **更正前（我报的）**：预登记件里"`#64` 出现次数 = 0"。
- **更正后（现读）**：**标题行含 `#65` 命中 1**（`^#+ .*#65`）∧ **正文出现过一次 `#64`**（`:111` 那句解释性引用
  "`#60`／`#63`／`#64` 预登记同样踩过"，见本件 §8 末段）∧ **标题行 `^#+ .*#64` 命中 = 0**。
- **原因（如实记）**：我最初量的是 **§7 之前的文件**（那时确实 0）；**§8 是我后来补的**，它把 `#64` 引进了正文 ⇒ 总数变 1。
  ⇒ **教训：对"文件级计数"下结论时，必须声明"在哪个版本上量的"**（我这句是跨版本量的 ⇒ 读数失效）。
- **判据影响 = 无**：该牙只看**标题行**（`^#+ .*${gen_decl}`），正文提别代不触发 ⇒ `PREREG4=NA rc=0` 不受影响（主控已独立复核）。

## §10 施工必记（更新至 11 条）

1–7 见 §7 与 `CONSTRUCTION-ORDER.md`｜8 **备份清单必须在读完全部落地目标之后才动手**（`D-G126`）｜
9 **建预登记件必须同趟带 `PREREG-NO-REGRESSION-DECISION:` 声明**｜10 **新建/改动任何被牙读的件后立刻单独跑那颗牙**（不许等整趟 verify-all 跑满 16 分钟）｜
11 **任何"影子/副本/回退树"在写之前先断言 `stat -c %h` == 1，否则拒写并报主控**（主控新发现的硬链接写穿装置缺陷）。
