# 波 `#74` 预登记 —— 「**装置自清理 ＋ `NA` 判据收紧**」（`TASK-0733` ＋ `TASK-0734` ＋ `TASK-0735`）

**owner** = 车道 `W172A`（当前**唯一写者**，波次串行）｜**本波** = `#74`｜**零产品改动**（仪器波）
**落地 2 件**（改内容）＝ `build/MilBridge/tools/r-gate-step.sh`（`aa9d7188b6b01a2f` → **`eafadc1cb3884749`**）／
`build/MilBridge/tools/prereg-four-requirements-check.sh`（`a40aac9031304a8f` → **`d4cd80c33d57919d`**）；
**同趟改 2 件**＝ `build/close-wave.sh`（白名单 **+3 行**）／`verify-all.sh`（首行插本代 `DECL` ＋ 口径句 ＋ `--expect` 同趟）；
**新建 1 件** ＝ 本件。**不加步**（`42 → 42`）。

## §0 判据的输入来源声明（**纪律 36**：取数前声明来源、**现取不缓存、不读台账**）
- **被判件**：上列两个仓内件的**现读**字节（落仓后 `sha256sum` 现算）；其**唯一**变体来源 ＝ 继承包
  `~/w166a/w73/patches/r-gate-step.sh.patched`（`eafadc1cb3884749`）／`~/w166a/w74/patches/prereg-four-requirements-check.sh.patched`
  （`d4cd80c33d57919d`）。**继承包完整性已核**（ENOSPC 窗口 `find` **0 命中**；587 件里 7 个 0 B **全是本就该空的日志**；
  两件现读与包**声明的基底逐位相同**：`aa9d7188b6b01a2f`／`a40aac9031304a8f` ⇒ **本波无重锚**）。
- **磁盘余量** ＝ **现取** `df -Pk <路径>` **第 4 列** ÷ `1048576`（GB）；`<路径>` = 待写目录的**最近存在祖先**；
  分别取 `OUT_ROOT`／仓根／**调用者给的 `OUT` 那一侧**，报**更小者**。取不到数／第 4 列非十进制整数 ⇒ **`NOINFO`**（**不放行**）。
- **残留集** ＝ **现取** `$OUT_ROOT/r-gate-step*`（**只取目录**、**排除符号链接**、`readlink -f` 须**直接**位于 `OUT_ROOT` 之下）；
  **本趟目录由 `janitor <cur>` 显式排除**（在跑的趟永不进清空集）。TTL 默认 `180` min，可 `R_GATE_OUT_TTL_MIN` 覆盖。
- **`NA` 判据的输入** ＝ **现取** 目标预登记件的**判据节**正文（节抽取＝标题含「判据」的那一节，同级标题为止）＋
  所引预登记件**全文**；**不读任何缓存/台账**。射程扫描成员表 ＝ **现取** `docs/WAVE*-PREREGISTRATION.md` 的 glob 展开。
- **覆盖面的活清单** ＝ **现取** `bash build/MilBridge/tools/fp-manifest-step.sh --expect N`（它用 `PATH` 前置 `sha256sum`
  shim 在同码路径上拦截 `xargs` 真正交给 `sha256sum` 的名字表）；**不**重写任何 `find`。
- **步数账** ＝ **现取** `grep -c '^run_step "' verify-all.sh`（`42`）与**首行 `DECL`** 声明的数**两向对账** ——
  这是**唯一不变量**；**`DECL` 行数 ≠ 步数**（现读 `39` vs `42`）⇒ 本件**不写**「`DECL` 行数 == `run_step` 数」。

## §1 判据（**落地前写死**；每条含**两极化**与**判否条件**）

> 本波**不做回归判定**（本波是**仪器波**：只改两颗判据装置本身 ＋ 覆盖面白名单，**不动产品件**、
  不产判定工件、不比对两臂计数）⇒ 四要件对本波 `N/A`。
PREREG-NO-REGRESSION-DECISION: wave74-instrument-only-teeth-and-coverage-decided-none

三态口径：`0 = PASS`／`1 = FAIL`／`2 = NOINFO`（**算不出来 ⇒ 绝不是绿**）。凡「没观察到」一律写成「**没观察到**」，**不许**写成「证不存在」。

### §1.1 `TASK-0733`（`D-G133`）—— 装置临时件必须**自清理** ＋ 长跑前**磁盘余量闸**
| # | 断言 | **判否条件** |
|---|---|---|
| H1 | 默认档跑完 ⇒ **本趟证据目录不存在**（`EXIT` trap 收）∧ `fate=` 行**必在** | 目录仍在／`fate` 行缺失 |
| H2 | **残留有界且不越界**（同趟）：同前缀**过期**件**必消失**；同前缀**新鲜**件／**非前缀**件／**符号链接** **必保留**且不跟随 | 过期件未清（无界）／新鲜件·非前缀件·符号链接被清（越界） |
| H3 | **余量反极**：阈值抬到现场余量之上（或 `OUT_ROOT` 指真小容量 FS）⇒ `DISK_HEADROOM=FAIL` ＋ **`rc=2`** ∧ **装置一次未跑** | `rc=0`／装置仍开跑（「先跑后闸」＝闸是装饰） |
| H4 | **余量正极**：现场余量 ≫ 阈值 ⇒ `DISK_HEADROOM=PASS`（**不许误红**） | 余量充足却红 |
| H5 | 余量**取不到数**（桩 `df` 输出不可解析）⇒ `DISK_HEADROOM=NOINFO` ＋ **`rc=2`** | 不可解析却 `PASS`／`rc=0`（静默放行） |
| H6 | `--keep` 唯一效果 ＝ 本趟目录**不被删** ∧ **必上屏** `R_GATE_OUT_KEPT=<path>`；**不改 `rc`、不改判据** | 改变了 `rc`／判词；或保留却不吭声 |
| H7 | `R_GATE_OUT` 显式给出 ⇒ `ownership=external`：**既不删它、也不入清空集** | 外部目录被删／被当残留清 |
| H8 | **判定面零位移**：`--selftest` **例数只增不减**（基件 `21` ⇒ 新件 `27`）、既有例**逐字不变**（`only_in_base=0`）；`--judge-dir` 对**同一份已落盘证据**的 `c01…c13` 判词格新旧**逐格相同** | 例数减少／既有例被改写／判词格分叉 |

### §1.2 `TASK-0734`／`TASK-0735`（`D-G134`／`D-G135`）—— `NA` 必须**写成声明**才算
| # | 断言 | **判否条件** |
|---|---|---|
| H9 | 判据节里只**提及**那句话、四要件一件没有 ⇒ **必 `FAIL missing=8`**（**不再是 `NA`**） | 仍 `PREREG4=NA`（假绿通道未堵） |
| H10 | 真声明仍 `NA rc=0`：① **行首锚定声明句** ② **机读行**且值非空非否定 | 任一形态不再 `NA`（能力被削） |
| H11 | **值盲令牌**：值 ∈ 否定集（`none`／`no`／`false`／`0`／`n/a`／`na`／`null`／`-`）／**空值**／令牌落在**句中** ⇒ **必 `FAIL`** | 任一形态拿到 `NA` |
| H12 | 声明了 `NA` 却**引用了回归判定工件**（机读行前缀／判定台账文件名，两条既有机械判据）⇒ 既有 `declared-no-rd-but-has-evidence` **仍须 `FAIL`** | 带证据仍 `NA`（**放宽**） |
| H13 | **软／硬分档可见**（`TASK-0735`）：软形态 ⇒ **必上屏** `PREREG4_NA_FORM form=anchor-sentence residual=self-negation-not-checked …`；**硬**形态 ⇒ **不得**出现 `residual=`；两者**都不进计数、不改 `rc`** | 软形态不带该行／硬形态带 `residual=` |
| H14 | **射程扫描**：仓内**全部** `docs/WAVE*-PREREGISTRATION.md` 新旧两臂逐件对账；**出现任何一件 `NA → FAIL` ⇒ 停手报主控** | 出现 `NA → FAIL`（收紧过度，会毁历史绿） |
| H15 | **自证**：**本件自己**必须 `PREREG4=NA rc=0` ∧ `form=machine-line(…)` ∧ **`residual=` 命中 0** | 本件的形态连自己都用不起来 |

### §1.3 范围决定（**显式扩面**；**主控 `2026-09-26` 裁定选 (B)**）
**覆盖面 194 → 197（白名单 +3 行）** —— 依据 ＝ `close-wave.sh` 里**成文**的那条惯例
「**读 ⇒ 进 `fp_inputs()`**（若判"不读"，则须给"命中 0"的机械证才能不进）」
（该判据句在现读 `build/close-wave.sh:223`；同族先例：`#56` 的判定台账、`#64` 的基线率闸 —— 皆因「**接线后的 `verify-all` 读它**」入名单）。
| 件 | 现读 `sha16` | **接线行（落仓时再取一次）** | 为什么进覆盖面 |
|---|---|---|---|
| `build/MilBridge/tools/prereg-four-requirements-check.sh` | `a40aac9031304a8f` | `verify-all.sh:1065`（第 `[34]` 步 `PREREG-FOUR-REQ`，`--gate`） | **读者是生产门禁的一步** ⇒ 改它＝改 `PASS` 的定义 |
| `build/MilBridge/tools/pc-line-step.sh` | `a6a2f0fc1b40783e` | `verify-all.sh:650`（`PcLineOracle·Start` 列） | 件头自称「`TextLine.Start` 的**冻树牙齿**」 |
| `build/MilBridge/tools/frame-step.sh` | `67583d58b2eacddb` | `verify-all.sh:672`（`FrameProbe-frame`） | 件头自称「**帧列**的**冻树牙齿**」 |
**收口判据（不是"我加了三行"）**：落地后**复跑同一次普查** ⇒ **「未被覆盖面保护的接线件 = 0」**。
检举与复算命令（**逐字**，将来任何冻结都可复跑）：
```
# ① 覆盖面活清单（同码路径拦截；件数须 == --expect）
bash build/MilBridge/tools/fp-manifest-step.sh --expect 197 --debug-tmp | grep -E 'FP_MANIFEST_STEP names_n|would_be_fp'
# ② 接线件抽取（42 条 run_step 行所引仓内件）
grep '^run_step "' verify-all.sh | grep -oE '(build|src|samples|docs)/[A-Za-z0-9._/-]+\.(sh|py|tsv|json|txt)' | sort -u > refs.txt
# ③ 差集（**必须为空**）
awk '{print $2}' "$(sed -n 's/^FPMS_TMP_DIR=//p' <上面那趟的输出>)/manifest.txt" | sed "s|^$PWD/||" | sort -u > cov.txt
comm -23 refs.txt cov.txt
```
⚠️ **本条是显式扩面**（**主控批准**），**不属**原派单范围；后来者请勿把它当"顺手加的行"。
⚠️ **同趟必改**（`#71` 教训）：覆盖面变 ⇒ 第 `[42]` 步 `FP-MANIFEST-TEETH` 的声明常数
`--expect 194` **必须同趟改 `197`**（漏改 ⇒ `FAIL reason=files-n-mismatch delta=-3`，**方向安全**）。
⚠️ 覆盖面变 ⇒ **`inputs_fp` 必移**（本波三个新入名单件 ＋ 两个改内容件**都在名单内** ⇒ 三重位移同趟发生）。

### §1.4 候选 `w74b`（**主控 `2026-09-26` 裁定 (A) 批准落地**）—— 补**装饰形态清单的缺口**
**判据先写的话**：`NA` 的两形态里，**"行首锚定"剥的装饰类**原先只有 `[[:space:]>#*|!-]`＋emoji＋`§：`＋数字＋`.`，
**独独漏了行内代码定界符**。⇒ 一件把**整行机读行**用一对反引号包起来（`` `PREREG-NO-REGRESSION-DECISION: yes` ``）
的历史件，被判成"**仅提及**"。**这不是那条声明不成立，是本规则的形态清单有缺口。**
**现场（本波现取）**：`docs/WAVE72-PREREGISTRATION.md:20` 正是该形态 ⇒ 收紧后 `NA → FAIL`
（`PREREG4_NOTE mention-only` ＋ `missing=6`）⇒ 第 `[34]` 步 `--gate` 批次 **`rc=1`** ⇒ **本波冻不了**。
**修法（`d4cd80c33d57919d` → `56f23e0704232675`，4 hunks）**：**只**在**机读行那一支**、**只认整行被一对反引号包裹**
（`'`'*'`'` ⇒ 剥**那一对**）；**只一端有反引号 ⇒ 不剥、不算声明**；**散文声明句那一支**与**值检查（非空 ∧ 非否定）**
**一字未动**；同趟把**可接受装饰形态清单**写进件头（**列全**：空白／`<!--`／成对行内代码／两者组合）。
**射程扫描的成员表与时刻（`D-G140` 口径句）**：`docs/WAVE*-PREREGISTRATION.md` 现取 **55 件**（在册 **17**／生效边界前 **38**），
扫描时刻 = `2026-09-26 09:1x` —— **一次性扫描只是当时的读数**；接线牙（第 `[34]` 步）**每跑必重扫**。

## §2 四处声明（**同趟**落）
① 首行 `DECL`：**插在现行第一行之前**（`decl_line()` = `sed … | head -1`；`D-G131`）＝ `# VERIFYALL-STEPS-DECL: 42 gen=#74`；
② 口径句：头注释里插 `**\`#74\` 收官起 = 42 步**`（`verify-all-step-check.sh` 用 `grep -qF` **逐字**找）；
③ `STEP-NAMES` **一字不动**（本波**不加步**，波名多重集合不变）；
④ 本件（标题行含 `#74` —— 该步用 `^#+ .*#74` 现扫 `docs/WAVE*-PREREGISTRATION.md`）。

## §3 边界（**如实**，不许读成更多）
1. `0733` 的自清理**只覆盖本件自己造的目录**；`R_GATE_OUT` 由调用者给出时 **`ownership=external` ⇒ 不删**（所有权原则）。
2. janitor 按 **TTL** 只清**同前缀**旧件 ⇒ 它**不是**通用临时件回收器，也**判不了**「别的前缀／别的车道的残留越堆越多」。
3. `0734`／`0735` 只判**形态**（声明 vs 提及），**判不了作者真心**；残余洞（行首声明句**随后自我否定**）
   **按裁定不修**，只做**可见性**（`residual=self-negation-not-checked` 只在**软形态**上屏）。
4. 射程扫描只覆盖 `docs/WAVE*-PREREGISTRATION.md` 的**现取成员**；**通过**只说明「**没有一件从 `NA` 变 `FAIL`**」，
   **不**等于「这批件的判据都正确」。
5. 覆盖面只保护「**改了会被看见**」，**不判**「该收的没收」；「读数取到 ≠ 判据成立」（`D-G120` 族）。
6. 本波**零产品改动** ⇒ 九位**只允许环成员 `pf` 同尺寸位移**；出现第二处 ⇒ **停手报主控**。
