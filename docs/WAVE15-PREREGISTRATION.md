# 波 `#15` 预登记（**落地前**写死判据与位移预测）

> 写于 **2026-09-15 12:0x（主控）**，此时活树 = 基线 **`#14`**（`pc 9adac6b8d8e285c3` / `pf ed51db81db76bc76` / `hbtextline 17b2cdfe08f13280` / `bridge 759a322431f1e457`，波 `close-wave-105739`），
> shim / PC 生成源码 / 应用器**一个字节都没动**；三份备稿在仓外：
> `$HOME/wfp-runs/draft-T2/`（T1d：`PATCH-T2-shim.md 7720e823e90f861e`、`CRITERIA-Q5-Q11.md 3484c65b0def23c1`、`D-O1.md 04f2ac374a320ef2`、`MIRROR-tab-anchor.md`、`RISKS.md 9e3c1eaf213145ca`）
> `$HOME/wfp-runs/draft-T2-pc/`（T1c：`patch-draft.md d785d7431ef7ceff`、`idempotency.md 312a97cecac6c808`、`risks.md e8d40b67a28186b5`、`README.md c8a48506cfc4bb2a`）
> `$HOME/wfp-runs/draft-F1b/`（T1d：`DIAGNOSIS.md abfb9c87dd137ecc`、`PATCH-F1b.md da81fb44caa99a98`、`CRITERIA.md 2dca33641564aae0`）
>
> **为什么要预登记**：纪律 26 的教训 —— 判据写在**改动之后**，就会被"事后合理化"污染；且原稿设想**三件改动同波**（**该设想已作废，见 §0 重排**：现在 `#15` = `D-F1c` + `D-F1b`，`D-T2`/`D-O1` 顺延 `#16`），若不先把"谁的位移归谁"写死，任何位移都无法归因。

## 0. 🔄 2026-09-15 11:3x **重排（主控；本文原稿的"三件同波"已作废）**

冻结 `#14` **之后**实测发现：**`#14` 上 `run.sh tline` 跑不完** —— 在 `layout-b34` oracle 段**跑不完**（RSS `1.9→3.1→4.19 GB`、101% CPU、12+ 分钟零输出、打开 `/usr/share/fonts/opentype/noto/NotoSansCJK-Bold.ttc`；按 **PID** `kill -TERM` ⇒ `rc=143`；**`#13` 同段秒级完成**）⇒ 立为在册缺陷 **`D-F1c`**（疑似 `D-F1` 的 `plan == null` + `allowFallback:true` 引入）。

**波次重排**：
- **`#15` = `D-F1c`（自旋/无界内存）+ `D-F1b`（回退 run 的字体身份）** —— 两件同属"字体回退残留"，且 `D-F1c` **阻塞所有 harness 读数**（`tline` 六项、34 条 `+CJK` 对比），必须先修。
- **`#16` = `D-T2`（Tab 缩进，shim + PC 半）+ `D-O1`（`HasOverflowed`，会打开折叠路径）** —— 本文第 2 节 B/C 的判据与"每件落完各自取一次读数"规则**原样沿用**。
- **`#17` = 产物侧 shim sha**（会改 `pc` 并经环传播动 `pf`/`reach`）。
⇒ 下面 **§1 表里的 A（`D-F1b`）保留**、**B（`D-T2`）与 C（`D-O1`）顺延到 `#16`**；**新增 A′（`D-F1c`）** 的判据见 §2 末尾。

## 1. 波次范围（**`#15` = A′ `D-F1c` + A `D-F1b`**；表中 B/C 已顺延 `#16`，各自可独立归因）

| # | 改动 | 车道 / 落点 | 锚点（现值，落地前须现场重读） |
|---|---|---|---|
| A | **`D-F1b`** 回退 run 的字体身份 | T1d / `build/shims/PresentationCore.HbTextLine.cs` | `:3517`（P1：1 参构造 ⇒ 带面号；`faceIndex==0` **不加** fragment，`n>0` ⇒ `GetComponents(AbsoluteUri,SafeUnescaped)+"#"+n`，语义 = 上游 `FontCacheUtil.cs:509-520`）；`:3518-3519`（P2：段面取不到 ⇒ **记账，不再静默**） |
| B | **`D-T2`** Tab 缩进语义 | T1d（shim）+ T1c（PC 生成物/应用器） | shim `:1589` `double pen = 0;`（**出现 1 次**）⇒ `= lineContentStart`；shim `:331` `TabClampInset(double paragraphIndent) => 0.0;`（**取"盒"侧**）；PC 生成物 `build/PresentationCore.Linux/TextFormatterImp.Linux.cs`（sha `5dedc21f5f372c78`，属主应用器 `patch-presentationcore-textline-fallback.py`）**5 个编辑点**（签名 `:230-231`、两个 `FormatParagraph` 调用 `:253-256`/`:302-305`、两个调用点 `:553-560`/`:569-575`） |
| C | **`D-O1`** `HasOverflowed` | T1d / shim | `:3086` `public override bool HasOverflowed => false;` |

**落地协议（2026-09-15 12:1x 定稿；取代"每件落完各自取一次读数"的笼统说法）**：
1. **静窗**（`pgrep` 确认无其他车道跑构建/harness；记 `loadavg` + `free -m`）。
2. **`D-F1c` 先落**（T1d 唯一写者；**整份 shim 先备份 + 记 sha**；B1+B2 主修法、B4 可观测上限、**B3/B5 不做**；DIRECT + 两个非 DIRECT 宿主 **`error CS` = 0**）。
3. **`D-F1b` 再落**（同一窗口、同一写者；**共用点 + `:3517` 走同一 helper**；`faceIndex==0` 不带 fragment、`n>0` 带 `#n`）。
4. **读数分开取**：㈠ **T2（独立）** = 峰值 RSS / `Scans` / `candidates` / **面选择普查**（逐格相同）/ `D-F1` 正极性 / `D-F1b` 的 C1 三腿；㈡ **T3 跑一趟有界 `run.sh tline`**（RSS 监视 + `timeout`；**`D-F1c`"能跑完"的唯一证据**，并产出 34 条 `+CJK` 对比所需产物）。
5. **随后发波 `#15`**（重建 PC/桥 ⇒ 应用级门禁 + `verify-all` + 重冻）。
6. **任一步不成立 ⇒ 停、回退该件**（整份还原 + `sha256sum` 自证；还原后**必须强制重编**，纪律 24）。

## 2. 预登记的判据（**落地前**写死）

### A `D-F1b`
- **正极性（必须变绿）**：`GlyphTypeface.FontUri` == 覆盖 `U+4E0E` 的实际整形面，且 `.ttc` 带面号（本机应为 `…/NotoSansCJK-Regular.ttc#2`）；**C1 `gid < GlyphTypeface.GlyphCount`**（负极性 = 现在这版：`9498 > 3884` 必红）；**C3 `AdvanceWidths[gid]×emSize == 观测 advance`**（`16.0000`）。
- **C5（face 0 不带 fragment）**：另一极 = 段落字体**自己覆盖**该码点 ⇒ `FontUri` 的 `Fragment` 为空（真值形态 `MSYH.TTC`；**真值里含 `#0` 的行 = 0**）。
- **不变（机器断言）**：行宽/advance 数值（`16.0000` / 折后 `20.1600`）、`tab-*`、`bidi`、`shaping`、`layout-b34`、`modifier-*` 逐位不变。
- **预期位移集合**：`font-fallback` 族读数 + `FACE_URI` / `ADV_FROM_TYPEFACE` 两类字段 + P2 新计数（默认 0）。
- **红则停**：若修后 `FontUri` 仍为段落字体 ⇒ **不许继续**（说明还有第二个挂面点）。
- **⚠️ 2026-09-15 追加（T2 在 `#14` 上的读数，本条**推翻**"只修 `:3517`"的原稿）**：`[fb/b34] C1=FAIL` —— **有计划的那条路也报段落字体**（同一 run 的 `PLAN_FACE=NotoSansCJK-Regular.ttc#0`）⇒ **根因在两条路共用的 run 构造**（`FormatLine:2455-2458` 那段把 `face` 落到 `GlyphRun` 的地方）⇒ P1 必须落在**共用点一次修完**，`:3517` 只作为同一不变量的第二个落点。
- **C1 口径（字体无关、本地可判红）**：① `gid < GlyphTypeface.GlyphCount`（负极性：`9498 ≥ 3884` ⇒ 红；**独立 `maxp.numGlyphs` = 3884 两嘴一致**）｜② 覆盖腿 —— `gid≠0 ⇒ 报出的面必须覆盖该码点`、`gid==0 ⇒ 必须不覆盖`（**绝不用 gid 等值**：该 ttc 10 面对 `U+4E0E` 给 `9497`(面0/1/5/6)/`9498`(面2/3/7/8)/`9499`(面4/9)，**差 1 全部来自 GSUB `locl`**，实测 `#0+zh-cn ⇒ 9498`、`#0+en-us ⇒ 9497`）｜③ `AdvanceWidths[gid]` 可查得且 `×FontRenderingEmSize == 观测 advance`（负极性 `TF_ADV_PRESENT=false`；修后本机应为 `16.0000`）。
- **本地取不到的那一极必须报 `NOINFO`**：本机计划面是 **face 0** ⇒ **face 0 不带 fragment（C5 本地可测）**；**`#n`（face>0）形态本地可能取不到 ⇒ 报 `NOINFO`，不许当绿/当红**。
- **`D-F1` 本体的判定（本轮已成立，写进记录）**：C2（`gid=9498≠0`）+ C3（`LINE_W` 差 `0.0000`、折后宽差 `0.0007`、区间一致）+ **CR 极**（`F_nbsp_zwsp_w40#3` 的 `CR=[14,2) W` **`−3.0560 → +3.3440`**，真值 `3.3433`）**全部 PASS**；**本轮唯一的红 = 新 C1 = `D-F1b`**；`nofb` 腿与 harness 旧产物**逐字相同** ⇒ 归因干净。**判据层的"公开 API"只说明观测面在哪，不等于外部独立** —— 外部独立只有 `advance_from_font.py`（自读 cmap/hmtx/maxp）。
- **位移预测的"结构性依据"（主控现场复核，2026-09-15）**：`SegmentIndex` 的**唯一行为消费点**是 `build/shims/PresentationCore.HbTextLine.cs:2456-2458`（把面挂到 `GlyphRun` 上）；`FaceSlot` 只在 `:604`（`SegmentIndex = seg.FaceSlot`）被读进 chunk 结构，其余出现处全是**赋值/复制/诊断**（`:476` 默认 `-1`；`:3519`/`:3903`/`:3912`/`:3918` 赋值；`:528`/`:534`/`:3269` 复制；`:493` 诊断串）⇒ **面身份只影响"上报/挂接"，不参与任何度量** ⇒ **行宽 / advance / 折后宽 / `Extent` 必须逐位不变**（机器断言）；**若它们动了 ⇒ 面被度量路径消费了 ⇒ 立刻停、报我**。另经复核：全 shim 只有 **1** 处 `new GlyphTypeface(`（`:3517`）、**1** 处 `new Typeface(`（应用路径按家族名解析）、`BuildSegmentFaces` 只有 **1** 个调用点（`:3969`，在 `TryBuildPlan` 内）⇒ **调用方自造的计划永远拿不到面表**（`FaceSlot` 恒 `-1`），与 `[fb/b34] C1=FAIL` 逐项吻合。

### B `D-T2`
- **本地现实**：三套 tab 语料里 **`Indent>0`/`ParagraphIndent>0` 的 arm = 0 个** ⇒ **本地一切读数应逐位不变**（`tab-zero` 结构败 `1`、`tab` `57/0`、`tline` 六项、`Extent` 余差 `58`）。
- **可判部分（本地）**：`tab-anchor` **镜像臂**（`CoverageProbe --tab-anchor-arm <json>`）里的**字体无关列** —— 规则级纯数据核验、`breakCause`、`hasOverflowed`、行区间的布尔与算术列 ⇒ 判 Q5–Q11。
- **必须走 U1 真机、且必须落地后重取**：行宽 / 逐字符位置 / 钳位数值族（真值面 = Arial `fontSha256=baa25152…`，本机无该字节 ⇒ 不可比）。**不许拿 `#13`/`#14` 的旧真机读数对**。
- **接线成功的机读判据**：PC 生成物里 `ParagraphIndent` 出现次数 **0 → 2**；`indentDip: paragraphIndent` 落码后 = 2；`FormatParagraph(` 次数**不变** = 3；应用器 `--check` **rc=0 ×2** + 生成物 sha 跑前==跑后。
- **红了代表什么**：`TabClampInset` 若真语义是"容器"侧 ⇒ **Q6 + Q8 两族同时红**（`Indent`/Q5/Q7 不受影响）⇒ 那是"选错侧"，不是回归。
- **预期位移集合**：仅 `Indent>0`/`ParagraphIndent>0` 的 arm（本地 0 个）；**其它任何位移 = 回归**。
- **风险（T1d 点名）**：S1 若写错成 `indentDip`，**`Indent=0` 时看不出来** ⇒ 判据**必须**含一条 `Indent>0` 观测 ⇒ 这是"要等 `tab-anchor` 臂才能宣称对齐"的原因。

### C `D-O1`
- **判据**：Q11 那 **4 例** 真值 = `true`；`tab-anchor` 里"**自然到达边缘**"那批 = `false`（反例批）。
- **分开归因**：`D-O1` 单独一条命令、单独一张表；与 `D-T2` **同一读数里两族齐变 ⇒ 该结论作废**。
- **预期位移集合**：只动"读 `HasOverflowed` 的那几列"；其它列逐位不变。
- **⚠️ 2026-09-15 量级上调（T2 实测炸出，必须计入）**：`HasOverflowed` 同时是**折叠资格闸门**的输入 —— `build/shims/PresentationCore.HbTextLine.cs:3142` `if (!HasOverflowed && !_keepState) return this;` ⇒ 恒假意味着**只有 `_keepState` 路径会折叠**（T2 的 CR 极**必须**用 `alwaysCollapsible:true` 才测得出来，就是这个原因）。⇒ **修 `D-O1` 会把折叠路径打开**，位移集合**必须包含"折叠"族**（逐族修前/修后对照 + 方向解释），**不只是 Q11 四例**。⇒ 这也是"落地顺序 `D-F1b` → `D-T2` → `D-O1`、每件落完各自取一次读数"的**硬理由**：否则 `D-T2` 的"本地应逐位不变"会被 `D-O1` 污染、归因直接作废。

## 3. 波务（主控）

1. `WAVE_OWNER` 认领 + `close-wave.sh`（`[0/6]` 预检 → 集成波 → 原生 → 桥 → 身份 → `verify-all` → 摘要 + 哨兵）。
2. **`ARTIFACT-SRC-FP` 两极化牙（本波免费获得）**：shim 一定变 ⇒ `file=17b2cdfe08f13280 …` 那一行**必须变**（正极性）；**未改动的行必须逐位不变**（负极性）。这是 `fp_inputs()` 补上 shim 之后**第一次**自然获得的两极读数。
3. **`HelloWpf` Release 收敛**（`APPSYNC` 既存红）：**✅ 已于 2026-09-15 12:32–12:33 关闭（主控，波前就做掉了）** —— `dotnet build -c Release samples/HelloWpf/HelloWpf.csproj`（0 错 0 警）使被点名的 `Provider.dll` 出现但**与权威不同**（`6134073cdbba990c` ≠ `71ba86c6495347fe` ⇒ `MISSING=0` 换成 `DIVERGENT=1`），按全仓模型同步**权威 Debug 副本**进该 refdir 后 ⇒ 校验器 **`OK=45 MISMATCH=0 MISSING=0 UNEXPECTED=0 DIVERGENT=0` = `APPSYNC=PASS`**；冻结件跑后逐位复核未变。⇒ **本波只剩一件事**：**把"该 refdir 的权威同步"纳入波的 app-local 刷新步骤**（否则下次 `-c Release` 构建会把它重新变成 `DIVERGENT`）。
4. **`D-R2` 关闭判据**：连续 5 趟 `verify-all` 无崩溃（当前 3+；本波 +1）。
5. 冻 `#15`：九位 + `inputs_fp` + 两档门禁 6/6 + 波内 `verify-all` + 三方逐位一致。
6. **把 `build/MilBridge/tools/tline-gate.sh` 接成 `verify-all` 的第 10 步**（**先决条件**：新世代有完整 `tline` 读数 ⇒ 按 T1b3 报告 §7 的三步 + **第 0 步门禁**（`grep -a '^通过 .* / 失败 '` 缺 ⇒ 不许钉；**harness `rc > 128` ⇒ 这趟不算读数 ⇒ 不许钉**）重钉 `generation` ⇒ `--logdir` 自检 `PASS` 且 `gone/drift == 0`）⇒ 接线后**记"步数与口径已变"**（`verify-all` 从 9 步变 10 步）。**门禁当前 sha**：`tline-gate.sh b8863c8d9f8ea59b`、`known-red.json 75f7823fdd39bb43`（schema `tline-known-red/3`、判据版本 `t1b3-tline-gate/2`）。**门禁是"读者"**（无任何"跑"的能力；只写 `$OUTDIR/arms.tsv` 与 `$OUTDIR/gate-readings.json`，都在 `$HOME`）。

## 4. 本波**不**做的（避免范围蔓延）

- **不**把 `tline` / `CoverageProbe` / `TextLineProto` 接进 `verify-all`（**但已登记为覆盖缺口** —— 现场 grep：`verify-all.sh` 只有 2 个构建 + 7 个测试工程 + 1 个 python 校验，**完全不含** MilBridge harness ⇒ `A8` 那种红可以长期"没人跑"）。接入需要先把 `run.sh tline` 的**在册红**变成"登记制"（`--known-red` 语义），否则会把 `verify-all` 永久染红 ⇒ **单列下一件**。
- **不**动 `build/MilBridge/tests/CoverageProbe/refs/**`、`build/MilBridge/staging/**`（**逐字节历史快照**，见 `handoff.md` 的更正块）。
- **不**动 `tests/parity/windows/**`（真值面，U1 领地）。
- **不**改 `ACCEPTANCE-BASELINE.md`（只在波收尾后冻新版本）。

### A′ `D-F1c`（自旋 + 无界内存）—— **本波第一件（`#15`）**
- **现象（已实测）**：见 §0 的现场读数。**触发面** = `run.sh tline` 的 `layout-b34` oracle 段（该段走**系统字体**，机器上真有 `NotoSansCJK-Bold.ttc`，**10 个面**）。
- **两条负对照（已更正，2026-09-15 12:0x）**：字体环境**两侧相同**（T2 从未设过旋钮；他那趟同 `fromSystemScan=1`）⇒ "T2 用 `build/fonts`、harness 用系统"**不成立**；真正的差别 = **内存压力 + 用例数**（T2 12 s 跑完、harness 12 min）。
- **实机制（T2 四趟有界实测）**：`Scans == 1`（`candidates=371(扫描1次)`）⇒ **不是循环**；**单次**读数峰值 **3.46 GB**（九次 3.48 GB ⇒ 峰值由一次扫描决定）；`WPF_LINUX_FONT_DIR=build/fonts`（4 面）⇒ **63 MB / 55×**；只 `hb_shape` ⇒ **8.6 MB**（非 shaper）；Bold 只是"扫遍每文件每面"的症状。⇒ 现象 = **一次性物化系统全部面并常驻** + 交换抖动（机器已用 4.1 GB / swap 801 MB）。
- **复现纪律（已更正）**：**`ulimit -v` 不可用**（CLR 起不来 `Fatal error. .NET runtime failed to initialize.`）⇒ 用 **`DOTNET_GCHeapHardLimit`** + `timeout 60` + **只按 PID 杀**，并记 `loadavg` 与 `free -m`（判读**依赖内存压力**）。
- **预登记的修法风险 + 预案（T1d 论证，主控批准）**：**B1 不可能改"选哪一面"**（`files.Sort(Ordinal)` 未改、`Cand` 六字段读法未改、`s_all` 内容与顺序**逐位相同** ⇒ B1 只改"物化时机"）；**B2 加了一个 AND 条件**（位图 ∧ `NominalGlyph>0`）⇒ 二者严格等价则结果不变，**若普查出现换面 ⇒ 最可能正是二者不等价**（cmap 形态/变体选择符差异）⇒ **预案 = 把位图降为"只做预筛、绝不参与判负"**（位图说"不覆盖"时仍走 `NominalGlyph` 确证）= **"只省成本、不改判据"**。
- **另登记的待办（不在本次修法内，单独一波）—— 只读分析已做（T1d，`~/wfp-runs/T1d-coversface-analysis.md` `4624caa3fc239679`）**：`TryFindCovering` 约 `:978` 的 `HbShaper.CoversFace(…)` 返回值被丢弃，**既不是死代码、也不是漏掉的 `if (!…) continue;`** —— 该函数**不是纯函数**（内部先 `HbFaceCache.Covers`，再 `HbFallbackDiag.NoteCoverageProbe(cacheHit)` **记账**，并预热 `Entry.Cover[cp]`），而**两个判据的实体是同一个 HarfBuzz 调用**（`hb_font_get_nominal_glyph != 0`）⇒ 对调用方**等价**（`P ∧ P ≡ P`）⇒ 接不接 `continue` **不改变"选哪一面"**，只在"不覆盖候选"上省一次 native 调用。**判据（单独一波时用）**：开/关该 `continue` 的两个 `/tmp` 变体，在同一组码点 × 同一批段落字体上，**候选集 + 选中面身份 + 全部计数器逐位相同**（只允许耗时不同）。⇒ **`D-F1c` 不含它** ✓
- **前置依赖已实测（T1d，2026-09-15 12:2x）**：`nm -D /lib/x86_64-linux-gnu/libharfbuzz.so.0` ⇒ `hb_face_collect_unicodes=1`、`hb_set_create=1`、`hb_set_destroy=1`、`hb_set_next=1`、`hb_face_get_glyph_count=1` **全部存在** ⇒ B2 只需在 shim 里**新增 3–4 个 `DllImport` 声明**，不改其它；**若落码时发现任一为 0 ⇒ 停、报我**（那时改用"候选面 `NominalGlyph` 但**即时 destroy**"的等价有界法）。
- **防漂断言**：落码脚本 `~/wfp-runs/land-F1cF1b.sh`（`f95dab80a3629c09`）对每处锚点同时记「备稿行号 + **逐字原文** + `grep -cF` 计数」，**用逐字原文定位、行号只作提示**，任一断言不过即 abort；前置闸门 `pgrep` 无并发 + `cp` 备份 + `cmp` 逐位自证。**B1 可机械落地；B2/B4/F1b 到那一步脚本会停下交人工**（不硬改）。
- **可读计数器（主控现场复核存在，判据写成计数式）**：`HbFaceCache` 的 `FaceLoads`（物化面数）/ `FaceLoadFailures` / `Evictions` / `ClearCoverLocked` 的 `CoverClears`（`:651`、`:678`、`:685`），`HbFontCandidates.Scans`（`:916`），诊断行 `:4008` 已印 `faceEvictions=`。⇒ **修后应满足**：**`FaceLoads ≈ 1`**（修前 ≈ 300）、**`Evictions = 0`**（`s_faces` 按 `path\0faceIndex` 计面、`MaxFaces = 512`，本机 300–414 面 ⇒ **本就不淘汰** ⇒ `Evictions=0` 是**预期**、不是缺陷）、**`Scans = 1`**、**`candidates = 371` 不变**。⇒ **若修后出现 `Scans > 1` ⇒ 查是谁调了 `:994`**（那里 `s_all = null; Scans = 0` —— **重扫入口确实存在**，只是本次未被触发）。
- **㈠ 正极性（"能跑完 + 资源有界"）—— 2026-09-15 首轮实测 = 只成立一半，波暂缓**：T3 的有界趟（件 `16db2d6194edc1f5`、`WPF_LINUX_FONT_DIR` 未设）⇒ **`rss_peak` 81 MB（⚠️ 那是"卡住的跑"的读数 ⇒ 不可用）**；但 **`rc=143`（按 PID 止损，`elapsed 633 s`）**，**停点前移到 `T1 · 73 例 CJK` 段头之后、零输出**（修前 T1 段 73/73 跑完、挂在 T2 段）、99.7% CPU、采样时无字体 IO ⇒ **自旋仍在、且是另一处**。⇒ **正极性不成立 ⇒ 不发 `#15` 波**；已派 T1d：**栈样本**（`dotnet-stack`/`gdb`，取不到就报原因）+ **按配置二分**（V1 当前件／V2 去 `D-F1b` 共用点／V3 去 `D-F1c` B1/B2，各 `timeout 150` + RSS 采样 + 按 PID 止损 + `gen/**` 跑前跑后 sha 复核）。
- **🔴 根因定案（主控读码，2026-09-15 12:00；**六腿二分取消**）**：`build/shims/PresentationCore.HbTextLine.cs:1021` 的 B2 覆盖位图循环 **每轮把游标重置为 `0xFFFFFFFF`** —— 而 `hb_set_next(set, &cp)` 的语义是"取 `*cp` 之后第一个元素；若 `*cp == HB_SET_VALUE_INVALID(0xFFFFFFFF)` 则取第一个" ⇒ **每轮都被要求"从头给第一个"** ⇒ 永远返回同一个值、`HashSet.Add` 反复空加 ⇒ **纯用户态死循环**（无 IO、无输出、CPU 满、内存平）。**与三道现场逐条吻合**：**0 面秒过**（不进该函数）／**≥1 面第一次探测就进不去出**（T2 原话）／`WPF_LINUX_TEXTLINE_DIAG=1` **一行都打不出来**（永不返回）／**采样看不到字体 fd**（mmap 早已完成）／**RSS 恒 63–65 MB**（`HashSet` 只有一个元素）。**正确写法**：`uint c = 0xFFFFFFFFu; while (hb_set_next(hs, ref c) != 0) { set.Add((int)c); }`（**循环体内不重置 `c`**）。
- **内存半边的诊断口径（2026-09-15 12:2x 定）**：先取 **3 个阶段点**（`FormatParagraph` 入口 / `EnsureScan` 首行 / `TryFindCovering` 首行）+ 外部 5 s 采样，并**必须分解 `/proc/<pid>/smaps_rollup`**（`Rss`/`Private_Dirty`/`Private_Clean`/`Shared_File`）—— **`Shared_File`+`Private_Clean` 占大头 ⇒ mmap 未释放**（查 blob 引用/`hb_face_destroy` 是否真降到零）；**`Private_Dirty` 占大头 ⇒ native 堆未释放**（查 `hb_ot_font_set_funcs` 建的 accelerator/表缓存是否随 face 释放）。**主控读码已收窄嫌疑**：`LoadMeta` → `CountGlyphs`/`ReadName`/`ReadOs2` **都不是"整文件读进托管内存"**（走 `hb_face_reference_table` 单表视图 + 小 `byte[need+8]` 缓冲 + `finally` 销毁）⇒ **托管侧不可能有 371 × ~9 MB 常驻**；**关键假设** = `Load` 里那句注释"`hb_blob_destroy(blob); // face 自己持有一份引用`"—— 若 `hb_face_create` 在某些路径上并未持有引用，face 销毁后映射仍活着，形态正与"`Shared_File` 占大头"吻合。
- **装置守卫口径（T3 提出，主控裁定）**：**守卫上限必须高于"合法峰值"**，否则趟必被杀 ⇒ 拿到的是"**被守卫止损**"而不是"能不能跑完" = **仪器自造假红**（与 `L27` 同族、方向相反的一格）。⇒ `D-F1c` 内存半边修好之前**不放** `tline` 的有效趟（`RSS_LIMIT_MB` 保持 2048，**不许为"跑通"而临时抬高**）。
- **计数可读性裁定（2026-09-15 12:2x，T1d 读码 + 主控裁定）**：`HbFaceCache.FaceLoads/FaceLoadFailures/Evictions/CoverClears`（`:656`）与 `HbFallbackDiag`（`:1086`）/`SummaryFragment()`（`:1174`）**均不在 `#if TEXTLINE_SHIM_DIRECT` 内** ⇒ **反射形态宿主可直读**；只有 **`HbTextFallback.SummaryFragment()`（`:4125`；`#if` 起于 `:3694`）**调不到，而它只是**打印**那几个计数的地方（`:4008` 的 `faceEvictions=`）。⇒ **零代码改动**：宿主自己把 `HbFaceCache.*` 打出来即可；`D2-counters.md` 降级为"未启用备选"（**可读性不是问题，只是打印位置在条件编译里**）。**要求 T1d 把"哪些成员在哪种配置下可读 + `#if` 边界行号"的表留在报告里**（与"隔离出口 native 解析"同型教训的解药）。
- **窗口 3 实测（T2，2026-09-15 12:2x）—— 一半达成**：**`D-F1b` 首次全绿**（`FACE_URI=…/NotoSansCJK-Regular.ttc`/face 0 不带 `#`、`GID=9498`、`GID_LT_COUNT=true`、`ADV_FROM_TYPEFACE=16.0000`、C1 三腿 `null`/`fb` 两条路全绿、**普查 26/26 逐格相同**、`D-F1` 无回退、**`CRITERIA=PASS` 首次全绿**）；**内存 `3.30 GB → 2.02 GB`（−39%，仍差 7×）**，**`faceLoads` 372 → 393（不降反升）**、`evictions=0`；**已声明的仪器位移被实测证实**：`coverageProbe 60 → 53` ✓（位图前置确实生效）。
- **主控读码更正一条**：`EnsureScan` 里 **`HbFaceCache.Release(f, i)` 确实每轮都在调用**（`:972` 附近）⇒ **"没调 Release"不成立** ⇒ 只剩两种形态，**必须靠 `smaps_rollup` 分解区分**：**`Private_Dirty` 占大头 ⇒ native 堆未归还**（`hb_font_create` + `hb_ot_font_set_funcs` 的 accelerator 被 free 但 glibc 不还给 OS）；**`Shared_File`/`Private_Clean` 占大头 ⇒ 映射仍驻留**（face 销毁未把 mmap 降到零引用）。
- **窗口 4 实测（T2，件 `14f6a728bc834c0e`）—— 真因定死**：`smaps_rollup` 峰值处 **`Shared_Clean(=Shared_File)` 占 83–88%（1.67–1.76 GB）**、`Private_Dirty` 仅 ~10% ⇒ 走"**映射仍驻留**"支；**`residentCount=22` + `releaseCalls=371`** ⇒ **释放发生了、内存没还**；**`segmentFaceResolveCalls=3` vs `faceLoads−evictions=393`** ⇒ **候选 A（驻留来自段面解析）证伪**。**独立交叉核对（T2 自算）**：目录 **299 文件 / 371 面**（与 `candidates=371` 逐位相同）｜**Σ文件大小 413 MB**｜**Σ(文件大小 × 面数) = 1,902 MB ≈ 1.86 GB**，实测 shared 峰值 1.67 GB ⇒ **实测/理论 = 90%**（例：`NotoSansCJK-Bold.ttc` 19.1 MB × **10 面** = **191 MB**）⇒ **`.ttc` 被按"面"重复 mmap，且映射从未销毁**。**免费对照**：纯计数件复现上一趟峰值（Δ `36 KB` = 0.0017%）⇒ **非红旗**。
- **窗口 5（进行中）**：先做**微实验**（单文件 `hb_blob_create_from_file` ⇒ 触碰页面 ⇒ `hb_blob_destroy` ⇒ 看 `/proc/self/smaps_rollup` 的 `Shared_Clean` **回落与否**）⇒ ㈠**可归还** ⇒ 修法目标 = **峰值降到单文件量级（几 MB~20 MB）**；㈡**不可归还** ⇒ 目标先定 **~600 MB（Σ文件大小 413 MB）**，并**同时**查引用链（`hb_face_create` 的"face 持有一份引用"假设、`hb_face_reference_table` 的子 blob、三条路径的 `finally`）。**修法规则 = 一个文件一份映射**：`FaceCountOf` / `Load` / `LoadMetaOnly` 三条路径**同一规则**，**绝不为每个面各建一份 blob**。**不许变**：`candidates=371`/`Scans=1`/选面/行宽/advance/CR/`D-F1` 极性/`CRITERIA=PASS`；**允许变并须声明**：`coverageProbe`（现值 53）、`faceLoads`（视口径）、新增计数。
- **窗口 6 实测（T2，件 `ac4104d67687c2c9`）—— "blob 持有者"这个方向被否掉**：**`liveBlobsPeak = 1`**、`[LIVEBLOBS]` **stderr 0 行**（全程建过的 blob **< 10**），而同一趟 **`shared_clean = 201 MB`**（1CJK 档：1 个 10 面 `.ttc`、`candidates=10`）⇒ **`liveBlobs 峰值 − residentCount` 得到 `1 − 2 < 0`（无意义）** ⇒ **那 1.67 GB 映射不经过 HarfBuzz 的 blob 门**（HarfBuzz 侧**已不泄漏** = 窗口 5 的干净功劳）。系统档三窗（窗口 4/5/6）**峰值与 `shared_clean` 纹丝未动**（2.02 GB / 1.67 GB）；三档缩放 **1CJK 201 MB ≈ 10 面 × 18.6 MB**、7CJK 528 MB、**4LAT = 0**（只在大 `.ttc` 上显形、量与"按面"吻合）。
  ⇒ **`D-F1c` 的内存半边不是 shim 里的问题**，而是 **PC / 渲染侧"按面各建一份整文件映射"**。**候选（主控读码锁定）**：㈠ **PC 侧上游 `FileMapping : UnmanagedMemoryStream`**（`build/PresentationCore.Linux/FontCacheUtil.Linux.cs:824`，`OpenFile(:846)` 走 `CreateFileMapping`/`MapViewOfFile` ⇒ 在我们这儿被 shim 成 `mmap`；**构造点不在 `build/PresentationCore.Linux`**，在上游被编译的源码里）；㈡ **渲染侧** `src/WpfGfx.Linux/Interop/MilHandleTables.cs:671` `SKTypeface.FromFile(path, faceIndex)`（FreeType 可能**每个 typeface 各 mmap 整份文件**），另有 `MilPresentation.cs:493` `SKTypeface.FromFile(path)`。
  ⇒ **窗口 7 = 只读诊断**：**T1d** 做两条 2 秒微实验（**只 PC 侧**：逐面 `Typeface`/`TryGetGlyphTypeface`；**只渲染侧**：逐面 `SKTypeface.FromFile(path,i)`／经 `MilFontFace_RegisterFromFile`）看 `Shared_Clean` 斜率（≈18.6 MB/面 ⇒ 就是它）；**T2** 修好 `maps-at-peak` 采样器（上趟 4 份快照**全 0 行** = 它自己装置的坑，已记账）后复取 1CJK 档，**用 `/proc/<pid>/maps` 直接指认"哪个文件被映射了几段"**。**修法的写者待定**（`build/PresentationCore.Linux/**` = T1c；`src/WpfGfx.Linux/**` = M7b），**定位清楚后由主控派**。
- **窗口 7 实测（2026-09-15 12:3x）—— 靶子钉成"可数段"**：
  - **T2（采样器修好后，1CJK 档）**：`/usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc`（**磁盘 18.6 MB**）被映射 **12 段**、Σ虚拟 **223.0 MB**（**÷18.6 = 12.0** ⇒ 同一文件被独立映射 **12 份整文件**）、**字体 ΣRss 161.2 MB** ↔ `smaps_rollup.shared_clean` **198.9 MB**（字体占 **81%**，其余是 dotnet 自身共享库）⇒ **对得上、非采样器问题**。**采样器旧版全 0 行的真因**：`cp /proc/<pid>/maps` 对**每个** PID 都做，其中一个**瞬时子进程已消失** ⇒ `stat` 失败 ⇒ 一个文件都没落；修法 = **只钉本 tick RSS 最大的那个 PID** + 补 `/proc/<pid>/smaps` + `snap-ok.txt`。
  - **T1d（实验 B）**：**裸 FreeType** `FT_New_Face` 逐面 10 面仅 **+8 MB** 且全回收 ⇒ **只排除"裸 FreeType"，不排除生产路径**；**生产路径是 `SKTypeface.FromFile`（经 Skia font manager 的 `SkData` mmap 整份文件）** ⇒ **"测谁就报谁"**：已要求用 **SkiaSharp 托管 API** 重跑 B1（`FromFile` ×10 存活）/ B2（按路径共享 `SKData` + `FromData(data,i)` ×10）。
  - **PC 侧候选 ㈠高度可疑**：原生 shim 里 `CreateFileMappingW`/`MapViewOfFileEx` 是 `return NULL` 的桩、`UnmapViewOfFile` 空实现（`src/WpfGfx.Linux.Native/src/win32_misc.c:400/404/406`）⇒ 上游 `FontSource.GetUnmanagedStream()` 那条链在 Linux 上**应当直接抛**（待 T1d 用实验 A 一句话证实）⇒ 若成立，**12 段就只能来自 Skia 那条**。
  - **新判据（一句话）**：修对之后该 `.ttc` 的**段数 12 → 1–2**、字体 ΣRss **161 MB → ~19–37 MB**、`shared_clean` **~30 MB 量级**、1CJK 档权威峰值 **≤300 MB**（今天 415 MB）。
  - **已派**：**M7b**（`src/WpfGfx.Linux/**` 写域）做 B1/B2 微实验 + 落码稿（`RegisterFromFile` 改"**每文件一份 `SKData` + 逐面 `FromData`**"，含缓存生命周期、面号语义逐位不变、位移预测与否证）；**T2** 把修好的 `bounded-run.sh` 落进 `build/DirectWrite.Linux/**`（关键装置不该只躺在 `$HOME`）。
- **窗口 8（进行中）—— 判据换成"段数"后，两条来源同时被确认**：
  - **① Skia 那条：已确认 + 修法已证**（T1d，托管 SkiaSharp **2.88.0.0**、`NotoSansCJK-Bold.ttc` 19.1 MB × 10 面、全部引用存活）：`SKTypeface.FromFile(path,i)` ×10 ⇒ **段数 10 / Σ虚拟 191.2 MB**（**RSS 只涨 13.2 MB**）；**按路径共享一份 `SKData.Create(path)` + 逐面 `FromData(data,i)`** ⇒ **段数 2 / Σ虚拟 38.2 MB** ✓✓ ⇒ **修法把 10 段压到 2 段**。⇒ 已派 **M7b** 落码（`Interop/MilHandleTables.cs:671`、`MilPresentation.cs:493`、`Text/FontSet.cs:86`/`:107` 三处统一；缓存生命周期 + 面号语义逐位不变 + 每 path 的创建/存活计数）。
  - **② PC 侧那条：新发现，可能更大**（T1d 实验 A，宿主补 `SkiaSharp` 引用后跑通）：`Typeface("Noto Sans CJK JP")` → `TryGetGlyphTypeface` **12 次 (style×weight×stretch) 尝试、命中 0 面**（每次抛 `LookupFontFamilyAndFace` 异常链），**却把该 `.ttc` 映射 88 段 / Σ虚拟 1823.4 MB**" ⇒ **该归因已被 T1d 撤回并改写，见下方 ④′**（真正的驱动是"**集合枚举 / 族-面构造**"，与查找次数无关）。
  - **⚠️ 判据更正（本轮转折点，必须记）**：**主判据 = "段数 / Σ虚拟"，RSS 只是旁证** —— `mmap` 之后**未被触碰的页不计入 RSS**（B1 的 RSS 只 13 MB，而虚拟 191 MB）⇒ T1d 早先两条"渲染侧/裸 FreeType 不是主项"的结论**正是被这个判据缺陷带偏**，现已限定为"**裸 FreeType、且用 RSS 判据时成立**"。
  - **③ 归属裁定（T1c 只读诊断 `$HOME/t1c-draft/PC-FAMILY-MAP.md` `9c464628b574620b`）**：**建映射那一行 = `src/WpfGfx.Linux/Interop/MilHandleTables.cs:671` `SKTypeface.FromFile(path, faceIndex)`**（在 `:649 RegisterFromFile(path, faceIndex, simFlags)` 内；表注释 `:600` 自证"**来源键 = `(path, faceIndex, simFlags)` ⇒ 幂等粒度是"面"不是"文件"**"）⇒ 10 面 = 10 次 `FromFile` = **10 份整文件 mmap**；**触发方 = PC 侧族查找/集合枚举**（`build/PresentationCore.Linux/FamilyCollection.Linux.cs` 的 `InvokeFamilyQuery :411` / provider 分支 `:407-473`）。**排除项**：同文件 `:854 File.ReadAllBytes(path)` 是**带缓存的 cmap 解析**（`s_cmapCache` + `MaxCmapFiles` 上界；托管内存、不产生 mmap 段）⇒ 不是来源。⇒ **主修 = M7b**（**幂等键仍是面，但"每面一张 `SKTypeface`"必须从"每路径一份 `SKData`"派生** ⇒ 10 面共享 1 份映射，B2 实测 10 段→2 段）；**T1c 那半（"少探/去重/一次查找内视图缓存 + 有界"）配合但不主修**，**落码判据 = 修后仍测到"一次查找里 `RegisterFromFile` 调用数 ≫ 该族实际面数"**（否则就是"同一缺陷上改两处"，归因变脏）。**追加定向读数（M7b 跑，改前/改后各一遍）**：同一族查找调 **1/2/4** 次 ⇒ `.ttc` **段数 + Σ虚拟**：线性？1 次 ≈ 几段（7–8 ⇒ 每次查找各建；10 ⇒ 10 面全开）。
  - **⑤ 主控复核 M7b 落地（只读，12:3x）**：新件 `src/WpfGfx.Linux/Interop/SkiaFontFileCache.cs`（191 行）—— **每规范路径一份 `SKData`**（`SKData.Create(key)`，`CanonicalKey` 解符号链接）+ **逐面 `SKTypeface.FromData(data, faceIndex)`**；**用 `Lazy(ExecutionAndPublication)` 而非 `GetOrAdd(key, factory)`**（注释写明"后者并发下会多建几份映射，正是本项要消灭的"）；四处调用点已全改（`MilHandleTables.cs:674`、`MilPresentation.cs:495`、`FontSet.cs:88/:110`）；诊断计数齐（`NoteFileDataCreated/Reused`、`NoteTypefaceCreated`、`LiveFaceCount`、`CachedBytes`、`Reset`）。**⚠️ 已点给 M7b 的风险**：`MaxCachedFiles=64` 是**软上界**、`TrimCollectedLocked()` **只淘汰"该文件的面全部已回收"的条目** ⇒ **系统档峰值 = 这一趟碰过的文件数 × 各自大小**（可能几百 MB，逼近 300 MB 判据）⇒ 要它给 **`CachedFileCount`/`CachedBytes`/`LiveFaceCount`（系统档同一趟）**；若 `CachedBytes` 逼近上限 ⇒ 补**按字节预算的淘汰**，但**前提必须先实测**"`FromData` 之后 Dispose 自己的 `SKData`、仍存活的 `SKTypeface` 不悬空"（Skia 的 `MakeFromData` 自持引用），**不许只靠推理**。**小瑕疵**：`MilHandleTables.cs:688` 的错误文本仍写旧 API 名（`SKTypeface.FromFile 返回 null`）⇒ 改成新入口名。  - **④ ❌ 该缺陷候选已撤销（T1d 取到原文后反证，2026-09-15 12:4x）**：异常**不是**族名解析问题，而是**仓外宿主缺件** —— `System.IO.FileNotFoundException: Could not load file or assembly 'System.IO.Packaging, Version=9.0.0.0'`，抛在 `FontFamily.cs:480`（`LookupFontFamilyAndFace`）⇒ **12 次尝试一次都没走到族名比对**；**补齐依赖闭包后**：`Fonts.SystemFontFamilies.Count=165`（**含** `Noto Sans CJK JP`）、`Noto Sans CJK JP` / `Noto Sans Mono CJK JP` / `DejaVu Sans` 各 **12/12 命中、0 异常** ⇒ **不登记**（T1c 那条候选同步撤回；T1d 自纠其 §18.2"拿异常链当旁证却没取原文"）。
  - **④′ 88 段的正确句式（T1d 撤回旧写法）**：**"88 段 = 集合枚举 / 族-面构造路径按 (文件, 面) 建立的整文件映射；与族查找次数无关"** —— 分相读数：起点 0 段 → **仅枚举后 96 段 / 1824.2 MB（ttc 88 段 / 1823.4 MB）** → 12 次族解析后**逐字节相同（查找新增 0 段）**；逐文件闭合 **段数 = 面数、Σ虚拟 = 面数 × 文件字节**（Σ面数 88 ✓）。**并且归因边界已写明**：expA 只加载 PC/WB/DWF/Provider、**不加载 `src/WpfGfx.Linux`** ⇒ 这 88 段的创建方**只能在 Provider/PC 侧**，`MilHandleTables.cs:671`（MIL 侧）**不是同一处** ⇒ **两侧必须分别计数**（与 T2 的 `--mode=nofb` 差分不冲突：Provider 侧已按文件共享，残余只在 shim 回退扫描那条路）。
  - **④″ T1d 独立复现了 T2 的 provider 修法**：1CJK 档 **段数 10 → 1**、Σ虚拟 **185.9 → 18.6 MB**；系统档 **24 文件 / 24 段（每文件恰好 1 段，无遗漏）**、命中 12/12（三个族）⇒ 这是**跨车道独立验证**（件 `Provider 9aa0d744802aaa31`）。
  - **④‴ 窗口 10（进行中，shim 侧最后一处）**：T2 的 `--mode=nofb`（关掉 shim 回退扫描）差分把残余指认出来 —— **系统档 26 段（nofb）/ 46 段（null）**、`Bold` **1 / 11 段**、1CJK 该 ttc **2 / 3 段** ⇒ **残余的"按面整文件映射"在 `HbFaceCache` 里**（`Load(path,faceIndex)` 每面各建一份整文件 blob，`finally` 只销毁自己那份引用、**face 持有引用**，而 `s_faces` 长期缓存 ≤512 项）。**修法 = 按 `path` 共享一份 blob**（`hb_face_create(sharedBlob, faceIndex)` 用同一 blob 的子面）；判据 = 系统档 `null` **46→≈26 段**、1CJK **3→2 段**、峰值不升；**同波**修 `liveBlobs` 负值（`hb_face_reference_table` 的表子 blob 也走了 `DestroyBlob` ⇒ 只对我们建的计数 + 单列 `ForeignBlobDestroys`，`peak` 语义不变）；并加"**MIL 侧 / shim 侧分别计数**"（承 ④′ 的归因边界）。
  - **⑥ M7b 的微实验（判据 = 段数/Σ虚拟，逐字；`~/m7b-w8/logs/`）**：**B1** `SKTypeface.FromFile(path,i)`×10 ⇒ **10 段 / ΣSize 185.9 MB / ΣRss 9.6 MB**；**B2** `SKTypeface.FromStream`（逐面） ⇒ **0 段**；**B2B** `SKFontManager.CreateTypeface(path,i)` ⇒ **10 段 / 185.9 MB**；**B3（选定修法）"每路径一份 `SKData` + 逐面 `FromData`"×10 ⇒ 1 段 / ΣSize 18.6 MB / ΣRss 2.9 MB** ⇒ **10× 压缩** ✓✓。**更关键**：**B1/B2/B2B/B3 四者对 `U+0041/U+4E00/U+3042/U+30A2/U+1F600` 的 glyph id 逐位相同** ⇒ **"面身份不变"有实测证据**（不是推理）。
  - **⑦ 复取的"测谁就报谁"硬要求（已写进 T2 派单）**：判据 runner 走"PC → provider → MIL 侧 `MilFontFace_RegisterFromFile`"这条链，而改动在 `src/WpfGfx.Linux/**` ⇒ **必须先 `dotnet build src/WpfGfx.Linux` 并核对 runner app 目录里那份 `WpfGfx.Linux.dll` 的 sha16 与 `bin/Debug` 逐位相同**（不同则先同步并记录前后 sha），并连同 `PresentationCore`/`WindowsBase`/`DWF`/`Provider` 一起核（纪律 23 的原事故就在这一族）；报告须写明"本次用的是哪一份 `WpfGfx.Linux.dll`"。**小瑕疵**：`MilHandleTables.cs:688` 的错误文本仍写旧 API 名（`SKTypeface.FromFile 返回 null`）⇒ 已请 M7b 改成新入口名。
- **窗口 9（进行中）—— 归属再次更正：判据 runner 的链**不含 MIL**，真凶是 **provider**。**
  - **实测装载面**（T2 峰值件 `w7-sys/maps-at-peak-1062655.txt` 逐段读）：**`WpfGfx.Linux.dll` 0 段**（MIL 托管渲染侧**没进那个进程**）｜`DirectWrite.Linux.Provider.dll` 1｜`libSkiaSharp.so` 8｜`wpfgfx_cor3.so` 7｜`PresentationCore.dll` 1｜`WindowsBase.dll` 1｜`libharfbuzz` 6｜`libwpfwin32` 5。
  - ⇒ **M7b 在 `src/WpfGfx.Linux/**` 的修法（B3 已证 10 段→1 段）在那个 runner 上"测不到"** ⇒ T2 **按纪律 23 如实报"我判不了"**，没有把"系统档没降"记成"修法无效" ✓。⇒ **判它必须换宿主**：自然加载 MIL 的其一（`Presentation.Tests`/`ManagedLayer.Tests`/`HelloMil.Tests`/应用级门禁），**判据 = 该进程里 `WpfGfx.Linux.dll` 段数 ≥1 且 M7b 新加的 `NoteFileDataCreated/Reused` 非零**；**不许**用不加载 MIL 的 runner 当"改后"证据（也**不给 T2 的 runner 加 `--force-mil`** —— 那会让仪器性质一变、历史读数不可对拍）。
  - ⇒ **该链上的建映射点 = provider**：`build/DirectWrite.Linux/Provider/LinuxFontCollection.cs:263` 与 `LinuxFontFace.cs:135` 的 `SKTypeface.FromFile(path, index)`（`:122/:123`、`:165/:166` 已用 `SKData.CreateCopy` + `FromData`）⇒ **已派 T2** 按"每路径一份 `SKData` + 逐面 `FromData`"修（缓存有界、**面号语义逐位对拍**、判据 = 1CJK 该 ttc **13→1–2 段**、系统档**总字体段数 117→≈文件数**）。
  - **PC 侧 `FileMapping` 再次确认是死路**（原生 shim 三入口是会失败的桩 ⇒ 移植版 `OpenFile` 必抛）⇒ **"PC 被加载（1 段）"只说明程序集在，不说明那条路被走到**。
  - **窗口 8 读数（T2，改前/改后同命令）**：1CJK 档 峰值 **400 MB**、`shared_clean` 201 MB、该 ttc **13 段 / Σ虚拟 241.6 MB**；系统档 峰值 **2.03 GB**、`shared_clean` 1.67 GB、**字体段数 117**（`NotoSansCJK-Bold` **20 段 / 382.5 MB**、`Regular` 11/204.4、`Black` 10/187.2…；25 个文件、字体 Σ虚拟 2215 MB）⇒ **与修前同量级**（因为修法不在该链上，**不是修法无效**）。
  - **仪器缺陷登记（T1d 修）**：`liveBlobs` **运行值为负**（`-746`）—— 销毁端把 HarfBuzz 自建的**表子 blob**也算进去 ⇒ **运行值无效、`peak=1` 仍有效**（"不经过 HarfBuzz 的 blob 门"这条结论依据是**峰值**）⇒ 修法 = 只对我们建的 blob 计数 + 单列 `ForeignBlobDestroys`。
- **⑧ 判据重定（2026-09-15 12:4x，主控；理由是"追不到的地板"）**：原写"系统档权威峰值 ≤300 MB"**不可达且不该作为发波条件** —— 因为 provider 的 `FromDirectories(...)` 会把字体目录**全量预载**、**每个面各建一个 `SKTypeface` 并长期持有** ⇒ **地板 = 被加载文件数 × 各自大小（`shared_clean`）+ 堆（`priv_dirty` 341 MB + `anon` 336 MB，T2 窗口 8 的 SNAP）**。⇒ **拆成两条**：
  - **`D-F1c`①（本波目标，可达）**：**①a** 1CJK 档权威峰值 **≤300 MB**；**①b** **消除 ×faces 乘法**（系统档段数 **117 → ≈被加载文件数**，报告须同时给"被加载文件数"）；**①c** 1CJK 档该 `.ttc` 段数 **13 → 1–2**。
  - **`D-F3`（另立在册，不在本波）**：**字体集合全量预载**（架构地板）⇒ 修法 = **惰性按面建 typeface**；判据 = **系统档峰值由"用到的面"而非"目录里的面"决定**；**本波只要求交地板读数 + 致因**。
  ⇒ **发波条件相应改为**：`D-F1c`① 三条全绿 + `D-F3` 已登记并有地板读数（**不要求系统档 ≤300 MB**）。
- **长趟守卫裁定（T2 四点理由，主控采纳）**：**不抬** `RSS_LIMIT_MB` —— ㈠ 1.67 GB 是与用例数无关的**固定底噪**（抬到 2600 MB 被它吃掉 2/3）；㈡ harness 还要持 614 例语料 ⇒ 抬了也很可能只是**晚一点**被止损；㈢ 本机 **swap 仅余 ~1.2 GB**（`803/2047` 已用）⇒ 越界即"零输出假死"、**证据质量更差**；㈣ 更优 = 先修映射 ⇒ 守卫不用抬。⇒ 要长趟证据时跑**有界切片并标注"切片，不是全趟"**，**不许用抬守卫把"被止损"洗成"跑完"**。
- **窗口 4 的落码（纯计数，`+8/−0`）**：（`ResidentCount => s_faces.Count`、`ReleaseCalls`、`SegmentFaceResolveCalls`；都在 `#if` 外）；② T2 用 **0.2 s** 采样 `smaps_rollup` 四列（3 s 的趟若按 3 s 采样会漏掉峰值与增长段）；③ 备好"**扫描期不建 font**"的修法稿（`LoadMetaOnly`：只建 face、读族名/OS-2/`maxp`、**立即销毁**，绝不建 font、绝不写 `s_faces`）—— **若 smaps 判为 `Shared_File` 占大头，则改法完全不同**（查 blob/face 销毁链）⇒ **先读数后动手**。
- **仪器变更记账（T2 主动声明，主控批准）**：`TOOTH-C1-NEG`（"今天这版必须判红"）在 `D-F1b` 修好后**必然变假** ⇒ 换成 **`TOOTH-D-F1b-ABSENT`**（现场断言历史形态不再出现）+ **`TOOTH-C1-REDCAP`**（"判据能红"由判据层合成用例每趟保证）；判据 sha `524936544a6b9033 → fc808896f23390f4`。⇒ **纪律 26 的正确执行**（判据绑在"当时的版本"上就会过期）。
- **装置守卫的现场结论（T3 提出、实测确认）**：修后**合法峰值 2.07 GB > `RSS_LIMIT_MB=2048`** ⇒ 长趟会被守卫在"扫描那一下"止损 ⇒ **不抬守卫、不放长趟**，先把内存压到判据内。"**守卫上限必须高于合法峰值**"由此从原则变成**有数字的现场结论**。
- **窗口 3 的修法（一行重排，已落，件 `92fc7605480fb289`）**：探测循环里 **`HbShaper.CoversFace(...)`（`:1053`，返回值被丢弃）在 B2 位图过滤之前被逐候选调用**，而它内部走 `HbFaceCache.Covers` ⇒ **`Get` ⇒ 建 Entry 并写进 `s_faces`**（只在 ≥ `MaxFaces=512` 时淘汰；本机 371 面 ⇒ **永不淘汰**）⇒ **每个过了主键筛选的候选面都被物化并常驻**（60~371 面 × ~9 MB ≈ **3.3 GB**）⇒ 这正是 T2 量到的峰值；**T1 段稳态 17.6–20.6 MB** 是因为那 73 例 CJK 用覆盖字体（**根本不进回退/探测路径**）。**修法 = 一行重排**：位图过滤**前置**，再调 `CoversFace`（记账/预热保留）⇒ B2 的"驻留面 ≈1"才成立。**必须声明**：`coverageProbe`/`coverageCacheHit` **两个自造计数会下降**（仪器口径变化）；`candidates`/`Scans`/选面/行宽/advance/CR **一律不许变**；面选择普查须仍 **26/26 逐格相同**。
- **新登记 `D-F2`**：`new GlyphTypeface(Uri)` 对**我们自己 pin 的纯 TTF** 也抛 `FileFormatException`（`FaceFromRef` **108 次 FAIL** 的去重原文）⇒ 移植版这条公开入口对**文件字体实质不可用**（DWrite"从文件取集合"未接）；仓内另有调用点（`CoverageProbe` ×3、`ContractProbe`、`FontEntryClosedLoop`、真机 oracle 宿主）。⇒ `D-F1b` 的修法因此定为 **E1 = `Typeface` → `TryGetGlyphTypeface`**（**复用应用路径 `GetResolvedFace`（`:3869`）那条，不另造第二条路**）。**本轮不修 `D-F2`**，只登记 + 判据候选（对 `build/fonts` 的 TTF 构造成功且 `FontUri` 一致）。
  **⇒ 两条教训**：㈠**"第一次调用就不返回"这类症状，先读被调方**（读 20 行定案，胜过 6 腿 × 150 s 的二分 + 栈采样）；㈡`hb_set_next` 的游标是 **in/out 参数**，不是"返回下一个"，极易写错。
  **窗口 2**（`~/wfp-runs/window-F1c-round2-pre.txt` `263eed12512953ea`，现件 `16db2d6194edc1f5`）已开：只改这一行（锚点整行 `grep -cF` = 1；删 ` c = 0xFFFFFFFFu;`）⇒ 三连编译 0 错 ⇒ 报新 sha ⇒ T2/T3 复取（判据：4 面目录回到"秒级完成"、系统 371 面完成且峰值仍 ~63 MB、有界 `tline` 能跑过 `T1 · 73 例 CJK`、四个计数现在可读）。
- **㈠′ 原判据（保留作口径）**：㈠ **正极性**：修后同一趟 `run.sh tline` **能在有界资源下跑完**（并给出 `TLINE_GATE` 行）；㈡ **负极性**：把修复关掉/回退 ⇒ 必须**重新出现**"RSS 单调上升 + 停点可复现"（**能复现的失败才算牙**）；㈢ `D-F1` 的正极性读数**不得回退**（`与` 行宽仍 `16.0000`、`gids=[9498]`）；㈣ **应用路径不得中招**（门禁 6/6 重跑仍 `PASS`）。
- **位移预测**：修的是"上界/去重/提前短路" ⇒ **不得改变任何既有数值读数**（行宽/advance/CR/`Extent` 逐位不变）；**若数值动了 ⇒ 说明"回退是否发生"被改动 ⇒ 停、报我**。
- **归因**：`D-F1c` 与 `D-F1b` 同波但**各自取一次读数**（`D-F1c` = "跑得完 + 资源有界"；`D-F1b` = `FACE_URI`/`ADV_FROM_TYPEFACE`）。

## 5. 残留（本波不解、登记在册）

`D-B1`（混合方向 bidi，用户裁定不开专项）｜`D-K1` 四项｜`D-U1`（已裁）｜`F_lat_words_w560`（ΔW `+0.0140`）｜`tab-rtl` 76/84 不可比｜`Tabs` 显式停靠位（无公开 API）｜`.ttc` face 0 路径含 `#` 的解析边界｜`ManagedLayer.Tests` 负载敏感性（`D-R2`）｜app-local 校验器"脚本拷贝的原生件"那半（`invisible_copysites=20|invisible_write=3|invisible_read=17`）｜债 #1/#26、sample `HintPath→ProjectReference`。
