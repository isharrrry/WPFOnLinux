# 波 `#25` 预登记（**落地前先登记；红了只许加严，不许放松**）

> 生成：主控，2026-09-17 11:3x，基于**当前冻结基线 `#24`**（权威 = `samples/WpfTextDemo/ACCEPTANCE-BASELINE.md`；⚠️ 按纪律 53 **本文件不重述整份 sha 的值** —— 唯一声明处是 `docs/CURRENT-STATE.md` 的 `BASELINE-FROZEN` 机器行，由 `verify-all` 第 `[7]` 步 `BASELINE-SHA` 现场核对）。
> 承接：`docs/WAVE24-PREREGISTRATION.md` §14.7 的候选表（① `D-T5-R`｜② `D-A2-r` 方案 B｜③ `D-T7`｜④ `hasOverflowed` 更大判别面｜⑤ 三步自报口径收紧）。
> **本波的资源方针（用户要求：控内存 + 最大并行）**：见 §7。

## §1 内容（四条落地 + 一条只读审计）

| 车道 | 件 | 是否动产品件 | `dotnet` |
|---|---|---|---|
| **W25A** | **`D-T5-R` 修法**（"全隐形段落"`hiddenonly` 仍 abort）：应用器 props 兜底 + 透传段落属性 | ✅ 动（**只在应用器**） | **是（本波唯一构建者）** |
| **W25B** | **`D-A2-r` 方案 B**：`check-applocal-sync.sh` 新增**只读**桥锚判据（`BRIDGE_SO_SHA256`）+ `--selftest` 用例 | ❌ 不动（判据） | **零** |
| **W25C** | **登记结算**：① 3 条**结构族帧红**建**书面登记**（`FrameProbe` 无在册红表）；② `known-red-PC-copies.md` 的 13 份**按世代结算** | ❌ 不动 | **零** |
| **W25D** | **三步自报口径**（只读侦察）：`pc-line-step`/`frame-step`/`baseline-sha-check` 各吐一条 `KEY=PASS` 汇总行，让 `run_step` 的失败诊断 grep 直接命中 | ❌ 不动（**只出方案**） | **零** |
| **W25E** | **可复算性审计**（只读）：抽 `#24` 冻结块里的结论逐条**现场重算**，报出"哪些今天复算不出来、缺什么" | ❌ 不动 | **零** |

`D-T7`（零宽占位码点）**本波不做** —— 它的真值**只能靠真机重录**，仓内无依据（`#24` 已定）。

## §2 W25A —— `D-T5-R` 修法（**本波唯一有位移的一件**）

**已定事实（`#24` 只读侦察，报告 `~/w25-recon/d-t5r-plan.md cb70e58b4e479ace`）**：
- 真身**不在 shim**（`grep -c CollectLenient build/shims/PresentationCore.HbTextLine.cs` = **0**）而在**生成物** `build/PresentationCore.Linux/TextFormatterImp.Linux.cs:243-248`；源头 = 应用器 `src/WpfGfx.Linux.Native/tools/patch-presentationcore-textline-fallback.py` 的 `REPLACEMENT_3`（失败点 `:403-404`）。
- 根因：全隐形段落里**每个 run 的 `Properties` 恒 `null`**（`TextHidden.Properties => null`（`sealed`）＋ `TextEndOfParagraph(1)` → `TextEndOfLine(length,null)`）⇒ 遍历完整段**连一次非 null 的机会都没有**。
- 兜底候选：`paragraphProperties.DefaultTextRunProperties`（非空且 `Typeface` 非空**已被机器强制**；调用点在同函数作用域内；仓内先例 `TextBlock.Linux.cs:2856`）。

**判据（落地后必须给出的读数）**：
- 目标用例 `hiddenonly`：**修前** `RED-EXC-LS` / `--nocatch` ⇒ `rc=134`；**修后** 四格（strict/lenient × catch/nocatch）`rc=0` 且 **`A1/A2/A3` 全 PASS**（`A3` = `Σ(Length−NewlineLength)==CpLength` 守恒）。⚠️ **必须带 `--collapsible`**，否则落 `SimpleTextLine` 快路径被当 Ghost 处理、**读成 GREEN**（量不到本缺陷）。
- **阳性对照不许动**：`control`/`mod0` **逐字节未变**；已转绿的 `eos1`/`mod1`/`hidden1`/`hiddenmid` **保持 GREEN**（四格全同）。
- **两极化（三级，缺一不可）**：① **牙级假修** —— 只改代码不改 needle ⇒ 应用器 `rc=1` 且**生成物不写盘**；② **返空串假修** ⇒ `A3` 必红（`Σ=1 ≠ 3`）；③ **半接线假修**（只加形参、调用点不传）⇒ **必须仍红**，并**补计数牙齿** `n_default_src == 2`（按应用器 `:764-773` 既有计数风格）。**只证"修后绿"不算证完。**
- **回退证**：`cp -p` 全份备份应用器 ⇒ 改 ⇒ `pc` 变 ⇒ 复原 ⇒ `pc` **逐字节回到 `476994e35d31a7e1`**（`cmp` + sha 双证）。
- ⚠️ **判别力边界（必须写进报告）**：`A1/A2/A3` 对"占位字符是否**真零宽**"**零判别力**（探针**从不读 `line.Width` 或任何几何**）；冻结语料**不含全隐形段落** ⇒ **"能否转绿"可测、"转绿是否对"本仓不可测**（要真机重录，即 `D-T7`）。**不许**把本车道的绿读成"隐形语义已正确"。

**位移预测（表外位移 ⇒ 停）**：

| 位 | 预期 | 依据 |
|---|---|---|
| `hbtextline`（shim） | **一个字节不动** | 修法 100% 在应用器；`tline-gate.sh:234 GEN_KEYS` **不含 `pc`** |
| 五臂日志 | **不重取** | 世代绑定三项（`run.sh`/`Parity.cs`/shim）全未变 |
| `known-red.json` | **不重钉**（`generation` **仍 `#23`** —— ⚠️ 主控先前把这一格误写成 `#24`、已改；**不许**因为"基线叫 `#25`"就去改它） | 它绑的是**世代三项**，不是基线世代 |
| `pc` | **必变** | 生成物变 ⇒ PC 重建 |
| `inputs_fp` | **必变** | 应用器在 G1 组 |
| 生成物 `TextFormatterImp.Linux.cs` | **必变** | 唯一生成者是该应用器（侦察已证） |
| 6 条 `BASELINE … config=pc:` | **必变**（只有 `pc` 那一格） | 应用门禁重取 |
| `bridge`/`pf`/`wb`/`provider`/`win32shim`/`wic_shim`/`dwf` | **不动**（`pf` 仅由波尾 `close-wave.sh` 重编引起） | `#24` 同形 |
| `APPSYNC` | **重取后红数变化**（`#24` 实测 `13 → 18`） | 纪律 52：只在某个 `pc` sha 下有意义 |
| `verify-all` 第 `[5]`/`[6]`/`[7]` 步 | **必须重跑**（都读 `pc` 或基线） | — |

⚠️ **登记表 `generation` 的口径**：它现在**就是 `#23`**（不是 `#24`），因为世代绑定三项自 `#23` 起没动。本波同样**不动那三项** ⇒ 登记表**必须保持 `generation=#23` 一字不改**（改了会 `registry-generation-inconsistent` ⇒ `NOINFO`）。

**「`pc` 变了不用重取五臂」这一条有直接实测依据（主控现场核）**：
- `tline-gate.sh:234` 的 `GEN_KEYS = ("instr_run_sh","instr_program_cs","instr_shim")` —— **`instr_pc` 不在其中**（只在 `generation` 里当注释用）。
- 而且 `generation.instr_pc` **今天就已经是两代前的值** `7b47a7b3d69ad62f`（`#23` 的 `pc`），而现场 `pc` 已是 `476994e35d31a7e1`（`#24` 重建过）—— **门禁照样报 `caliber=OK tree_gen=same drift=0 gone=0`** ⇒ **"`pc` 前进"这件事既不进世代一致、也不让臂失鲜**。
⇒ 但这只是**预测**：W25A 落地后**必须实测** `TLINE_GATE=` 那一行；若它变成 `NOINFO`/`drift≠0` ⇒ **停**（停条件 2），并按纪律 34 评估是否真要重取。

## §3 W25B —— `D-A2-r` 方案 B（**零位移**）

- 锚 = `build/MilBridge/.artifacts/publish/MilBridge.Linux/release_linux-x64/bridge-src-fp.txt:3` 的 `BRIDGE_SO_SHA256`（写点 `build/publish-milbridge.sh:81`）。
- **必须用方案 B**：独立**只读**检查，**不进 `ITEMS`** —— 因为给 `ITEMS` 配非空权威会打 `STALE` ⇒ `sync-applocal-authority.sh:119-122 → do_refresh:103` 会 **`cp -f` 用 376 B 的 `.txt` 覆盖 4,987,840 B 的 `.so`**（发布目录那份是 `cp X X` 自覆盖 ⇒ 可能截断成 0 字节）。**车道的任何红证都不许跑 `--apply`。**
- 判据口径（主控已裁）：**只看 sha，不看 mtime/权限**；记录缺失 ⇒ **`NOINFO`（不许当绿）**；"记录过期"与"件被换"**不需要可分判据** ⇒ 两种口径都判红。
- **反极性**：把两份副本换成旧桥（`~/wfp-runs/w18-pre/wpfgfx_cor3.caf7baf9e67719aa.so`，现算 sha16 `caf7baf9e67719aa`）⇒ **必须红**；`cp -p` 还原后**逐字节回到绿**（sha + `cmp` 双证）。**动真件前 `cp -p` 全份备份并记 sha**（纪律 16）。
- **今天必须 0 条新红**（现盘两份副本 sha16 都是 `d567c26f197ec1e3` == 记录）。
- `--selftest` 新增用例（现 20 例 A…O ⇒ 新例 **P**，插在 `:707` 与 `:709` 之间），并跑一次**全量** `--selftest` 证明没打坏旧例。

## §4 W25C —— 登记结算（**零位移**）

① **3 条结构族帧红**（`行数我方=4 真值=2`，`FrameProbe` 点名）**今天无处可登记** —— `FrameProbe` 不读任何在册红表。⇒ 建**书面登记**（仿 `build/DirectWrite.Linux/wic-shim/known-red-PC-copies.md` 的形态：**只读、自述"登记≠已容忍、本段不参与判定"**）。**判据**：登记条目数 == `FrameProbe` 现场点名的条数，且**摘掉登记文件不会让任何判据变绿**（证明它确实只读）。
② `known-red-PC-copies.md` 的 **13 份按世代结算**：写清"哪一份归哪个 `pc` 世代、`[在册红·已转绿]` 只在哪个 `pc` sha 下成立"（纪律 52）。**不改 `rc`**、**不改 `check-applocal-sync.sh`**（那是 W25B 的写域 —— **两条车道不许碰同一个文件**）。

## §5 W25D —— 自报口径（**只读侦察，只出方案，不动文件**）

现状：`verify-all.sh` 的 `run_step` **只在 `rc≠0` 时**才打印日志细节，且诊断 grep 是"事后补的宽 grep"。⇒ 让三步各自吐**一条汇总行**（形如 `PCLINE_START_STEP=PASS …`、`FRAME_STEP=PASS …`、`BASELINESHA=PASS …`）的**行级方案**：
- 现有输出到底长什么样（逐字段抄原文）；
- 新增那一行插在**哪一行之后**（给行号与原文）；
- `verify-all.sh` 的失败 grep 需要怎么改（**只许加、不许删**）；
- ⚠️ **反极性**：必须证"故意让该步红 ⇒ 新汇总行确实变 `FAIL` 且 `run_step` 抓到"；**并说明它今天抓不到什么**。
**只读**：报告写 `$HOME/w25-recon/selfreport-plan.md`，**不许改仓内任何件**。

## §6 W25E —— 可复算性审计（**只读**）

用户的核心要求是"**每个结论都要可复算**"。⇒ 从 `docs/CURRENT-STATE.md` 的 `#24` 冻结块 + `handoff.md` 的 `#24` 记录里**取 8–10 条具体结论**（例：`TLINE_GATE=PASS …`、`SHIM_SHA=no`、`6/6 BASELINE … result=PASS`、`871 通过 2 跳过`、`inputs_fp=…`、`BRIDGE_SRC_FP=…`、九位各 sha、`drawn=260/144`），逐条现场重算并**分类**：
- **A 类**：**零 `dotnet` 就能复算**（给出一条可粘的命令 + 实测输出）；
- **B 类**：**必须跑某仪器才能复算**（点名仪器与命令，**不要真跑**）；
- **C 类**：**今天复算不出来**（说清**缺什么**：缺真机、缺留档件、缺仪器、无记录…）。
**只读**，报告写 `$HOME/w25-recon/recompute-audit.md`。**C 类就是下一波要补的洞**，请按"补它的代价"排序。

## §7 资源与内存纪律（**用户要求：控内存 + 最大并行**）

- **同时最多 2 个 `dotnet` 进程**；**构建者只有 W25A 一个**，且它在构建时**独占**（其余车道此刻只做 `grep`/读文件）。W25B/C/D/E **零 `dotnet`**（硬约束）。
- 构建旗标：`-m:1` + `DOTNET_gcServer=0`；**起步前 `MemAvailable` 必须 > 1200 MB**，否则等。
- 任何车道**不许** `pkill`（会自匹配），不许用 `pgrep -f` 单独下结论（本会话已两次自匹配）。
- 主控同时跑内存采样（`$HOME/w25-mem.log`，30 s 一次），波尾给出**信封**（最低 `MemAvailable` / `SwapFree` 最低 / `load1` 峰值）。

## §8 停条件（**触发即停、如实上报**）

1. `hbtextline` **变了**（说明有人动了 shim）⇒ 停（本波不该付世代成本）。
2. 五臂需要**重取**或登记表需要**重钉**（与 §2 预测不符）⇒ 停、复核。
3. 阳性对照 `control`/`mod0` 或已转绿的 `hidden1`/`hiddenmid` **读数变了** ⇒ 停（修法越界）。
4. `#24` 已冻基线里**除 §2 位移表所列**之外还有位动了 ⇒ 停。
5. W25B 的判据今天**报了新红**（现盘应与记录一致）⇒ 停、先查是不是把真件碰了。
6. 任何车道**改了不属于自己写域的文件** ⇒ 停、回退、登记。

## §9 收官清单（顺序不可颠倒 —— 纪律 46；收官时再加勾）

- [x] **W25A**：应用器改 + 生成物重生成 + `pc` 重建 + 探针四格 + 三级反极性 + 回退证 ⇒ **四格全绿、三级反极性全拿到、往返闭合**；报告 `build/MilBridge/W25A-report.md 2bb495e622b9ec19`
- [x] **W25B**：`check-applocal-sync.sh` 只读锚判据 + `--selftest` **18/18** + 反极性红证 + 还原逐字节证 ⇒ 报告 `fb7469b4bc107312`；**今天 0 条新红**
- [x] **W25C**：3 条结构族帧红的书面登记（`known-red-frame-structural.md`，只读性有沙箱实证）+ 13 份 PC 副本按世代结算（**只增不改**）⇒ 报告 `0dac420628c926c8`
- [x] **W25D/W25E/W25F/W25G/W25H/W25I/W25J/W25K**：八份只读交付（方案 / 审计 / 配方 / 草稿），**仓内零改动**；其中 **W25I 的补丁被主控实测可 `patch`（产物与其参考件逐字节相同）**
- [x] `close-wave.sh`（**第一个跑**）⇒ 九位/`inputs_fp` 复核 ⇒ 五臂实测 ⇒ `verify-all`（**13 步**）⇒ 应用门禁 ×2 ⇒ **重冻 `#25`**（机器行由脚本重算）⇒ 文档
- [x] 波尾硬性动作 §9.1：① 核"桥 `exp` 仍为空"② 收敛落后副本 ③ `APPSYNC` 定型后读 ⇒ **实测：① 仍空 ✓ ② `sync` 干跑 `refreshed=0` ⇒ 不需要 `--apply`（更正 W25C 的"31 份"）③ `APPSYNC=MISMATCH`、`BRIDGE-ANCHOR=0`、零新增红**
- [x] §9.1b：`--selftest` **静树重跑 = PASS/rc=0**（并发期那次不作波尾读数）
- [x] §9.2：三支 tab 臂日志现场 sha 已**写进手边记录**（`D-G9` 的机器可核落点由 `#26` 落，本波先登记）
- [x] 纪律 54：重冻后再跑一趟 `verify-all`，使第 `[7]` 步的 PASS 落在**最终冻结版**上

### §9.1 波尾**新增**的三条硬性动作（`#25` 实测得出，缺一不可）
1. **核"桥 `exp` 仍为空"再跑任何 `sync`**：`sync-applocal-authority.sh:103` 的 `cp -f "$asrc" "$dst"` 对**非空 `exp` 的 `ITEMS` 条目**是**破坏性**的（`D-A2-r` 侦察实测：会用 376 B 的 `.txt` 覆盖 4,987,840 B 的 `.so`）。⇒ 跑之前必须确认 `check-applocal-sync.sh` 里 `wpfgfx_cor3.so` 那一格**仍是空权威**（`W25B` 已按方案 B 落地：`:148` 明写"**权威不进本表**（本格留空）"、`:274` 是"独立只读、不进 `ITEMS`"的锚判据）。**主控已在 12:4x 核过 ✓。**
2. **收敛落后副本要刷的是 31 份，不是 12 份**：`W25A` 在 **12:30:42** 重建 `pc`（`476994e35d31a7e1 → 7374308a00c55572`）后，**上一代那 12 份"已转绿"在同一分钟内全部回落为"仍红"**（`W25C` 12:32 实测：已转绿 0 / 仍红 13）。**主控独立枚举**：全仓 34 份 `PresentationCore.dll` 中 **3 份 = 新权威、31 份落后** ⇒ 波尾要 `sync-applocal-authority.sh --apply` 刷的是 **31** 份。〔⏪ **`#25` 波尾实测更正（口径错，已撤回）**：那 31 份是**按 `find` 全枚举**数出来的，而**校验器只认它自己声明的 app-local 加载源**那一族 ⇒ **权威口径是 0**：`sync-applocal-authority.sh` 干跑给 `refreshed=0 newer=0 applied=0`，并明说"**同类加载源副本都已是权威 sha**" ⇒ **本波不需要 `--apply`**；且本波 `APPSYNC` 相对 `#24` **反而收敛**（`MISMATCH 18→1`、`DIVERGENT 3→1`）。**教训：报"要收敛 N 份"必须用校验器的口径，不能用 `find` 数文件数。**〕
   ⚠️ **`close-wave.sh` 自己不会刷新**（主控实测 `grep -n sync-applocal build/close-wave.sh` = **0**；它只在 `:159-160` 跑 `check-applocal-sync.sh` 并按结果打 `✅/⚠️`）。
3. **`APPSYNC` 只能在"`pc` 定型 + 副本收敛"之后再读**（纪律 52 + 本波实测的世代切换现场）：否则读到的是一个**混合世代**的红数，既不是上一代的、也不是这一代的。

### §9.1b `--selftest` **必须在静树上跑**（`#25` 实测出的新纪律）
> ⏪ **`#26` 修订（静树是必要但****不充分**条件）**：`#26` 车道 W26C 报出**第二个假红源** —— 自称 `printf '%s' "$outM" | grep -q PAT` 在 `set -o pipefail` 下会因 `grep -q` **提前退出**、写端吃 **SIGPIPE** ⇒ **"串在却判成不在"**，它实测 M 段三条 ≈**6.6%/次**并**当场撞到 1 次**。⚠️ **主控复现失败**：按同形与更强形态共试 **2500 次**（200 KB 位首 ×200、5 MB 位首 ×2000、5 MB **位末** ×300，并直接统计管道 `rc`）⇒ **假判 0 次、`rc` 恒 0** ⇒ **标"自称、待复现"，未拿到复现配方或留档 transcript 之前不许据此改判据**。
> ⇒ **纪律改为**：① 静树**必要**；② 凡 `--selftest` 报 `FAIL`，先分清"**判据失败**"与"**取数环境/机制造成的假红**"；③ **把两时刻的值逐字打印**（W26C 已让 M2 判词行打印 `M 时刻 capped=… ｜ M2 时刻 capped=…` —— **此举应推广**）。
> ✅ **同趟接受的一处（主控裁定不算越界）**：W26C 把 M 段 3 条 + 新增 M2 段 4 条的 `printf|grep -q` 改成 `<<<`（**所需字符串一字未改**），并用"真坏③ ⇒ `SELFTEST_M=FAIL` rc=1"证明**牙仍在**；`--selftest` **18/18 PASS / rc=0**（主控独立复跑 ✓）；**判定面位移 = 0**（静树整份输出改前/改后**逐字节相同** `33e759c22654fb6d`，主控复核 ✓）。
> ✅ **并确证这一趟是"加严"**：以前只查告诫**字样在不在**、**不查数字**；现在 N 必须逐位等于枚举器自报值、自报 0 时不许出现对应告诫 ⇒ **实测"改前 18/18 PASS、改后 FAIL"**（`57c76545b3311337` vs `ac91c13136cdde49`）⇒ **它在旧自测里抓出一个真缺陷**。
⚠️ **站点计数口径待澄清**：W26C 说"其余同形真站点 **14 处**"；**主控数到 `printf … | grep -q` 形态共 38 行** ⇒ **两个数不是同一口径**。⇒ `#27` 若要机械替换，**必须先出"逐点清单 + 复现配方 + 每点假红率"**。
`check-applocal-sync.sh --selftest` 的 `SELFTEST_M2` 有**既有竞态**（新登记 `D-G13`）：它拿 M 时刻一次性取下的输出，去满足一个由 M2 时刻新算出来的要求 ⇒ **并发改仓时会假红**。⇒ 波尾跑它之前必须确认：**所有车道已收工、没有 `dotnet` 在跑**；并且**把两次读数（并发期那次 / 静树那次）都记进报告**。主控在 7 条车道并发时跑过一次 `--selftest` = `PASS`（`rc=0`，18 例）—— **属侥幸，不作为波尾读数**。

### §9.2 波尾必须复算的三条（`W25E` 的审计口径：**"数值可复算"≠"引用的物可考"**）
- 九位 + `inputs_fp` + `BRIDGE_SRC_FP`（脚本算，纪律 49）；
- 基线整份 sha + 机器行（`baseline-sha-check.sh`，**并在改完基线后再跑一趟 `verify-all`** 让第 `[7]` 步落在最终版上 —— 纪律 54）；
- **三支 tab 臂日志的现场 sha 要写进某个"机器可核"的地方**（`D-G9`：今天只在 `generation.leg_resolution.cross_check` 的**散文串**里，且门禁**不读**它）。

## §10 收官（主控：`#25` 波收口）

### §10.1 顺序（纪律 46）逐条兑现
`close-wave.sh --skip-verify-all`（13:02:52→13:05:28，rc=0，`OUT=~/wfp-runs/close-wave-130252`）→ 九位/`inputs_fp` 复核 → 五臂实测 → `verify-all`（13:05:38→13:18:04，**rc=0/13 步/871 通过 2 跳过**）→ 应用门禁 ×2（13:18:09→13:24:06，**两趟都 `RUN_RC=0`**）→ 收敛副本**不需要**（权威口径 0）→ 重冻 `#25` → 文档 → **再跑一趟 `verify-all`**（纪律 54）。逐条读数见勾选清单、`handoff.md` 的 `#25` 记录（`590e1d09f20e0a2c`）与 `ACCEPTANCE-BASELINE.md` 的 `#25` 块。

### §10.2 预测 vs 实测（本文件 §2 位移表 / §8 停条件）
| 预测 | 实测 | 判定 |
|---|---|---|
| `hbtextline` **一个字节不动**（零世代成本） | `e89fed55fd8e32bc` 未变 | ✅ |
| 五臂**不重取**、`known-red.json` **不重钉** | `generation=#23 tree_gen=same drift=0 gone=0 unregistered=0 caliber=OK`、登记表一字未改 | ✅ |
| `pc` 必变、`inputs_fp` 必变 | `pc 476994e35d31a7e1 → 7374308a00c55572`；`inputs_fp a8703419… → 0b8b6559…`（G1 只改 1 份） | ✅ |
| `bridge`/`wb`/`provider`/`win32shim`/`wic_shim`/`dwf` 不动 | 逐位未动；`pf` 仅由**波尾**重编引起 | ✅ |
| 6 条 `BASELINE … config=pc:` 的 `pc` 那格变 | 六行 `config=pc:7374308a00c55572` | ✅ |
| 应用门禁读数**其余不变** | `6/6 result=PASS`、`drawn=260/144`、`colors=3960/2828`、`cross_ae=0`、`leftover_after=0`、`distinct_origin_y=12 runs=167` | ✅ |
| **停条件 1**（`hbtextline` 变了 ⇒ 停） | **未触发** | ✅ |
| **停条件 2**（五臂需重取/登记表需重钉 ⇒ 停） | **未触发** | ✅ |
| **停条件 3**（阳性对照/已转绿读数变了 ⇒ 停） | **未触发**（`control`/`mod0`/`hidden1`/`hiddenmid` 逐字节未变） | ✅ |
| **停条件 5**（W25B 判据今天报新红 ⇒ 停） | **未触发**（今天 0 条新红） | ✅ |

### §10.3 预登记**没写**、收官时新做或新发现的事（如实登记）
1. **收官补了六条登记 `D-G9`–`D-G14` + 三份可施工草稿**（臂日志结构化登记与只读核对器 / `run_step` 回显 / 产物↔生成物等号读者 / `perChar[].width` / `--selftest` 竞态 / **列级覆盖闸**）。它们的依据全来自本波八条只读车道，**逐条都有实测**（草稿本波不落地）。
2. **`D-R7` 的修法只做了一半 —— 主控顺手补齐**：`patch-presentationcore-lineheight-trace` 已进 `APPLIERS_EXPLICIT`（会跑）但**不在 `applier-audit-expected.txt`** ⇒ 摘掉它**不会红**。已补一行（纯加强：被审计 **22**/必查 **22**，补前 22/21；补后审计 `rc=0`）。**这个洞是顺着 W25K 的数量对账找到的**。
3. **三处文档漂移已修**（都改用**现场实测值**）：步数口径表停在 11 步 ⇒ 补到 13；`appliers=20 ok=74` ⇒ 实测 `22/80`（不带旗标）/ `22/102`（`--with-check`）；基线里"`CURRENT-STATE.md` 第 4 行"⇒ 改为**按 `grep -m1` 定位、不以行号为准**。
4. **主控一次误判已撤回**：曾据进程树点名一条只读车道"越界"跑了 `frame-step.sh`；复核后撤回 —— **`PPID` 定不到车道**（所有车道的 bash 都由同一个 dsh 宿主派生），而 `list_agents` 显示当时唯一在跑的是 **W25A**、它**正是有 `dotnet` 权限的那条**、且**本文件 §2 位移表本就写了"第 `[5]`/`[6]`/`[7]` 步必须重跑"**。留下的两条真教训：**定人不能用 `PPID`**；**"禁止车队"必须连"会自行构建的步骤脚本"一起点名**（`frame-step.sh:94` 自建 harness）。
5. **主控自己的看门狗报了 60 条误报**（阈值 `dotnet>2` 被 `verify-all` 的 `dotnet test` 正常顶到 3–6）⇒ **一个在正常操作上会响的判据是坏判据**（与 `D-G13` 同族）；**内存两档阈值一次没响**。
6. **更正 W25C 的"31 份落后副本"**：那是按 `find` 全枚举数的，**校验器的权威口径是 0**（`sync` 干跑 `refreshed=0`、明说"同类加载源副本都已是权威 sha"）⇒ **不需要 `--apply`**；本波 `APPSYNC` 相对 `#24` **反而收敛**（`MISMATCH 18→1`、`DIVERGENT 3→1`）。
7. **W25D 推翻了主控派单的前提**（三颗牙齿**今天就在自报**，缺的只是 `run_step` 的回显）；**W25H 推翻了主控文档里一句跑了四波的老话**（"RTL 那 33 行全在 `C-rtl-indent`" ⇒ 数目对、归属错：在 `D-paraindent`）；**W25G 更正 W25H/W25I 的档 A 数**（301/170 → **149/94**）；**W25J 把 `D-G9` 的措辞收窄**（"没被登记"→"**没有机器读者**"）；**W25F 撤回一条无效证据**（本机**没装 git**）。**五条车道推翻主控或互相推翻，全部被主控复核后采纳。**

### §10.4 下一波（`#26`）候选（**按价值**）
① **`D-G10`** 落 `run_step` 回显 + 三颗牙齿汇总行（草稿已备、**只加不删**）｜② **`D-G14` 列级闸**（补丁已被主控实测可 `patch`；**不动产品件**，但要按纪律 34 **人工重取三支 `tab-*` 臂**并按列对账）｜③ **`D-G9`** 结构化臂日志 + 只读核对器（**零世代成本、不动 `inputs_fp`**）｜④ **`D-G11`** 产物↔生成物等号读者（`1 笔 pc 重建 + 1 个工具项目`；改 `patch-*.py` ⇒ **必须波前落地**）｜⑤ **`D-G12`** 把 `perChar[].width` 接进比较｜⑥ **`D-T5-R` 变"有牙"**（新写一步）｜⑦ **`D-T7`** 真机重录（配方已备：**真值录"断行后果"、不录码点**；**语料未录时该步必须报 `NOINFO`**）｜⑧ **`hasOverflowed` 的更大判别面**（真值 **1293 行**、今天只判 **421**；`dependentLength` 是唯一"无人看且不冗余"的逐行真值）。
