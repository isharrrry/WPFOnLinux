# 波 `#72` 预登记 —— 「**冻结器与抽取器锚在语义上**」（`TASK-0730` ＋ `TASK-0731`）

- 车道 = **W170A**（`#72` **唯一写者**，波次串行）｜写定于**任何产品/装置改动之前**（判据先行，纪律 1）。
- 权威树 `R=/home/links-dev/netTest/wpf-linux-20260906/wpf-linux`（**不是 git 仓**）；车道工作目录 `~/w170a/`。
- 判据文件 = `~/w170a/criteria.md`（`a557c1f048c10928`，写定时刻 `2026-09-25T22:13:13+08:00`）。

## §1 基点（**落仓前现取**；落仓那刻再取一遍）
| 项 | 值 | 出处 |
|---|---|---|
| `verify-all.sh` | `d5829ded84c7adb3` | `sha256sum \| cut -c1-16` |
| **步数（唯一不变量）** | 首行 `DECL` = **42** == 现取 `grep -c '^run_step "'` = **42** | 纪律 46：`DECL` **行数** = 38 ≠ 42，**没写那条假断言** |
| `build/close-wave.sh` | `247cb3d16e2a5392` | 同上 |
| 覆盖面 | **192**（`192 → 192`，本波不加不减行） | `fp_inputs()` |
| 世代 / 基线 | `gen=#71` / `cc814b708c141ae8` | `sed -n '9p' docs/CURRENT-STATE.md` |
| `w27-freeze.py`（**仓外**） | `c22cfb93c43d23fa`（1,128 行／104,070 B） | 现场算；**不进覆盖面** |
| `column-floor-check.sh` | `2be59234f7266e0f`（783 行／52,716 B） | 现场算；**在覆盖面内**（`close-wave.sh:263`，`verify-all.sh:895 run_step "COLUMN-FLOOR"`） |
| 基线件块头现取 | `^# RE-FROZEN ` **6** 行（未降级）／`^# ⏪ ` **45** 行（降级，**全部**是块头）／合计 **51** = 块数 | `grep -acE`／`grep -ac` |

## §2 判据（**先写**；本波不做回归判定 ⇒ 机读行见下）
`PREREG-NO-REGRESSION-DECISION: yes`

### 2.1 `TASK-0730`：冻结器必须**自动核每个 `prev_*`**
**输入来源声明（纪律 36）** —— 基准值**只**从基线件 `^# RE-FROZEN #<prev>` 块取，**逐条写死**：

| key | 来源（块内） | 命中数要求 |
|---|---|---|
| `prev` | `docs/CURRENT-STATE.md` 的 `> BASELINE-FROZEN gen=` 机器行（**既有断言，保留**） | 1 |
| `prev_pf`／`prev_pc`／`prev_wb`／`prev_wsh`／`prev_dwf` | **唯一**那条 `^# **九位（Release 权威件）**` 行里的 `` `名` `<16hex>` `` | 各 ==1 |
| `prev_bsfp` | `` `BRIDGE_SRC_FP` = `<16hex>` `` | ==1 |
| `prev_infp` | `` `inputs_fp` = `<64hex>` `` | ==1 |

- **交叉核**：`BASELINE tier=` 机读行 `config=` 里凡出现同名键（`pf`／`pc`／`win32shim`…）必须**逐位相符**。
- **判否条件**：任一 key **命中数 ≠ 1**／**取不到值**／**与表项不符** ⇒ **拒冻**（`rc ≠ 0` ∧ **基线件零字节改动**，核验插在**任何写盘之前**）；块尾必须落在**行首锚**上，走到 EOF 找不到 ⇒ 同样拒冻。
- **范围边界（如实）**：本检查只对**待冻代的上一代块**生效。现取：51 个块里只有 `#71`／`#69`／`#68`／`#67` 的来源形态 **8/8 全可抽**（`#70` 及更早缺 `` `inputs_fp` = `` 形态、`#56` 及更早缺九位行）⇒ **更老代会被响亮拒冻**，**绝不**降级成"取不到就放行"。
- **两极化**：A 腿 = 把某个 `prev_*` 故意填错（含 `#67` 事故里的 `prev_pf=cbd1884faeb4837e`）⇒ **拒冻 ∧ 基线件逐字节未变**（**基线件沙箱副本**，绝不动真基线）；B 腿 = 填对 ⇒ **放行**。
- **顺带（同族）**：冻结器**自己**在写盘前留 `B.pre-freeze.<gen>.bak`（`cp -a` 真拷贝 ＋ **逐字节断言** ＋ `%h==1`）。

### 2.2 `TASK-0731`：「找块」抽取器的**出口条件锚到行首**
- 判定点 = `build/MilBridge/tools/column-floor-check.sh` 的 `extract_newest_block()`（改前 `:156-158`；旧锚**逐字** `awk 'BEGIN{seen=0;p=0} /RE-FROZEN #/{ if(seen==1) exit; if(/^# RE-FROZEN /){seen=1;p=1} } p'`）。
- **修法**：出口条件改成**块头行**（行首锚）；**入口语义一字不改**（仍只认未降级块头）。
  ⚠️ **不能只写未降级那一种**：历史块头是**降级形态** ⇒ 只认行首会让抽块**跑到 EOF**（真件实测 **57 → 3573** 行）。
- **两条附加守卫（主控 `#72` 附加硬条件 ①②）**：① 块尾向后找不到 ⇒ `NOINFO reason=block-end-not-found`（**绝不把 EOF 当块尾**）；② 出口锚的**域**必须**现取自证** —— 两种机制数块头（A = 本锚；B = 剥掉 Markdown 行内代码跨度后按"行首 `# ` 且含 `RE-FROZEN` ＋ 井号＋世代号"）必须**相等**，不等 ⇒ `NOINFO reason=block-header-form-mismatch`，**不许静默继续**。
- **三＋一极化**：(a) 块内**非行首**提到该字样 ⇒ **不得**提前截断；(b) 正常档 ⇒ 取到**正确块**（块首/块尾/行数与独立现算边界逐行相符，且真件上新旧输出**逐字节相同**）；(c) **无块** ⇒ `NOINFO`（**不是** `PASS`）；(e) 下一块块头换成**第三种装饰** ⇒ 必须**响亮 `NOINFO`**（点名病因），**不是** `PASS`。
- **四腿进自测（附加硬条件 ③）**：(a)(b)(c)(e) 做成 `--selftest` 用例；**既有例逐字不变、例数只增不减**。
- **判否条件**：任一极化腿取不到成对读数、(b) 出现判定面位移、(c) 出 `PASS` ⇒ 本件作废。
- **边界（如实）**：`#67` 的**原始现场件未留存** ⇒ 「复现 `#67` 原样」= **`NOINFO`**；出口锚是**形态白名单**，域名自证只能保证"第三种形态出现了会被点名"，**不能证明它不存在**。

### 2.3 账与自举
- **落仓件**：`build/MilBridge/tools/column-floor-check.sh`（改，`2be59234f7266e0f → e997d316515e4128`）＋ 本文件（新建）。
- **仓外件**：`~/w21-verify/w27-freeze.py`（`c22cfb93c43d23fa → 56e54035c6e1df1b`）＋ 前像 `w27-freeze.py.bak-w72` ＋ 版本档 `~/w21-verify/versions/w27-freeze.py.w72-56e54035c6e1df1b` ＋ 两份补丁件。**如实标注：它不进覆盖面。**
- **不加步** ⇒ 步数 `42 → 42`；覆盖面 **192 → 192**（无增删行）⇒ **`inputs_fp` 必移**、**逐件归因仅 1 件**（`column-floor-check.sh`）。
- **自举声明（逐字）**：本波改了冻结器 ⇒ **`#72` 的冻结就是它改后的第一次真用**。⇒ ① 先 `cp -p` 备份 `w27-freeze.py.bak-w72`；② 落仓前用新增的 `--prev-check-only` 在**沙箱基线副本**上排练（正常档放行／错值档拒冻 ∧ 副本逐字节未变）；③ 改前/改后 sha16 逐字入本文件与报告。
- **落仓件自检**：写前逐件 `stat -c %h==1`；`temp + rename`；落仓件 `grep -c "$HOME/w"` == **0**；临时件一律 `mktemp -d` ＋ `trap`（纪律 43／44）。
- **`NOINFO` 既不算绿也不算红**；`rc=127/126` = 没跑过；引行号前先打印该行；**不许手抄哈希**。

## §3 冻后更正 · **诚实边界**（`2026-09-25 23:5x` 追加；**原文一字未删**）
- ⚠️ **本波这次冻结（基线 `2eb64610f65a0d07`）发生在"生产调用点接线之前"**：冻结那一刻跑的是**既有**的
  「树世代交叉断言」（`树上 #71 == GENS['#72']['prev']`），而新写的 `check_prev_values()` **只在
  `--prev-check-only` 排练模式里被调过** ⇒ 那一次**生产路径没有自动核 `prev_*`**。
- **冻后已补齐**：`~/w21-verify/w27-freeze.py` **`56e54035c6e1df1b` → `dfe84694c6cf7a25`**（前像
  `w27-freeze.py.bak-w72`；版本档 `versions/w27-freeze.py.w72-56e54035c6e1df1b`（**冻结那一刻用的版本**）
  ＋ `versions/w27-freeze.py.w72b-dfe84694c6cf7a25`（接线后版本）＋ `w27-freeze.py.w72-frozen` 留档）。
  生产路径现在**自己打印** `PREVCHECK=PASS gen=… keys=7 base=…` 或 `PREVCHECK=REFUSE reason=…`；
  **决定性两腿（走生产入口）**：填对 ⇒ `PREVCHECK=PASS` 且执行**继续**到下一个守卫（树世代断言）；
  `prev_pf` 填成 `#67` 事故那个错值 ⇒ `PREVCHECK=REFUSE MISMATCH …` ＋ `TIER-DISAGREE` ＋ **`rc=2`**，
  且**沙箱基线副本 sha16 跑前跑后逐位未变**（`cc814b708c141ae8`）。
- ⇒ **新路径的「首次真冻」将是 `#73`**（`#73` 的放行条件之一 = 生产路径必须打印 `PREVCHECK=PASS`）。
- **本次冻结用到的 7 个 `prev_*` 值确实核过**（同一函数、同一基线 `cc814b708c141ae8`：7 个来源**命中数全 1**、
  `BASELINE tier=` 三键 `agree=yes`、更老代／九位行重复／无块尾三档全 `REFUSE rc=2`）——
  **核过的是"值"，缺的是"生产路径自动执行"这一跳**。冻结产物本身**不受影响**（该检查只读、且在同一基线上判过 PASS）。
- ⚠️ **桩仓口径（如实）**：排练用的是**桩日志 ＋ 桩门禁行 ＋ 基线件沙箱副本**（`--baseline` 测试钩子）；
  冻结器的**仓库根 `R` 是编译期常量**、没做成可覆盖 ⇒ 没有造"整棵桩仓"（那需要桩 `verify-all.sh`／九位件／
  各检查器）。**如实标注**：这**不**等于"整仓端到端排练"，只等于"生产入口 ＋ 真实检查函数 ＋ 真实基线形态"的端到端。
